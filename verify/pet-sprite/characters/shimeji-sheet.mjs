/**
 * shimeji-sheet.mjs — Shimeji 精灵表的**行语义**，唯一来源
 *
 * 谁需要这份知识：
 *   · `import-shimeji.mjs`  —— 切图，按行取动作
 *   · `make-preview.mjs`    —— 预览，把切出来的帧指回原表的格子
 *
 * 抽出来是因为第二个用途出现时，"第几行是什么动作"就有两个读者了。
 * 原样复制一份到预览脚本里也能跑，但表换了排布之后只有一边会改——
 * 那时预览页会把帧指到错误的行上，**画面上看不出错**（高亮的还是某个动作的格子）。
 *
 * 行的含义**起初**取自 `AI-desktop-pets` 的 `PetState` 枚举与
 * `SpriteConfig.DEFAULT_STATES`（`spriteLine` 从 1 起），**但第 3 行是错的**：
 *
 *   | row | 状态  | 帧数 | loop | fps | 来源 |
 *   |-----|-------|------|------|-----|------|
 *   | 0   | STAND | 1    | ✓    | 9   | 参考项目 |
 *   | 1   | WALK  | 4    | ✓    | 9   | 参考项目 |
 *   | 2   | SIT   | 1    | ✓    | 9   | 参考项目 |
 *   | 3   | REST  | 8    | ✓    | 9   | **像素证据修正，见下** |
 *   | 4   | JUMP  | 1    | ✓    | 9   | 参考项目 |
 *   | 5   | FALL  | 3    | ✗    | 9   | 参考项目 |
 *   | 6   | DRAG  | 1    | ✓    | 9   | 参考项目 |
 *   | 7   | CRAWL | 8    | ✓    | 9   | 参考项目 |
 *   | 8   | CLIMB | 8    | ✓    | 9   | 参考项目 |
 *
 * ## ⚠ 第 3 行修正（2026-09-21）
 *
 * 参考项目把它叫 `GREET`（招手），**是错的**——它画的是一段**待机动画**。
 * 三条像素证据（`docs/temp/shimeji_caneko.png` 第 3 行，8 格逐帧量）：
 *
 *   1. 8 帧里只有 5 个姿势，且**成对重复**（帧 0/1、3/4、5/6 各自逐像素相同）——
 *      慢速待机的典型特征；招手要做连贯动作，不会出现重复帧。
 *   2. 帧 7 回到帧 0（平均 alpha 差 0.50，而帧内两两差异是 0.96–7.09）⇒ **是循环**，
 *      而参考项目抄来的 `loop` 标的是 `✗`。
 *   3. 内容**右缘恒定在 x=89、只向左扩**（左缘 43 → 18）。人物头朝右，招手该往
 *      **右**伸；往左动的是背后（这套素材是猫，像尾巴）。
 *
 * **2026-09-22 补充：是待机没错，但幅度不适合当待机。** 8 帧里有 2 帧是**趴着**的
 * （内容顶边 95），另外 6 帧是坐起来（顶边 78-87），格内上下跨度 17px。这条行
 * 因此一度被接给 `Idle`（2026-09-21），次日又撤下——用户报「看着头晕」。
 * **目前没有任何组用它**（`ROWS.REST` / `DECLARED.REST` 保留，用于描述这张表本身）。
 *
 * **教训（别再犯）**：这个文件原本写着「`sheet-rows.mjs` 独立量出来的帧数九行全部
 * 对上（1/4/1/8/1/3/1/8/8），所以这份语义是验证过的，不是照抄的」——**帧数对上不等于
 * 语义对上**，任何 8 帧的动作都能对上 `DECLARED` 里的那个 8。那次"验证"只验了帧数，
 * 语义整个是照抄的。行语义只能靠**看画面/量姿态**确认。
 * 同理，「这一行是待机动画」也不等于「它适合当待机」——**幅度也要量**
 *（`measure-idle-motion.mjs` 就是为此写的）。
 */

/** 表的来处。外部项目路径，不在本仓库里——找不到时调用方自行降级 */
export const SHEET_DIR =
  process.env.SHEET_DIR ??
  'C:/myself/projects/my/open-source/AI-desktop-pets/app/src/main/res/drawable-nodpi'

/** 格边长。这套素材是固定 128 的方格，表宽高都必须是它的整数倍 */
export const CELL = 128

/** 行语义（0 起，与 `spriteLine - 1` 对应）。出处与第 3 行的修正见文件头。 */
export const ROWS = {
  STAND: 0,
  WALK: 1,
  SIT: 2,
  REST: 3,
  JUMP: 4,
  FALL: 5,
  DRAG: 6,
  CRAWL: 7,
  CLIMB: 8,
}

/**
 * 每行的**声明帧数**，取自 `AI-desktop-pets` 的 `PetState` / `DEFAULT_STATES`。
 *
 * ⚠ 为什么不能只按「这一行有几格有内容」数帧——**踩过**：
 * `shimeji_nekojapan.png` 的第 0 行有 9 格，但 9 格的包围盒全等（45×36）、
 * 两两姿态差异 0.0，也就是**把同一帧复制填满了整行**。
 * 按内容数会得出「待机有 9 帧」，而它声明的 `frameMax` 是 1。
 * 所以这里以声明值为准，内容只用来**交叉校验**：少了报缺帧，多了当填充丢掉。
 */
export const DECLARED = {
  STAND: 1,
  WALK: 4,
  SIT: 1,
  REST: 8,
  JUMP: 1,
  FALL: 3,
  DRAG: 1,
  CRAWL: 8,
  CLIMB: 8,
}

export const p2 = (i) => String(i).padStart(2, '0')

/**
 * 出哪些动作组。
 *
 * `Idle` 用**单帧 STAND** 而不是某个循环行：单帧正好把「角色动不动」全部交给
 * 程序化原语（`bob`/`breathe`），这是 Lumii 的一条核心下注，值得单独验。
 * `WALK` 那 4 帧则提供一条**真·多帧循环**，用来验逐帧时长与循环衔接。
 *
 * `Sit` / `Fall` / `Picked` 是 2026-09-21 为**自主行为**（R9）补的：
 * 桌宠要能自己坐下、掉落、被拎起来。组名不是随手取的——
 * `Picked` 正是 `PetOrchestrator.playConventionalMotion('Picked')` 找的名字，
 * 命名对上，`setPicked(true)` 那条既有链路不用改一行就能播。
 *
 * 三个「不」：
 * - `Fall` **是 loop 不是 once**：`once` 的语义是"播完回 next"，可掉落的结束时间是
 *   **物理事实**（什么时候落地），不是动画时长。这 3 帧只有 333ms，用 once 会出现
 *   「还在空中就站起来了」。参考项目的 FALL 就是 `loop=false` + 播完即切走，
 *   实测表现正是落地前姿势就没了。用 loop 让下落全程姿势正确，落地由物理层切走。
 * - `Fall` **不 pin**：Idle Pin 的语义是"首末帧锚到待机让接回不跳"，
 *   而掉落的末帧不是待机，锚了反而会闪一下。
 * - `params` 只给**长时间维持**的姿态（Idle/Sit）。走路的 4 帧本身就是动画，
 *   再叠浮动会变成"边抖边走"。
 */
export const GROUPS = [
  {
    group: 'Idle',
    from: 'STAND',
    kind: 'loop',
    fps: 9,
    clip: (id) => `${id}_stand_00`,
    // 待机**用单帧**（2026-09-22 改回；此前是 REST 的 8 帧）。
    //
    // 起因是用户报「待机的上下浮动看着头晕」。量了一下
    //（`measure-idle-motion.mjs`）：REST 那 8 帧并不是"轻微呼吸"，而是**趴下—起身**——
    // 帧 0/1 趴着（内容顶边 95），帧 3/4 坐起来（顶边 78），格内跨度 17px。
    // 这套素材的 `scale` 是 2.2，屏幕上就是 **37px 的上下往复**，3fps 一轮 2.67 秒，
    // 一直在动。换成单帧之后，垂直方向只剩 `breathe`（2% 缩放 ≈ 2px）。
    //
    // "角色动不动"由此全部交回程序化原语——这正是本文件开头对 Idle 写下的原设计，
    // 2026-09-21 换成 REST 多帧时被推翻了，现在按实测改回来。
    //
    // **`bob` 与 `sway` 都没有**（2026-09-22 去掉）：用户的原话是「待机不需要左右和
    // 上下移动，待机有自己的动画，需要添加特效」——待机该是**站在原地**的，
    // 位移与摆动都不算"有动画"，该用动画和特效去表达。
    // 实测 `bob: 9` 不是 9px 而是**屏幕上 18px 的上下摆动**（偏移量在 stage 坐标里施加，
    // 不乘 scale），确实到"看着头晕"的量级。
    // 保留 `breathe`（2% 缩放，以脚底为锚点，头顶动 ~2px）与素材自带的眨眼。
    params: () => ({ breathe: 1.02 }),
  },
  {
    group: 'Walk',
    from: 'WALK',
    kind: 'loop',
    // ⚠️ 4fps 而不是参考项目的 9fps（2026-09-21）。
    //
    // 它的行走是 4 帧 @9fps + 60px/s，而我们**这两个数原本一模一样**——差别在体型：
    // 它的宠物 128px 直接显示，我们的 `scale` 是 2.2。同样的腿频配同样的速度，
    // 在 2.2 倍的身体上就成了"原地小跑"：它每轮走 0.21 个身位，我们只走 0.094。
    //
    // 4fps（0.9 秒一轮）把"每轮走的距离 ÷ 体高"拉回参考项目的比例。
    // 代价是 4 帧动画慢放到 1 秒一轮，可能显得顿——先这样跑，观感不对再调。
    fps: 4,
    clip: (id, i) => `${id}_walk_${p2(i)}`,
  },
  {
    group: 'Sit',
    from: 'SIT',
    kind: 'loop',
    fps: 9,
    clip: (id, i) => `${id}_sit_${p2(i)}`,
    // 坐着要比站着更静：只有呼吸，没有晃动
    params: () => ({ breathe: 1.015 }),
  },
  { group: 'Talk', from: 'STAND', kind: 'loop', fps: 9, clip: (id) => `${id}_stand_00` },
  // 这里原本有个 `Wave` 组，from 指到 GREET（第 3 行）——**整个是虚构的**：
  // 第 3 行画的是待机（见文件头的修正），素材里根本没有招手动作。
  // 该组从落地起就没有调用者（实测日志里从没出现过 `[playMotion] group="Wave"`），
  // 2026-09-21 删除；第 3 行一度改由 Idle 使用，2026-09-22 又因幅度太大撤下。
  // 于是第 3 行现在**没有组用它**——不是漏了，是量过之后不用（见 Idle 那段）。
  // 另：`pin: true`（Idle Pin 首末帧锚定）随之暂时无组使用，机制保留在
  // import-shimeji.mjs 里，将来有真正的一次性动作（如招手素材到位）可直接开。
  { group: 'Fall', from: 'FALL', kind: 'loop', fps: 9, clip: (id, i) => `${id}_fall_${p2(i)}` },
  { group: 'Picked', from: 'DRAG', kind: 'loop', fps: 9, clip: (id, i) => `${id}_drag_${p2(i)}` },
  // 跳跃：素材只有 **1 帧**（"跳起来"的瞬间姿势，底边比别的行高 28px）。
  // 所以用 `holdMs` 把它停久一点——一次蹦跳本来就是个瞬间，但"蹦"要看得见。
  // 700ms → 300ms：单帧动作停太久反而像卡住，300ms 是"看得见又干脆"的量级。
  {
    group: 'Jump',
    from: 'JUMP',
    kind: 'once',
    next: 'Idle',
    fps: 9,
    holdMs: 300,
    clip: (id, i) => `${id}_jump_${p2(i)}`,
  },
  // 攀爬：爬到程序主窗口的边缘上（见 `pet-core` 的 `perch`）。两行各 8 帧，
  // 是这套素材里帧数最多的动作——爬行本来就比走路需要更多中间帧才不显得跳。
  { group: 'Climb', from: 'CLIMB', kind: 'loop', fps: 9, clip: (id, i) => `${id}_climb_${p2(i)}` },
  // ⚠️ **这一行不要翻转**（2026-09-21 改）。
  //
  // 原先这里开着 `flipY: true`，理由是"素材里是正着横躺的猫，而用途是倒挂，
  // 所以替它翻过来"。**这个前提是错的**：CRAWL 行本来就是**脚底向上**画好的倒挂
  // 姿态——逐帧包围盒实测，内容贴帧**顶**（y 0..40）。再翻一次等于把它翻正，
  // 宠物就成了"头朝上吊在天花板上"。
  //
  // 当时唯一的依据是参考项目 `SpriteAnimationView.calculateFlip` 的注释写着
  // "垂直翻转：因为是倒挂着的，头朝下"，并据此断定它代码里那句 `false` 是它自己的
  // bug。反了——那个 `false` 是对的，原图不需要翻转。
  //
  // 代价：本行的锚点语义与别的行不再统一（别的内容贴帧底，这里贴帧顶），
  // 由 `perchGaps.ceiling` 表达（内容顶边到锚点的距离 ÷ 画布高 ≈ 0.99）。
  {
    group: 'Crawl',
    from: 'CRAWL',
    kind: 'loop',
    fps: 9,
    clip: (id, i) => `${id}_crawl_${p2(i)}`,
  },
]
