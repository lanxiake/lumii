const fs = require('fs')

// ── 1. 验证计划 ──────────────────────────────────────────────
{
  const p = 'docs/plans/客户端UI/2026-09-20-宠物自制系统技术验证计划.md'
  let s = fs.readFileSync(p, 'utf8')
  const rep = (a, b) => { if (!s.includes(a)) { console.error('[计划] 未匹配: ' + a.slice(0, 60)); process.exit(1) } s = s.replace(a, b) }

  rep(
    '> **执行状态（2026-09-20）**：T1–T5、T7 已完成；T6（图生图参考链）**待用户确认后执行**（产生生图费用）。\n> 验证结论：**A / B / C 全部通过，主路径成立**。报告见 [`../../test/pet-sprite/验证报告.md`](../../test/pet-sprite/验证报告.md)。',
    '> **执行状态（2026-09-20）**：**T1–T7 全部完成**（含 T6）。\n> 验证结论：**A / B / C 通过；D 部分通过但给出更优路线**。报告见 [`../../test/pet-sprite/验证报告.md`](../../test/pet-sprite/验证报告.md)。\n>\n> 追加实验（用户提出）：**直出图集**（一次生成多姿态）—— 已证明可用，且优于链式图生图，见报告 §5之二。'
  )

  rep(
    '- [ ] T6（可选）：**未执行** — 需生图费用，待用户确认',
    '- [x] T6（可选）：**已执行** — 4 帧参考链 + 1 张直出图集。结论：单跳保真 91–93%，3 跳降至 73%，**漂移累积且参考图压过文字提示**，不可作量产手段；改为推荐「直出图集」路线'
  )
  rep(
    '- [x] 明确回答：**未验证**（T6 未执行）',
    '- [x] 明确回答：**链式图生图不可作量产手段**（漂移累积）；**直出图集可用**（需逐格归一化）'
  )

  rep(
    '## 十一、验证命令汇总',
    `## 十之二、追加实验 — 直出图集

**触发**：用户在 T6 执行期间提出「可以让生图模型直接产出目标格式，现在的生图模型很厉害」。

**做法**：一次生成 2×2 图集（站立/挥手/坐下/跳跃，四格均为纯品红底）。

**结果**：S1 不越界 ✓ / S4 角色一致 93.3% ✓ / S5 抠底可用 ✓；S2 格子对齐 ✗ / S3 尺度一致 ✗（软要求）。

**判定：可用，但需逐格归一化。** 软要求不达标不影响可用性 —— 精灵流水线本就要求逐格抠底 + 按共同地线对齐（该步骤已在 A3 验证，误差 0px）。

**意义**：一致性由单次生成天然保证，且成本为链式的 1/N。**这是后续素材生产的推荐路线。**

验证脚本：\`verify/pet-sprite/check-sprite-sheet.mjs <sheet.png> [cols] [rows]\`

---

## 十一、验证命令汇总`
  )

  rep(
    '```bash\n# 素材生成（确定性）',
    `\`\`\`bash
# 真实 AI 出图的抠底补验（关闭报告 §6 适用边界）
node verify/pet-sprite/check-real-cutout.mjs <img1.png> <img2.png> ...

# 直出图集验证
node verify/pet-sprite/check-sprite-sheet.mjs <sheet.png> 2 2

# 参考链一致性
node verify/pet-sprite/check-reference-chain.mjs <f1> <f2> <f3> [f4]

# 素材生成（确定性）`
  )

  fs.writeFileSync(p, s)
  console.log('✓ 验证计划已更新')
}

// ── 2. 设计文档 ──────────────────────────────────────────────
{
  const p = 'docs/design/客户端UI/2026-09-20-虚拟人精灵图渲染后端设计.md'
  let s = fs.readFileSync(p, 'utf8')
  const rep = (a, b) => { if (!s.includes(a)) { console.error('[设计] 未匹配: ' + a.slice(0, 60)); process.exit(1) } s = s.replace(a, b) }

  rep(
    '图生图参考链（以上一帧为参考生成下一帧）是序列帧唯一可能成立的路径，但存在**误差累积**风险，列为待验证的加分项（验证点 D），不作为主路径依赖。',
    `图生图参考链（以上一帧为参考生成下一帧）是序列帧唯一可能成立的路径。**已实测（验证点 D）**：

| 对比 | 剪影 IoU |
| --- | --- |
| 相邻一跳 | **91–93%** |
| 相隔三跳 | **73%** |

**每跳忠实，但漂移累积且不自纠正**；且**参考图会压过文字提示** —— 实测要求「回到站立」时，模型仍保持参考帧的坐姿比例（与参考帧 IoU 93.1%）。

→ **链式图生图不可作量产手段**（除非改为始终参考同一张锚点图）。

**已实测的更优路线 —— 直出图集**：让模型**一次生成多姿态图集**（如 2×2 四格）。实测四格角色一致（调色板重合 93.3%）、互不越界（边界带非角色像素 0），抠底可用；仅取景与尺度不统一，而这正是精灵流水线本就要处理的步骤（逐格抠底 + 按共同地线对齐，已在验证点 A3 验证误差 0px）。

→ **素材生产推荐走直出图集**：一致性由单次生成天然保证，成本为链式的 1/N。`
  )

  rep(
    '| **图生图序列帧误差累积** | 生成的帧序列不可用 | 列为验证点 D（加分项）；失败则放弃生成帧，不影响主路径 |',
    '| **图生图序列帧误差累积** | 生成的帧序列不可用 | **已实测确认存在**：3 跳后 IoU 降至 73%，且参考图压过文字提示。**缓解：改用直出图集**（已验证可用，见 §3.2） |'
  )

  rep(
    '**结果**：A / B / C 全部通过，D 未执行（需生图费用，待确认）。详见 [验证报告](../../test/pet-sprite/验证报告.md)。**结论：主路径成立，可进入 P0-a。**',
    '**结果**：A / B / C 通过；**D 部分通过但给出更优路线**（链式图生图漂移累积 → 改走直出图集）。另已完成「真实 AI 出图抠底补验」，关闭了报告原标注的适用边界。详见 [验证报告](../../test/pet-sprite/验证报告.md)。**结论：主路径成立，可进入 P0-a。**'
  )

  fs.writeFileSync(p, s)
  console.log('✓ 设计文档已更新')
}
