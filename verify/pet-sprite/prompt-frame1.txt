请只做一件事：调用 image_generate 工具生成一张图片。

参数必须严格如下（prompt 请**原样照传**，不要改写、不要翻译、不要增删内容）：

- filename: "pet_frame1"
- width: 1024
- height: 1024
- prompt: "pixel art game sprite of a small orange tabby cat standing upright on two legs, full body front view, simple flat colors, thick dark brown outline, centered composition, on a flat solid magenta background exact color #D9218F, no shadow, no gradient, no text"

调用完成后，把工具返回的 filePath **原样**贴出来即可。不要做任何其他事，不要写文件，不要解释。
