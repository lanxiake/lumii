# Windows 打包体积治理 — 结论与规则

> 日期：2026-09-21
> 范围：`apps/windows` 的 electron-builder 打包链路（Windows 为主，Linux 侧同步受益）
> 状态：已实施并验收；**正式链路（`pnpm package:win`）复验通过**，见 §7（提交 `13908728` /
> `5d79cf44` / `e6de7310` / `e981537f` / `98d3b260` / `cc76daec`）
> 起因：跑「Windows 门槛 3」时发现产物不可用（electron-builder 缺陷，见
> [`../../plans/Linux客户端移植/2026-09-19-Linux桌面客户端第一期实施计划.md`](../../plans/Linux客户端移植/2026-09-19-Linux桌面客户端第一期实施计划.md) §9），
> 顺着查出「包体比记忆中的 ~170 MB 大了 80 MB」。**膨胀是静默的**——命令退出码 0、没有任何报错。

## 1. 结论摘要

| 指标 | 治理前 | 治理后 |
| --- | --- | --- |
| 安装包 `Lumii-Setup-*.exe` | 253.9 MiB | **156.9 MiB**（−38%） |
| `app.asar` 数据区 | 595.2 MB | **120.5 MB** |
| `release/win-unpacked` | — | 598 → 531 MB |

功能逐项实测无回退（判据见 §4）：asar 内 package.json 全可解析、启动四条就绪标记齐、
**Wiki/记忆嵌入仍走真 ONNX**（`backend:"transformers"`）、PDF 文本提取正常（118 页中文）。

## 2. 排掉了什么（按发现顺序，均为实测数字）

| # | 项 | 文件量 | 为什么能排 |
| --- | --- | --- | --- |
| 1 | `.mtbot/tool-results/**` | 250 MB | agent 运行时的工具结果落盘（默认 `<cwd>/.mtbot`，dev 模式下 cwd 就是 `apps/windows`）。被 `.gitignore` 忽略，但**没被 electron-builder 排除** → 整包进 asar |
| 2 | `src/`、`assets/`、`bundled-skills/`、`resources/`、`pet-lab/`、`docs/` | ~43 MB | 生产环境一律走 `process.resourcesPath`（extraResources 那份），asar 里这份是重复/开发用 |
| 3 | 渲染层专用包：`echarts`/`zrender`/`pptx-preview`/`lottie-web`/`pixi.js`/`@pixi/**`/`pixi-live2d-display` | ~90 MB | vite 已把它们打进 `out/renderer`；三条判据全为 0（主进程产物无 require、源码/技能无 import、渲染层 chunk 里确实含它们） |
| 4 | `onnxruntime-web` | 66 MB | `@xenova/transformers` 顶层静态 import 它，但 Node 分支只用 `onnxruntime-node`；用 2 KB **占位包**顶替（真被调用会明确抛错，不静默降级） |
| 5 | `**/*.map`、`pdf-parse/dist/pdf-parse/web`、`@larksuiteoapi/node-sdk/es`、`@napi-rs/canvas` | ~130 MB | 依次是：sourcemap（仅 devtools）、浏览器构建（应用走 esm）、ESM 构建（主进程 require 走 lib）、`pdf-parse` 的**可选**依赖（只用于 PDF 渲染成图，本应用只做文本提取） |
| 6 | 开发文件：`.env*`、`*.config.ts`、`tsconfig.json`、`paths.ts`、`agent-harness/`、`scripts/`、`build.log` | ~0.3 MB | 不在运行路径上；`.env*` 属**不该进包**的东西 |
| 7 | `@xenova/transformers/dist/ort-wasm*.wasm` | 36.6 MB | onnxruntime-web 的 wasm，只在浏览器分支加载；配置层面排不掉（见 §3 真相 2），改用 `beforePack` 钩子在打包前从 node_modules 删（`pnpm install` 可恢复） |

第二轮做的是**文件级审计**（`find -printf` 按大小排序 + asar 内按扩展名/目录名汇总），又找出这些：

| # | 项 | 文件量 | 为什么能排 |
| --- | --- | --- | --- |
| 8 | `better-sqlite3/deps/**` + `src/**` | ~10 MB | SQLite 合并源码（`sqlite3.c` 9.1 MB）与 C++ 源码，**只在编译期**用；运行期只需要 `build/Release/better_sqlite3.node` |
| 9 | `node_modules/@mtbot/**` | ~4.5 MB | 三个产物（`out/main`、`out/preload`、`out/renderer`）里 `@mtbot/` **零出现**（workspace 包已被内联打包），`bundled-skills` 也不 require 它们 |
| 10 | openai / @mistralai / zod / @anthropic-ai 的 `src/**.ts` | ~5 MB | 这些包的入口都在 `dist`，`src` 是 TS 源码。**不能用全局 `!**/src/**`** —— `@xenova/transformers` 的入口恰好在 `src/`（`main: ./src/transformers.js`），只能逐个包列 |
| 11 | `@xenova/transformers/dist/*.map` | 4.0 MB | 全局 `!**/*.map` 兜不住：该包在顶层 `files` 里被单独 include 过，需要针对它再写一条 |
| 12 | `onnxruntime-node/bin/napi-v3/*/arm64/**` | 8.9 MB | 异构架原生库（本仓只出 x64）；`files` 排除实测无效（见 §3 真相 2 补充），改用 `beforePack` 钩子删 |
| 13 | `*.d.mts` / `*.d.cts` | ~1 MB | 类型声明 —— electron-builder 的默认排除项里有 `.d.ts`，但没有这两个新后缀 |

## 3. electron-builder 的三条配置真相（都实测过，反直觉）

### 真相 1：`files` 的**顶层**模式只作用于 node_modules，**平台段**才作用于 app 目录

`release/builder-debug.yml` 里两套模式分得很清楚：

- `firstOrDefaultFilePatterns`（app 目录文件集）= `**/*` + `!**/node_modules/**` + 输出目录 + **平台段 `files` 的排除项**
- `nodeModuleFilePatterns`（node_modules 集）= `**/*` + **顶层 `files` 的全部模式**

踩过的坑：`!.mtbot/**` 写在顶层，重建后 asar 里 `.mtbot` 纹丝不动 —— 必须写进
`win.files` / `linux.files`。

### 真相 2：`asarUnpack` 是**第二套白名单**，`files` 的排除管不到它

`@xenova/transformers/dist/ort-wasm*.wasm` 三条路都试过：

| 配置 | 结果 |
| --- | --- |
| `files` 里 `!...dist/*.wasm` | **不生效** |
| 加上 `asarUnpack: node_modules/@xenova/transformers/**/*` | 被解包到 `app.asar.unpacked`（`@xenova` 共 45 MB） |
| 去掉那条 asarUnpack | 又整个塞进 `app.asar`（130 → 176 MB）——**安装包大小一模一样** |

即：`asarUnpack` 的模式同时充当「包含」规则，与 `files` 的排除是两套，压不到一起。
另外实测：`asarUnpack` 里写 `!` 取反只改变「解包与否」，不改变「是否进包」。

**同一个坑的另一个实例**：`onnxruntime-node/bin/napi-v3/*/arm64/**` 写进 `files` 排除后**照样随包**。
模式本身没问题 —— 单独用 `minimatch` 验过：`!.../arm64/**` 对 arm64 路径返回 `false`（= 排除）、
对 x64 路径返回 `true`，语义正确；问题在文件集不受这条排除约束（同目录下有文件命中 `asarUnpack`
的 `bin/napi-v3/*/x64/*` 时尤其明显）。所以第 7、12 两项都收进 `build-resources/before-pack.js`：
**打包前从 node_modules 删源文件**，`pnpm install` 按 store 恢复。钩子里每一步都打日志并带体积。

**顺带确认**：wasm 留在 asar 里由 Node 原生读没问题（Linux 那轮已验）。真正需要「真文件」的
只有 `node_modules/silk-wasm/lib/silk.wasm`（232 KB），所以顶层 asarUnpack 已从 `**/*.wasm`
收窄成它。

### 真相 3：只自动排除**当前**输出目录，兜底目录会把上一轮产物打进下一轮

`package-app.js` 在 `release/` 被占用时会兜底输出到 `release-build-<时间戳>/`，此时陈旧的
`release/` 不再被排除 → 被当 app 内容整包打进去（实测一次 **864 MB**）。已在平台段用
`!release*{,/**/*}` 统一覆盖（含 `release`、`release-build-*`、自建的 `release-trim` 等）。

## 4. 判据（可复跑）

`apps/windows/scripts/verify-win-package.mjs`（npm script `verify:win-package`，子命令
`asar` / `launch` / `all`，可用 `--release-dir` 指向兜底目录）：

| 子命令 | 判据 | 能抓到的缺陷 |
| --- | --- | --- |
| `asar` | 逐个 `package.json` 可解析；条目声明不超数据区；**体积护栏**（出现 `out/`、`node_modules/`、`package.json`、`config/` 之外的顶层项，或数据区 > 250 MB 即报错）；打印体积构成前 8 名 | asar 损坏（内容错位）、产物目录/开发文件混入、包体异常膨胀 |
| `launch` | 隔离数据根起 `win-unpacked/Lumii.exe`，等四条就绪标记（`日志系统已初始化`/`React 应用已挂载`/`窗口准备就绪`/`创建系统托盘`）、`Cannot find module` 计数 0、控制口就绪 | 打包产物根本起不来（今天真实发生过：只弹一个标题为 `Error` 的对话框） |

功能级判据（本轮新增的两条，用来证明「排掉的东西真的没人用」）：

- **嵌入**：`lumii-ui wiki vector rebuild` 必须返回 `backend:"transformers"`（不是 `bigram-hash`），
  日志出现 `[wiki-embedder] 已加载`
- **PDF**：用产物内的 pdf-parse 解析一份真实 PDF（走应用实际用的 esm 路径），页数与文本正确

> 判据本身也要拿已知反例试一遍：`asar` 用 ① 那份坏 asar 跑过，两条判据都拦下（退出码 1）。

## 5. 明确排不掉的（别再试，都有依据）

| 项 | 体积 | 依据 |
| --- | --- | --- |
| `@ffmpeg-installer/win32-x64` | 62 MB | ffmpeg.exe 本体（只装了 win32-x64，无跨平台浪费） |
| `sherpa-onnx-win-x64` | 22 MB | 语音 VAD/ASR |
| `onnxruntime-node` | 19 MB | 嵌入用的原生 ONNX（注意与 sherpa 的同名 DLL 顺序闸，见[同目录另一篇](2026-09-18-原生ONNX运行时加载顺序闸.md)） |
| `@img/sharp-win32-x64` | 19 MB | 图像处理 |
| `better-sqlite3` | 12 MB | 主数据库 |
| `@esbuild` | 12 MB | `src/main/ts-runner.ts` 运行时转译 TS 技能 |
| `out/` | 28.5 MB | 应用本体（含渲染层 chunk） |
| `@larksuiteoapi/node-sdk` / `playwright-core` / `pdf-parse` | 8.4 / 7.2 / 6.5 MB | 渠道、浏览器控制、文档解析 |

更激进的压缩要换策略（例如把 onnxruntime-web 的静态 import 从 transformers 里彻底摘掉、
或把嵌入模型改成纯按需下载），不是配置层面能做的。

## 6. 复发风险

- **`.mtbot/tool-results` 会继续长**：每次 dev 跑 agent 都往里写（单个结果可达 47 MB）。
  现在已排除出包，但它仍会占仓库磁盘；根治要让它默认落到客户端数据根（`.lumii`），属
  `packages/agent-runtime` 的改动，未做。
- **各种产物目录**：`release/`、`release-build-*/`、临时自建的 `release-*/` 都可能残留，
  已用 `!release*{,/**/*}` 兜住，但**新增别的名字的产物目录要记得加**。
- **新增依赖时**：先按 §2 第 3 条的「三条判据」判断它是否只在渲染层用；平台二进制注意
  §3 真相 2 的白名单语义。

## 7. 正式链路复验（2026-09-21，`pnpm package:win`）

§2 的迭代都是「改配置 → 直接调 `electron-builder` 打包」，收尾时用**正式命令**完整跑了一遍
（含 `pnpm install --frozen-lockfile` + `electron-vite build` + `beforePack` 钩子 + NSIS），
确认两条路径等价、且钩子在真实链路里也生效。

```bash
pnpm package:win -- --skip-draw-check   # 本机 config/draw-config.json 未填 key，用脚本自带开关
```

| 项 | 值 |
| --- | --- |
| 产物 | `apps/windows/release-build-<ts>/Lumii-Setup-0.1.4-x64.exe` |
| 安装包 | 164,535,530 字节 = **156.9 MiB** |
| `app.asar` | 120.5 MiB（内部零 wasm） |
| sha256 | `5e77afea4e99b116577f66e399a5dcb4b039fa0edea25d7d24713d23a1315639` |

（§2 第一轮排完时是 162.3 MiB；上表是**做完第二轮（第 8–13 项）之后**的正式复验结果。
两轮各跑过一次 `pnpm package:win`，口径相同、可直接对照。）

四项判据全部**对这份正式产物**复跑：

| 判据 | 结果 |
| --- | --- |
| `asar` 体检（含体积护栏） | ✓ 450 个 package.json 全可解析、数据区自洽、无越界顶层项 |
| `launch` 启动冒烟 | ✓ 四条就绪标记齐、`Cannot find module` 0、控制口 2s 就绪 |
| 嵌入 `wiki vector rebuild` | ✓ `backend:"transformers"`（真 ONNX，未退回 bigram） |
| PDF 文本提取（应用实际走的 esm 路径） | ✓ 118 页、中文正常 |

**只有正式链路才验得到的一点**：第 2 步的 `pnpm install --frozen-lockfile` 会把 §2 第 7 条那 4 个
浏览器 wasm 从 store **恢复回来**，随后 `beforePack` 钩子在打包前又删一次 —— 最终产物在 asar 内与
解包目录**两处都零 wasm**，而 `silk.wasm` 仍正常解包。即「install → 钩子 → 打包」这条链路自洽，
**不依赖手工制造的 node_modules 状态**。与直接调 electron-builder 的结果相差 4.7 KB
（构建时间戳差异），两条路径等价。

> 输出落在 `release-build-<ts>` 而非 `release/`：`release/` 被文件系统过滤器（杀软）锁着，
> `package-app.js` 自动兜底切目录 —— 这条兜底路径的输出目录已被 `!release*{,/**/*}` 覆盖，
> 不会再出现「上一轮产物被打进这一轮」的自我污染。
