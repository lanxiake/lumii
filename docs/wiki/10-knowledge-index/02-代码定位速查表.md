# 代码定位速查表

本速查表按**功能域**组织，每条给出：**绝对文件路径** + **关键类 / 函数名** + **简要职责**。目标：新人遇到问题 ≤3 跳定位到实现位置。

---

## 1. 应用启动三层入口（Main / Preload / Renderer）

| 功能 | 绝对路径 | 关键类 / 函数 | 简要职责 |
|------|----------|---------------|----------|
| 主进程入口 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\index.ts` | `createWindow()` / `app.whenReady()` | 创建 BrowserWindow、加载 preload、挂载 Tray、启动 IPC handler 注册表 |
| 主进程 DB 入口 | `c:\myself\projects\my\open-source\lumii\packages\agent-runtime\src\storage\local-database.ts` | `class LocalDatabase` / `migrate()` / `getInstance()` | SQLite 单例连接、`PRAGMA user_version` 读写、迁移管道调度、真相表建表 |
| 托盘管理 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\tray-manager.ts`（或同级 `tray-manager/`） | `class TrayManager` / `buildContextMenu()` | 托盘图标、右键菜单（显示/设置/退出）、托盘气泡通知（Tray 反馈通道） |
| 自动更新 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\updater-service.ts` | `class UpdaterService` / `checkForUpdates()` | electron-updater 封装、更新通道（beta/stable）、下载进度 IPC 广播 |
| Preload 桥总入口 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\preload\index.ts` | `contextBridge.exposeInMainWorld('electronAPI', {...})` | 窄暴露通道：按域薄封装 `ipcRenderer.invoke`，禁止直接传递 ipcRenderer 对象 |
| Preload 类型声明 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\preload\electron-api.d.ts` | `interface Window { readonly electronAPI: ElectronAPI }` + `interface ElectronAPI` | Renderer 编译期类型安全：所有通道的入参 / 返回值 TS 定义（与①③三处同步铁律） |
| Renderer 入口 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\renderer\main.tsx` | `ReactDOM.createRoot().render(<AppProviders><App /></AppProviders>)` | React 根挂载、StrictMode、Suspense、全局 ErrorBoundary 安装 |
| Renderer 路由与 Providers | `c:\myself\projects\my\open-source\lumii\apps\windows\src\renderer\App.tsx` | `App()` / `AppProviders()` | 6 个全局 Context Provider 组合顺序、HashRouter 安装、主题 [data-theme] 属性注入 |
| HashRouter 集中配置 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\renderer\routes\index.tsx` | `routes: RouteObject[]` / `createHashRouter()` | 14+ 页面路由、懒加载 `React.lazy`、每个路由强制包裹 `PageErrorBoundary` |

---

## 2. Agent 协调三核心（Orchestrator / Broker / Tool Registry）

| 功能 | 绝对路径 | 关键类 / 函数 | 简要职责 |
|------|----------|---------------|----------|
| 主入口 & 内核创建 | `c:\myself\projects\my\open-source\lumii\packages\agent-runtime\src\kernel\index.ts` + `create-pi-agent.ts` | `createPiAgent()` / `interface PiAgentKernel` | 编排器工厂：组装 ModelRouter + ToolRegistry + MemoryPipeline + PermissionChecker，返回可跑 turn 的内核实例 |
| 内核适配层 | `c:\myself\projects\my\open-source\lumii\packages\agent-runtime\src\kernel\pi-agent-kernel-adapter.ts` | `class PiAgentKernelAdapter` | 把通用 Kernel 适配到 Electron 侧生命周期；实现 `runTurn()` / `abort()` / `onEvent()` |
| 内核 Fake（测试桩） | `c:\myself\projects\my\open-source\lumii\packages\agent-runtime\src\kernel\fake-agent-kernel.ts` | `class FakeAgentKernel` | 确定性测试桩：不联网、回合按脚本 reply，用于 Vitest 200+ 单测不卡网络 |
| Agent 桥（Electron 侧） | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge.ts` + `index.ts` | `class AgentBridge` / `startAgent()` / `sendUserMessage()` | Main 侧持有 Kernel 实例；接收 Renderer `agent:*` IPC；广播 agent events 回 Renderer |
| Bridge 生命周期 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-lifecycle.ts` | `onAppQuit()` / `onSleep()` / `onWake()` | 系统睡眠挂起推理、唤醒恢复、关闭时 flush 未提交记忆与 compact |
| 工具注册（总入口） | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-tool-registrar.ts` | `registerAllTools()` | 汇总所有 bridge-tool-registrar-*.ts，把 12+ 白名单工具注册进 Kernel ToolRegistry（AI 护栏第一道闸） |
| 工具注册-同步 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-tool-registrar-sync.ts` | `registerSyncTools()` | VCS 状态、会话列表、设置读写等无副作用工具 → L0 自动批准，不进审批队列 |
| 工具注册-Cron | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-tool-registrar-cron.ts` | `registerCronTools()` | 心跳、注意力扫描、总结、记忆固化等主动式工具，由 Cron 调度器驱动 |
| 工具注册-集成 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-tool-registrar-integration.ts` | `registerIntegrationTools()` | MCP 工具、外部 Skill、自定义 HTTP 工具等扩展点 |
| Bridge-Wiki 工具 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-wiki-tools.ts` | `wikiSearch` / `topicCreate` / `synthesisRegenerate` 等 8 个 | 暴露 Wiki Vault/Topic/Graph/Synthesis 四能力给 Agent；禁止 AI 静默覆盖真相表（禁令 B7） |
| Bridge-浏览器工具 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-browser-tools.ts` | `browserNavigate` / `screenshot` / `extractContent` | 调 `packages/browser-control` 的 Playwright 能力，全量 L2 审批 + 域名白名单 |
| Bridge-录屏工具 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-screen-record-tools.ts` | `startRecord` / `stopRecord` / `addSubtitle` | 桌面录屏 + 字幕编辑器流水线；需用户屏幕共享授权 |
| Bridge-App UI 工具 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-app-ui-tools.ts` | `gotoPage` / `openModal` / `highlightElement` | 自我控制 UI：跳页、打开模态、红框聚焦元素（App UI Control 子系统） |
| 会话管理 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-conversation-manager.ts` | `class ConversationManager` / `newSession()` / `listSessions()` | 会话 CRUD、消息分页、软删除、最近会话排序 |
| Renderer IPC 桥 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-renderer-ipc.ts` | `agent:*` 全部 12 个 handler 入口 | 接收 Renderer 发来的 sendMessage / abort / loadSession / rename 等 IPC，鉴权后转 Bridge |
| 事件转换器 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\event-converter.ts` | `kernelEventToRendererEvent()` | Kernel 内部 10+ 种事件（delta/tool_call/thinking/heartbeat）统一转换成 Renderer 可消费协议 |
| 权限 IPC 转发 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\bridge-permission-ipc-forward.ts` | `askUserPermission()` → 弹窗 L1/L2 审批 | 工具执行需要用户批准时，转发到 Renderer Modal 收集 resolve/reject |
| Ask User 弹窗控制 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\ask-user-question-controller.ts` | `class AskUserQuestionController` | 排队多个弹窗问题（串行不打断）、超时处理（L1 15min 拒绝 / L2 超时归档） |

---

## 3. 自主进化模块（7 个子模块）

| 功能 | 绝对路径 | 关键类 / 函数 | 简要职责 |
|------|----------|---------------|----------|
| 模块总装 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\autonomous-wiring.ts` | `wireAutonomousModules()` | 把元认知 / 目标生成 / 人格 / 心跳 / 审批 / 归因 / 反馈 7 模块注入 Kernel |
| 能力追踪（Elo Rating 核心） | `c:\myself\projects\my\open-source\lumii\packages\agent-runtime\src\storage\autonomous-repo.ts` | `class AutonomousRepo` / `updateEloAfterTest()` / `getCapabilityMatrix()` | 8 维度能力（记忆/工具/规划/反思/写作/编码/沟通/学习）Elo 打分 + λ=20 置信度公式：`confidence = 1 − exp(−testCount/20)` |
| 元认知算法 | 元认知实现位于 `autonomous-repo.ts` 内部 4 函数 | `priorityDemand()` / `confidenceWeightedScore()` / `selectNextPractice()` | 按 `priority = demand × (1 − currentLevel) × confidence²` 选择下一轮练习项；避免过拟合已掌握能力 |
| 离线审批队列（L0/L1/L2 三级） | 审批位于 `apps/windows/src/main/exec-approvals-manager/`（若目录存在）或 `packages/agent-runtime/src/security/` | `class ApprovalQueue` / `classifyRiskL0L1L2()` / `TimeoutScanner` | L0 内部自治自动批 / L1 可见无副作用 15min 超时拒绝 / L2 有副作用超时归档；每日 5 条预算 04:00 重置 |
| 超时扫描器 | `TimeoutScanner`（同上位置） | `scanEvery15Minutes()` / `rejectStaleL1()` / `archiveStaleL2()` | 每 15min 扫描一次过期请求；L1 非默示同意（拒绝）/ L2 不丢不执行（归档） |
| 多层协同归因（Shapley Value 简化版） | 归因位于 `evolution-tick.ts` 内部 | `fastCreditAssignment(4 layers)` / `normalizePhi()` / `coolingOff(negative)` | 4 层（模型/编排/工具集/提示词）归因：O(2⁴=16) → O(4+4)=8 次仅变动层评估；负贡献 3 轮冷却不再放大 |
| 进化 Tick 驱动 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\evolution-tick.ts` | `class EvolutionTick` / `onEachTurnEnd()` | 每个 turn 结束后跑：Elo 更新 → Shapley 归因 → 需求矩阵 → 下一个目标提名（提交给目标生成模块） |
| 内在目标生成 | 目标生成位于 Kernel 内部模块（见 `docs/design/自主进化Agent/3-内在目标生成算法.md`） | `generateIntrinsicGoal()` / `GPA_Goal_Plan_Action()` | 按 Goal / Plan / Action 三步拆分目标；UCB1 + Thompson Sampling 多臂老虎机选策略 |
| 人格进化（Big Five + EMA） | 人格位于 `packages/agent-runtime/src/` persona 子模块 | `updateBigFiveByEvent()` / `emaSmooth(α=0.12)` | 大五人格（开放性/尽责性/外向性/宜人性/神经质）指数平滑 α=0.12；每日漂移 ≤ 0.02（生命感护栏 B3） |
| 心跳驱动 + 情绪 EMA | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\cron-scheduler.ts` + `cron-focus-memory.ts` | 30s 档：`runHeartbeat30s()` / 5min 档：`runConcernScan5m()` / 1h 档：`runDailyCheck1h()` | 30s 情绪 EMA / 5min 用户关注项扫描 / 1h 学习与整理；禁令 B1-B7 全在 heartbeat 每档入口硬编码拦截 |
| 反馈信号采集 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\autonomous-feedback-signals.ts` | `collectImplicitSignals()` / `collectExplicitSignals()` | 隐式：用户翻页速度/重写次数/停留时长；显式：👍👎按钮；两者加权喂回 Elo 与人格 |
| Cron 驱动 & 通知 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\cron-scheduler.ts` + `cron-notify-format.ts` + `cron-notify-dispatch.test.ts` | `CronScheduler` / `notifyRule()` / `dispatchToastOrBanner()` | Cron 注册 30s/5min/1h/4h/daily 五档；通知按频率节流，避免轰炸（禁令 B6 禁止无焦点乱弹通知） |
| 自主进化记忆持久化 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\agent-runtime\evolution-memory-persist.ts` | `snapshotEvolutionState()` / `restoreEvolutionState()` | 每次关机快照：Elo 矩阵 / 人格 / 待办目标 / Shapley φ；开机恢复避免冷启动从零开始 |

---

## 4. 记忆系统（四 Pipeline + Wiki 四解耦）

### 4.1 记忆抽取-打分-召回-注入 Pipeline

| 阶段 | 绝对路径 | 关键函数 | 简要职责 |
|------|----------|----------|----------|
| 抽取（Extractor） | `packages/agent-runtime/src/` memory extractor 模块（单测见 `__tests__/memory-extractor-prompt.test.ts`） | `extractFactsFromTurn()` | 每个 turn 结束后从对话中抽取：事实 / 偏好 / 承诺 / 项目上下文 / 情绪 5 类候选 |
| 打分（Scorer） | 同上模块（单测 `__tests__/recall.test.ts`） | `scoreMemoryByTemperatureAndSemantic()` | 方案 A+ 双轴：语义轴（事实/偏好/任务…）× 温度轴（热 < 1h / 温 1h–7d / 冷 > 7d）；温度按半衰期 exp 衰减 |
| 召回（Recall） | 同上模块（单测 `__tests__/unified-memory.test.ts` / `memory-scenarios.test.ts`） | `recallForPrompt(turnContext, budgetToken)` | 按当前 turn 的关键词 + 会话 ID + 温度阈值拼召回集；压缩预算在 `context-usage-breakdown.ts` 精确分配 |
| 注入（Injector） | 同上模块（单测 `__tests__/memory-consolidation.test.ts`） | `injectIntoSystemPrompt()` / `consolidateColdToLongTerm()` | 热记忆 inline 注入系统提示；温记忆 top-K 摘要；冷记忆每日 consolidate 进长期存储 |
| 记忆消费记录 | 同上（单测 `memory-provenance.test.ts`） | `recordProvenance(memoryId, usedAtTurn)` | 记录每条记忆被哪个 turn 引用；反向评估召回命中率，给 Elo「记忆能力」维度打分 |

### 4.2 Wiki 模块（Vault / Topic / Graph / Synthesis 四解耦）

| 子模块 | 绝对路径 | 关键类 / 函数 | 简要职责 |
|--------|----------|---------------|----------|
| Wiki Repo（总入口） | `c:\myself\projects\my\open-source\lumii\packages\agent-runtime\src\wiki\wiki-repo.ts` | `class WikiRepo` | DB CRUD 统一门面：Vault / Topic / Relation / Synthesis 四张真相表 + 派生索引表 |
| Vault 管理 | 同 wiki 目录（单测 `wiki-vault-sync.test.ts` / `wiki-vault-layout.test.ts`） | `class WikiVaultManager` / `syncVaultFromDisk()` / `layoutVault()` | Markdown 真相 ↔ SQLite 真相双向零迁移转换；磁盘 Vault 目录结构 layout（P1 Taxonomy 分类法） |
| Topic 层级（父子 + 反链） | `wiki-topic-tree.ts` + `wiki-topic-mutate.ts` + `wiki-ref-store.ts` | `buildTopicTree()` / `addBacklink()` / `rebuildWikiLinks()` | Topic 父子层级 + `[[wikilink]]` 解析；反链双向索引（派生表，可 100% 重建） |
| Topic 分类法 | `wiki-taxonomy-prompt.ts`（单测 `wiki-taxonomy-prompt.test.ts`） | `taxonomyClassify(topic)` / `taxonomyPromptTemplate` | LLM 自动分类：P1 技术/项目/人物/概念/流程/引用 6 类；不确定进「待整理」Inbox |
| 智能重分类 | `wiki-reclassifier.ts` + `wiki-reclassify-types.ts`（单测 `wiki-reclassifier.test.ts`） | `class WikiReclassifier` / `runReclassifyBatch()` | Inbox 批处理 50 条；基于 Embedding 最近邻 + LLM 二次判断，低置信度（< 0.75）保留人工 |
| 标题打分 | `wiki-title-score.ts`（单测 `wiki-title-score.test.ts`） | `scoreTitleClarity()` / `suggestRename()` | 标题质量 4 指标（长度/歧义/动词化/日期冗余）；P7 Rename 阶段批量重命名 |
| 向量检索 | `wiki-vector.ts` + `wiki-source-vector.ts`（单测 2 份） | `embeddingForTopic()` / `topKByCosine()` | text-embedding-3-small 1536 维；派生表可重建；P4 Summary Vector 阶段上线 |
| 自动合成（Synthesis） | `wiki-summary.ts` + `wiki-organizer.ts` + `wiki-organize-queue.ts`（单测 3 份） | `synthesizeCluster(clusterId)` / `organizeQueue()` / `summaryLLMPrompt` | 社区聚类后自动合成综述页；合成产物存 `wiki_synthesis` 真相表（用户可编辑覆盖） |

---

## 5. 同步模块（Export / Import / Conflict / Manager）

| 子模块 | 绝对路径 | 关键类 / 函数 | 简要职责 |
|--------|----------|---------------|----------|
| 导出（SQLite → JSONL/热备） | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\cloud-sync\sync-exporter.ts` | `class SyncExporter` / `exportIncrementalSince(exportCursor)` | Phase0 走 SQLite 热备 + 每表 hash 签名 + 删除 tombstone；JSONL 导出只用于外部导入 |
| 导入（Apply） | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\cloud-sync\sync-importer.ts` | `class SyncImporter` / `applyChangesetInTx()` | 单个事务内按 FK 拓扑序 apply；遇冲突调 ConflictResolver；失败整事务 ROLLBACK（原子性） |
| 冲突解决器（3 规则） | `ConflictResolver`（同 cloud-sync 目录，`sync-manager.test.ts` 有冲突用例） | `resolveLWW()` / `resolveMergeText()` / `resolveAskUser()` | 规则 1：LWW 最后写胜（updated_at 精确到 ms + 设备向量钟避偏）；规则 2：Markdown 文本合并（git merge-file 三路）；规则 3：结构性冲突挂起让用户选（红图标） |
| Sync Manager 状态机 + 五色图标 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\cloud-sync\sync-manager.ts`（单测 `sync-manager.test.ts`） | `class SyncManager` / `triggerSync()` / `state: Idle|Pulling|Conflicted|Pushing|Error` | 5 色圆形图标：Idle=灰 / Pulling=蓝 / Conflicted=红 / Pushing=绿 / Error=橙；状态机单向跃迁防止双推 |
| 调度器（节流 + 空闲触发） | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\cloud-sync\sync-scheduler.ts` | `scheduleOnIdleOrEvery(5m)` / `debounceAfterKeystroke(10s)` | 键盘 10s 静默 / 每 5 分钟兜底 / 用户点立即同步三触发；网络中断指数退避 |
| Git Provider（本地优先） | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\cloud-sync\git-provider.ts` | `class GitSyncProvider` / `pullRebase()` / `push()` / `detectNonFastForward()` | 默认 transport；强制禁用 `--squash` 和 `--force`；检测到 non-fast-forward 自动中止变 Red（防止设备失联） |
| GitCode Provider（云端） | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\cloud-sync\gitcode-provider.ts`（单测 1 份） | `class GitCodeProvider` extends `GitSyncProvider` | Token 存 OS Keychain；PAT scope 最小化（仅 repo write）；2FA 用户走 device flow |
| 同步日志 + 审计 | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\cloud-sync\sync-log.ts`（单测 1 份） | `appendSyncLog()` / `listRecent(limit=50)` | 每次同步写一行审计日志：何时 / 哪端 / 冲突数 / 失败原因；用于排障（不参与合并决策） |
| Sync IPC（Render 查询） | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\cloud-sync\sync-ipc.ts` | `sync:getStatus` / `sync:trigger` / `sync:resolveConflict` 三个 handler | Renderer 状态栏图标、SyncSettingsPage、冲突面板都通过这三条 IPC 与 Manager 通 |
| 配置（远程地址 + 分支） | `c:\myself\projects\my\open-source\lumii\apps\windows\src\main\cloud-sync\sync-config.ts`（单测 1 份） | `class SyncConfig` / `validateRemoteUrl()` | 远程 URL 校验；默认分支 `lumii-sync`；禁止切 `main/master`（用户代码混同风险） |

---

## 6. IPC 通道总表（按功能域 8 域分组）

| 域（domain:） | 绝对路径（Main handler） | 典型通道（action 示例） | Renderer 调用方 service |
|---------------|--------------------------|--------------------------|--------------------------|
| **vcs**（版本控制 & Diff Workbench） | `apps/windows/src/main/workspace-vcs/` 下 `vcs-diff.ts` / `vcs-repo.ts` / `project-git/project-git-status.ts` 或 `main/ipc/vcs.ts` | `vcs:getStatus` / `vcs:diffByOidTreeWalk` / `vcs:lazyLoadHunk` / `vcs:stage` / `vcs:commit` | `renderer/services/vcs.service.ts` |
| **voice**（TTS / ASR / 语音通话） | `main/` 下 voice 子目录 或 `main/ipc/voice.ts` | `voice:listEngines` / `voice:ttsSpeak` / `voice:ttsStop` / `voice:asrStart` / `voice:cloneMicRecord` | `renderer/services/voice.service.ts` |
| **agent**（对话 & 生命周期） | `apps/windows/src/main/agent-runtime/bridge-renderer-ipc.ts` | `agent:sendMessage` / `agent:abort` / `agent:newSession` / `agent:listSessions` / `agent:loadSession` / `agent:renameSession` / `agent:deleteSession` / `agent:getCapabilities` | `renderer/services/agent.service.ts` |
| **wiki**（知识库） | `main/ipc/wiki.ts` 或 wiki 模块内嵌 IPC | `wiki:listVaults` / `wiki:listTopics` / `wiki:getTopic` / `wiki:updateTopic` / `wiki:createTopic` / `wiki:deleteTopic` / `wiki:getGraph` / `wiki:regenerateSynthesis` | `renderer/services/wiki.service.ts` |
| **sync**（数据同步） | `apps/windows/src/main/cloud-sync/sync-ipc.ts` | `sync:getStatus` / `sync:trigger` / `sync:resolveConflict` / `sync:listLogs` / `sync:updateConfig` | `renderer/services/sync.service.ts` |
| **settings**（用户偏好） | `main/ipc/settings.ts` 或 `app-ui-control/settings-channel.ts` | `settings:getAll` / `settings:update` / `settings:resetSection` / `settings:exportJson` / `settings:importJson` | `renderer/services/settings.service.ts` |
| **mcp**（模型上下文协议） | `main/` MCP 子模块（按 `config/mcp-config.ts`） | `mcp:listServers` / `mcp:startServer` / `mcp:stopServer` / `mcp:listTools` / `mcp:callTool` | `renderer/services/mcp.service.ts` |
| **approval**（自主进化审批） | `main/exec-approvals-manager/` 或 `bridge-permission-ipc-forward.ts` | `approval:pendingCount` / `approval:listPending` / `approval:resolve`（approve/reject）/ `approval:listHistory` | `renderer/services/approval.service.ts` |

---

## 7. 数据库 Schema 迁移注册表

| 功能 | 绝对路径 | 关键入口 | 简要职责 |
|------|----------|----------|----------|
| 迁移总调度 | `c:\myself\projects\my\open-source\lumii\packages\agent-runtime\src\storage\local-database.ts` | `class LocalDatabase.migrate()` + `getMigrationsRegistry()` | 启动时：`PRAGMA user_version` 读取当前版本；注册表缺哪个跑哪个；单向递增，永不降级 |
| 注册表（V1..V34 全量） | `packages/agent-runtime/src/storage/schema.ts`（单测 `schema-autonomous-v34.test.ts` / `schema-wiki.test.ts` / `schema-diary.test.ts` / `schema-autonomous-p2.test.ts`） | `MIGRATIONS: Migration[]` + `up()` / `down()`（down 仅测试用） | V29/V30/V31 关键里程碑：V29 approval_requests 表 / V30 approval_actions 表 / V31 能力矩阵 + 心跳表 |
| SQLite CHECK 约束规避流程 | 同上 `schema.ts` 内部注释有 5 步 SQL | 5 步：`BEGIN` → RENAME old → CREATE new（新 CHECK）→ INSERT SELECT → DROP old → `COMMIT` | SQLite 不支持 `ALTER TABLE ... ADD/DROP CHECK`；必须整表重建 + 外键临时关 `PRAGMA foreign_keys=OFF` |
| 真相表 vs 派生表边界 | 同上 `schema.ts` 顶部注释 + `docs/wiki/08-experience/04-工程实践最佳做法.md` §2.2 | 代码中用 `-- TRUTH:` / `-- DERIVED:` SQL 注释区分 | 真相表（用户输入）：conversations / messages / wiki_topics / wiki_vaults / approval_requests… 禁止 AI 静默写；派生表可删库重建：fts5 / embeddings / wikilinks / backlinks / temperature_scores / page_rank |

---

## 8. 设计系统（Design Tokens 8 文件 + tokens.css）

| 资源 | 绝对路径 | 关键变量 / 导出名 | 简要职责 |
|------|----------|-------------------|----------|
| CSS 变量总入口（运行时） | `c:\myself\projects\my\open-source\lumii\apps\windows\src\renderer\styles\tokens.css` | `:root` + `[data-theme="light"]` + `[data-theme="dark"]` | 定义所有 `--mt-surface-rgb` / `--mt-fg-1` / `--mt-border` / `--mt-radius-*` 语义变量；主题切换只改 `[data-theme]` 属性 |
| TS：颜色 | `tokens/colors.ts`（styles/tokens 目录下同） | `export const colorsLight` / `colorsDark` | HSL 色板：暖奶油浅色（#FFF8F0 基调）/ 深蓝灰深色（#0F1419 基调），色盲友好校对 |
| TS：间距 | `tokens/spacing.ts` | `export const spacing = { 0.5: '2px', 1: '4px', ..., 24: '96px' }` | 4px 步长；所有组件 margin/padding 只能用 0.5/1/2/3/4/6/8/12/16/24 离散档位 |
| TS：字体层级 | `tokens/typography.ts` | `export const typography = { display, headline, title, body, label, caption, overline, code }` | 8 级字体：Display 40/52 → Code 12/16；中文字体栈 PingFang SC / Microsoft YaHei，等宽 JetBrains Mono |
| TS：圆角 | `tokens/radius.ts` | `radius: { sm: 4, md: 8, lg: 12, full: 9999 }` | 四级圆角；输入/卡片 md=8，头像 full，按钮 sm=4 |
| TS：阴影 | `tokens/shadows.ts` | `shadows: { xs, sm, md, lg }` | 四级柔和阴影：浅色=暖灰投影 / 深色=发光投影，颜色不硬编码 |
| TS：过渡 | `tokens/transitions.ts` | `easings` + `durations`（100ms/150ms/200ms/300ms） | Hover=150ms，展开=200ms，页面切换=300ms；禁用 > 400ms 拖沓动画 |
| TS：Z-Index | `tokens/z-index.ts` | `z: { base, dropdown, sticky, banner, modal, popover, toast, tray }` | 8 档 z-index；禁止 inline `zIndex={9999}`，一律从 tokens 取 |
| TS：断点（响应式） | `tokens/breakpoints.ts` | `breakpoints: { sm: 640, md: 900, lg: 1200, xl: 1600 }` | 与 Responsive 组件 `useBreakpoint()` 对齐；最小支持 1024×720（Windows 笔记本） |
| lucide-react 图标独占（体系） | 任何组件中 `import { X } from 'lucide-react'` | **禁止** 引入 Ant Design Icons / Heroicons / 自定义 SVG | 所有按钮/导航/状态图标只走 lucide-react；大小 `size={16/18/20}` 三档 |
