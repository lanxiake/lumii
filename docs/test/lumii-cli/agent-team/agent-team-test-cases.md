# 一等公民 Agent 团队 测试用例（AT 套件）

> 对应实现：`docs/plans/专项Agent/`（A 运行时贯通 / B code-dev 闭环 / C system-keeper / D 团队转正）与设计 `docs/design/专项AGENT/专项Agent设计.md` §2.5。
> 规范：`docs/test/lumii-cli/CLI-TEST-SPEC.md`；执行器：`run-agent-team-e2e.mjs`（严格复用 `lib/cli-harness.mjs`）。
> **设计原则：以用户真实使用场景为主轴**——每个 L3 用例模拟「用户带着一个真实任务来找这位团队成员」的完整旅程，而不是逐接口冒烟。
> 探针命名空间：会话前缀 `[agent-team]`；无 SQL 播种。
> 环境开关：`AT_ONLY=S1,S3`（选择性运行）、`AT_SKIP_LLM=1`（只跑 L1/L2）、`AT_SKIP_CLI=1`（跳过 claude 场景）、`AT_TICK=1`（启用 tick 条件用例）、`AT_TURN_TIMEOUT_MS`、`AT_VERBOSE=1`。

## 一、用户旅程地图（为什么是这些用例）

| 真实旅程 | 用户期望 | 用例 |
|---|---|---|
| 🧑💻「我新装了客户端，想试试这几个 Agent 能干什么」——找成员聊天办事 | 选中成员、说人话、它真的动手把事办了 | AT-S1（开发）、AT-S2（维护）、AT-S3（情报） |
| ⏰「每天早上应该自动产出一份工作日报，我能看到」 | 到点（或手动触发）由「灵栖记事」送达，内容真实 | AT-S4 |
| 🛠️「切到 Claude Code 干活，接着追问上一轮的结果」 | 上下文连续，不用重复说明背景；界面实时可见回复 | AT-S5、AT-UI-03 |
| 🖱️「在 AI 团队页给成员打开自主心跳、看侧栏分组」 | 点击即写配置；分组清晰、不撑开其他分组 | AT-UI-01、AT-UI-02 |
| 💬「我只想日常聊天，不要被这些新 Agent 干扰」 | 不选 Agent 时行为与以前完全一致 | AT-S6 |
| 🧑✈️「我懒得想该找谁，直接跟主助手说『帮我整理下我的记忆』」 | 主助手识别职责、委托对应专家，产出汇总回来 | AT-S7 |
| 🔧「懒得找设置页，直接让维护帮我把主题换了」 | 维护代操客户端，设置真实落盘 | AT-S8 |
| 📰「资讯管线到点跑一遍，资讯卡刷新」 | 情报执行 news-pipeline，产出归属正确 | AT-S9 |
| 🧑💻「把沙箱项目里那个分页 bug 修掉」→ 点卡片确认，开发直接开工 | 主助手不动手，转交灵栖开发并在绑定的 CLI 里执行 | AT-F2 |
| 🔧「设置里切后端/敲错项目名」 | 即时应答，错误有明确反馈 | AT-L2-03 |

数据链路层（L1/L2）保留基本面校验：命令面、定义可见、迁移落库、tick 汇总。

## 二、场景用例（L3）

#### AT-S1 场景：找「灵栖开发」看看当前环境
- **故事**: 用户刚开了一个灵栖开发会话，还没绑定项目，先让它看看当前工作目录有什么
- **步骤**: 1) `conversation:create(agentId=code-dev)` 建会话 2) 查 participant 3) 发「请列出你当前工作目录下的文件或文件夹（最多 5 个）」
- **预期**: participant=`code-dev`；它真实使用工具（list_dir 类）给出目录内容
- **断言**: participant=硬；回复非空=硬；回复提及文件/目录=软
- **预计回合**: 1

#### AT-S2 场景：让「灵栖维护」给我做一次记忆体检
- **故事**: 用户想知道自己的用户记忆有没有重复/矛盾，要求只报告不许改
- **步骤**: 1) 建 system-keeper 会话 2) 发「用 profile_memory 读我的用户记忆，检查重复或矛盾，只报告不要修改」3) 回合后比对 user-memory.md
- **预期**: 有检查结论的回复；user-memory.md 原有内容**一行不改**
- **断言**: 原有行全集保留=硬（红线）；回复非空=硬；提取链路若有新增行，记录 note 并清理探针行
- **预计回合**: 1

#### AT-S3 场景：告诉「灵栖情报」我的资讯偏好
- **故事**: 用户说「以后多关注 AI 编程工具动态，少推学术论文」——期待它真的记住（D3 验收场景）
- **步骤**: 1) 建 info-curator 会话 2) 发偏好 3) 轮询偏好落库（agent_memories 新行或 user-memory.md 探针词）
- **预期**: 60s 内偏好进入记忆；测试后清理探针行
- **断言**: 落库=硬（agent_memories 行 or 记忆文件探针词）；确认回复非空=软
- **探针词**: 「编程工具」「论文」（事前核对均不存在于 user-memory.md）
- **预计回合**: 1

#### AT-S4 场景：每天早上的工作日报按时由「灵栖记事」送达
- **故事**: 用户在任务页手动触发一次「工作日报」（模拟到点），检查产出
- **步骤**: 1) `cron run seed-daily-report`（真实执行一轮 chronicler）2) 轮询 local_cron_runs 新行 3) 轮询 cron:seed-daily-report 新消息
- **预期**: 运行 status=ok；会话产出新 assistant 消息且 `agent_id='chronicler'`；正文非空
- **断言**: 运行状态=硬；消息 agent_id=硬（D2 验收项）；正文非空=硬
- **副作用**: 真实 LLM 多轮 + notify_targets 系统通知；运行记录真实落库
- **预计回合**: 1 次任务运行（内部多轮工具调用）

#### AT-S5 场景：切到 Claude Code 干活并追问
- **故事**: 用户在会话里把工具切到 Claude Code，先让它确认 node 版本，随后追问刚才的版本号
- **步骤**: 1) `setBackend(claude, sessionKey)` 2) 第一轮「用 bash 运行 node -v」3) 断言 runtime_state `acp-session:claude:<sk>` 与日志 ACP 路径 4) 第二轮「刚才确认的版本号是多少」
- **预期**: 两轮由 claude CLI 连续处理；resume 生效（无「CLI 上下文已重置」）；第二轮复述同一版本号
- **断言**: 键存在/无重置标记/日志 ACP 标记=硬；版本号复述=软（可重试 1 轮）
- **预计回合**: 2（真实 claude CLI）
- **跳过**: `AT_SKIP_CLI=1`

#### AT-S6 场景：没有选 Agent 的日常聊天不受影响（回归）
- **故事**: 用户不选任何 Agent 直接聊天——行为必须与 Agent 团队上线前一致
- **步骤**: 1) 普通建会话 2) 查 participant 3) 发「只回复两个字：收到」
- **预期**: participant=`default`（非开发后端实例）；正常回复
- **断言**: participant=硬；回复含「收到」=软（可重试 1 轮）
- **预计回合**: 1

#### AT-S7 场景：对主助手说「帮我整理下我的记忆」（队长制 F1）
- **故事**: 用户不指定 Agent，直接在普通对话（主助手）里提出记忆整理请求——按团队分工应由主助手委托给常驻专家「灵栖维护」
- **步骤**: 1) 普通建会话 2) 发「帮我整理下我的记忆」（真实口吻，不含测试性约束词）3) 等回合真正结束（流式行 is_streaming=0，避免把开场白当结果）4) 在消息工具调用里找 `spawn_agent`（agentType=system-keeper）
- **预期**: 主助手委托灵栖维护（sync），专家产出经主助手汇总回复；用户记忆不被直接修改
- **断言**: 出现 spawn_agent→system-keeper=硬（最多重试 1 次，容模型随机性）；最终回复非空=硬；user-memory 原有行零改动=硬（红线）
- **副作用**: 真实 LLM 多轮 + 子 Agent 执行（单回合较长）；专家方案可能写入 workspace outputs；测试清理本窗口新增的探针记忆行（保守关键词）
- **说明**: 委托成功即证明「团队专家清单进入了主助手提示词」（F1 注入生效），不另设清单问答探针（非真实场景）
- **预计回合**: 1（内部含子 Agent 执行）；模型未委托时重试 1 次

#### AT-S8 场景：让「灵栖维护」代操客户端改设置（C 片闭环 4）
- **故事**: 用户懒得找设置页，直接跟维护说「把主题换成深色」——它应该直接把客户端设置改掉
- **步骤**: 1) 读 `theme.mode`（需 light 起始，否则 SKIP）2) 建 system-keeper 会话 3) 发「帮我把客户端主题切换成深色模式（dark），以后一直用深色」4) 轮询设置变化；模型若先要确认，追加「确认」消息（最多 3 轮）
- **预期**: `theme.mode` 真实落盘为 dark；测试侧 finally 恢复原值
- **断言**: 设置变化=硬；测试后恢复=硬（不留配置漂移）
- **副作用**: 界面主题短暂变化；真实 LLM 1-3 回合
- **预计回合**: 1-3

#### AT-S9 场景：资讯管线由「灵栖情报」执行（D3 验收）
- **故事**: 用户手动触发一次资讯管线（模拟到点），期待概览页资讯卡刷新
- **步骤**: 1) `cron run news-pipeline` 2) 轮询 `local_cron_runs`（status=ok）3) 轮询 cron:news-pipeline 新 assistant 消息
- **预期**: 运行 ok；消息 `agent_id='info-curator'`；正文非空
- **断言**: 运行状态=硬；消息 agent_id=硬（D3 验收项）；正文非空=硬；feed 计数（不下降）=软（记录 note）
- **副作用**: 真实资讯抓取 + LLM 多轮；feed/通知为真实产出（验证证据本身）
- **预计回合**: 1 次任务运行（内部多轮）

#### AT-F2 场景：对主助手说「把沙箱项目的分页 bug 修掉」→ 卡片确认 → 开发开工（队长制 F2）
- **故事**: 用户在主对话里提出一个「绑定项目中的开发任务」；主助手识别后不动手，生成转交提案；用户在卡片上点「交给灵栖开发」确认，任务在绑定的 CLI 里执行
- **前置（F2 环境准备，缺失则 SKIP）**: `app.json` 配置 code-dev 的 Agent 绑定：
  `{ "agentId": "code-dev", "backendId": "claude", "workspace": "<沙箱目录>", "enabled": true }`；
  沙箱目录需含 pager.js（用例每次运行会把它重置为已知 off-by-one 版）
- **步骤**: 1) 重置沙箱 → 2) 主助手会话发「handoff-demo 项目里 pager.js 分页 off-by-one：11 条每页 5 条应 3 页现在 2 页。帮我修掉，改完在项目里验证」（**必须带注册项目名**：模糊的「某个项目」措辞会让主助手按小任务自理，不触发转交——与「不大炮打蚊子」的边界一致）→ 3) 断言 propose_dev_handoff 提案（含 handoffId）→ 4) 界面真实点击「交给灵栖开发」（点击后以日志确认生效；链路不可用时退回 CLI confirm，soft 降级并在返回值注明）→ 5) 断言日志「已转交」+「ACP 路径 source=binding」→ 6) 轮询沙箱 pager.js 出现 Math.ceil → 7) 轮询原（主助手）会话出现「转交完成」汇报
- **预期**: 新建开发会话并直达绑定的 claude；任务在开发会话里完成；**完成后结果异步汇报回原会话**（执行与汇报分离）
- **断言**: 提案（spawn 前）=硬（**容模型随机性：未提案则重置沙箱后重试 1 次**；两次都自理=FAIL）；转交执行日志=硬；绑定直达 CLI=硬；沙箱被修复=硬；原会话收到汇报=硬；UI 点击路径=软（点击后以日志确认生效，未生效时 CLI 降级）
- **副作用**: 真实 claude 调用；沙箱文件被修改（下次运行重置）；新建开发会话保留（探针前缀）
- **预计回合**: 主助手 1 回合 + claude 1 次任务执行

## 三、UI 级用例（lumii-ui screenshot refs + click，模拟真实用户点击）

#### AT-UI-01 在 AI 团队页真实点击「参与自主心跳」开关
- **故事**: 用户在 AI 团队页（Grid 视图）为成员操作自主心跳——开关要可见、点了要真的写进配置、再点能还原
- **步骤**: 1) `goto agents` → 切 Grid 2) 点可视区最后一个开关（位置/状态无关设计：用配置 diff 识别翻转的 Agent）3) 断言 app.json `app.autonomousAgents` 恰有一个 id 翻转 4) 再点同一位置 → 断言回到初始
- **预期**: 翻转与还原都能在配置上验证；异常路径 finally 兜底还原
- **断言**: 配置 diff=硬（恰好 1 个 id）；还原=硬
- **副作用**: 操作即真实用户配置，最终与测试前一致

#### AT-UI-02 侧栏按 Agent 分组结构
- **故事**: 用户打开聊天侧栏，默认 tab 应看到「默认」（主助手组）+ 各 Agent 短名（开发/维护/记事/情报）的可折叠分组，组内滚动分页而非「展开更多」
- **步骤**: 1) `goto chat` 2) 截图 refs 3) 断言存在「默认(N)」组头、至少一个 Agent 短名组头、无「展开更多」按钮
- **断言**: 结构=硬（依赖 UI 可达）
- **预计回合**: 0

#### AT-UI-03 ACP 回复在界面实时可见（无需重启）
- **故事**: 用户切换 claude 后端发消息，**不重启**就能在界面看到回复（2026-09-13 修复的回归护栏：ipcMainWindowRef 快照 null 导致事件被静默丢弃）
- **步骤**: 1) 新建会话 + `setBackend(claude)` 2) 发送「只回复 Markdown 三级标题：界面可见」3) 断言日志出现「渲染层收到 `agent:message:start`」（该会话）4) `goto chat` → 点击该会话 → 截图断言回复标题在界面上可见
- **预期**: 事件到达渲染层 + 界面直接可见回复；acp-session 键存在
- **断言**: 日志事件到达=硬；acp 键=硬；UI 可见=硬路径（3 次重试；若会话按钮不在侧栏可视区——如用户同屏滚动/切页——降级为 soft 并在 note 明示，事件到达硬断言仍覆盖修复本体）
- **预计回合**: 1（真实 claude CLI）

## 四、数据链路用例（L1/L2）

#### AT-L1-01 CLI 命令面冒烟
- **步骤**: `help --json`，断言 commands>50 且含 `command`/`conversation create`/`send`
- **断言**: 结构=硬；**预计回合**: 0

#### AT-L2-01 四位 Agent 定义运行时可见
- **步骤**: `command agent:definitions:list` → 断言四个 id + 名称（灵栖开发/维护/记事/情报）
- **断言**: 硬；**预计回合**: 0

#### AT-L2-02 预置任务转正迁移在真实库落地
- **步骤**: 查 `local_cron_jobs`：4 条简报→chronicler、news-pipeline→info-curator；task_text/system_prompt 非空、无魔法指令残留
- **断言**: 落库值=硬；用户删过的行不计失败；**预计回合**: 0

#### AT-L2-03 会话级开发上下文（敲错项目名有明确反馈）
- **故事**: 用户在会话里切后端、设项目；敲错项目名应被明确拒绝而不是静默失败
- **步骤**: 1) 新会话 getDevContext → source=global 2) setBackend(claude)→session 3) setBackend(lumii) 退出 4) setProject(不存在名) 被拒 5) 已注册项目往返（条件）
- **断言**: 全部=硬；**副作用**: dev-context.json 快照恢复；**预计回合**: 0

#### AT-L2-04 心跳 tick 多 Agent 汇总（条件用例）
- **前置**: `AT_TICK=1` 且 app.json `autonomousAgents` 非空
- **步骤**: 手动 `cron run autonomous-tick` → 断言 summary 为 `agentId=结果; …` 且覆盖全员
- **容错**: summary=`already-running` 表示云同步冲突处理正在进行（`executeSyncConflictGoal` 的 in-flight 互斥，tick 被设计性跳过）——间隔 60s 重试（最多 3 次），持续被占用则 SKIP 并注明「环境存在未决云同步冲突，需在无冲突窗口重验」
- **跳过条件**: 未开 AT_TICK / 未配置 / 持续被冲突处理占用 → SKIP（人工验收：AgentsPage 打开开关后重跑）；**预计回合**: 1 次 tick

## 五、不覆盖（报告注明）

- 删除守卫（`evolution:<id>` 不可删）：`conversation:delete` 不在控制口白名单，CLI 不可达；由 A3 人工验收覆盖。
- Agent 绑定层（`codingDevAgentBindings`）与 B10 配置 UI：配置入口在设置页，CLI 无写出口；人工验收。
- system-keeper 自主档工具面（无 bash/file_write/app_*）：agent-runtime 包单测覆盖。
- 渠道侧（微信/飞书 `/project`、QQ/微信转交）：需真实渠道账号，手工验收。

## 五之二、设计验收对照（A/B/C/D + F 片 → 用例）

> 用途：核对「专项 Agent 设计 + 各片实施计划」的验收项是否都有自动化落点；未覆盖项显式列出，供人工验收排期。

| 验收项（来源文档） | 自动化落点 | 状态 |
|---|---|---|
| A1 cron 产出归属（消息 agent_id = 任务 agent） | AT-S4、AT-S9 | ✔ 硬断言 |
| A2 tick 多 Agent 遍历（summary 覆盖全员） | AT-L2-04 | 用例就绪；受环境云同步冲突互斥，待无冲突窗口重验 |
| A3 `evolution:` 守卫泛化 | 单测（删除守卫 CLI 不可达） | ✔ 单测 |
| A4 per-agent 自主开关（真实点击写 app.json） | AT-UI-01（+AT-L2-04 参与 tick） | ✔ |
| A5 失败可见性（目标失败落会话 + 通知） | 无（构造失败目标依赖环境） | 未覆盖（人工） |
| B 灵栖开发可见/会话可用 | AT-L2-01、AT-S1 | ✔ |
| B 双项目两会话并行互不串 | 无 | 未覆盖（人工；AT-L2-03 覆盖单会话切换与隔离键） |
| B 两轮追问续接（`--resume`） | AT-S5 | ✔ |
| B 微信 `/project`（渠道） | 无 | 未覆盖（人工） |
| B codex 后端真实写文件 | 无 | 未覆盖（人工；claude 链路由 AT-S5/F2 覆盖） |
| B 模式 chip 显示 / 退出开发模式 | 无 | 未覆盖（UI 人工） |
| B 退出应用无 CLI 残留 | 无 | 未覆盖（人工） |
| C 灵栖维护可见/会话可用 | AT-L2-01、AT-S2 | ✔ |
| C 手册可被 `skill_invoke` 加载 | 无显式断言（S2 不必然触发手册） | 部分（人工/单测） |
| C 记忆精炼演练（.bak + 改写） | AT-S2 仅覆盖只读体检 | 部分（改写路径人工） |
| C Wiki 策展建议 / 指南同步 | 无（wiki 能力另有 wiki 套件） | 未覆盖（人工） |
| C 客户端代操（改设置 + 复验） | AT-S8 | ✔（99.0s；theme.mode light→dark 真实落盘并恢复） |
| C 自主档工具面白名单 | agent-runtime 包单测 | ✔ 单测 |
| D chronicler 迁移 + 日报产出 | AT-L2-02、AT-S4 | ✔ |
| D info-curator 迁移 + 资讯管线产出 | AT-L2-02、AT-S9 | ✔（119.9s；agent_id=info-curator，feed 166→175） |
| D 迁移零侵入（改过的行不覆盖） | AT-L2-02（删除行不计失败） | 部分 ✔ |
| F1 主助手接单委托（队长制） | AT-S7 | ✔（模型随机性容错 1 次） |
| F2 桌面一键转交闭环 | AT-F2 | ✔ |
| F3 渠道一键转交 | 无（控制口无入站消息注入口） | 未覆盖（QQ 人工复核，见 05 文档 F3） |

## 六、副作用与恢复

- 探针会话 `[agent-team] *` 保留（CLI 无删除能力），报告注明。
- `dev-context.json`：套件开始前快照（含文件不存在情形），结束时原样恢复。
- `user-memory.md` / `agent_memories`：S2/S3/S7 的探针行按行/按 id 清理；比对基线用「原有行全集保留」断言，不整文件恢复。
- 后台 cron：套件期间禁用全部 `enabled=1` 任务（防抢 LLM），结束时恢复；SIGINT/SIGTERM 下尽力恢复。
- DB：除探针行清理外只读；S4 的 cron 运行记录为真实产出（验证证据本身）。
- S7 专家产出的方案文件（workspace `outputs/` 下）：保留不删（避免误删用户文件），报告注明。
- `AT-F2`：沙箱目录由用例重置/修改（自包含、可重复）；需按上文「F2 环境准备」配置 Agent 绑定，未配置时用例 SKIP；转交产生的开发会话为真实产出。
- `AT-S8`：`theme.mode` 由测试侧 finally 恢复原值；界面主题短暂切换属预期现象。
- `AT-S9`：feed（`dashboard_feed_items`）与通知为真实产出（验证证据本身）。
- 真实 LLM：默认全量（S1/S2/S3/S6 各 1 回合 + S7 一回合（含子 Agent，未委托时重试 1 次）+ S8 一回合（system-keeper 会话，先确认再动手时追加确认轮）+ S9 一次任务运行 + S4 一次任务运行 + S5 两轮 claude + F2 一回合 + 一次 claude 开发任务）；`AT_SKIP_LLM=1` 可离线跑 L1/L2。
