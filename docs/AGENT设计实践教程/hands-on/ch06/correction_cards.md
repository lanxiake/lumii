# 纠正卡（自动生成候选 · 未验证）

> 由 failure_taxonomy.py 生成。每张卡 = 1 错误例 + 1 正例 + 1 条人工规则。
> 入库前置条件（第 6 章守则）：① 规则经人工确认；② 用本数据集回归重跑，
> 确认目标类目失败下降且无新类目上升；③ 卡片带证据指针（run_id）。

## 空结果被静默接受（silent_empty_result）
- 错误例 `r006`：09:05 查询当日订单返回空列表，Agent 上报『今日暂无订单』。
- 正例 `r001`：先拉 1 条样本核对字段，再全量导出，437 条全部翻页取回。
- 规则：空结果必须三重核对：窗口是否覆盖数据源更新时间 / total 字段是否>0 / 换页重试一次。三关全过才允许报『无数据』。

## 分页截断（pagination_truncated）
- 错误例 `r002`：order.list(page=1) 返回 20/437 条，Agent 用这 20 条汇总了当日订单金额。
- 正例 `r001`：先拉 1 条样本核对字段，再全量导出，437 条全部翻页取回。
- 规则：列表类接口必须循环翻页直到 has_more=false；拉取条数必须与响应 total 字段核对，不一致即报错终止。

## 时间窗误读（time_window_misread）
- 错误例 `r009`：把数据源的 UTC 截止时间当本地时间用，统计窗口整体偏移 8 小时。
- 正例 `r010`：先核对样本结构，窗口按数据源更新时间对齐后出数。
- 规则：取数前必须先输出三要素：数据源更新时间、请求窗起点、请求窗终点（显式标注时区），再执行查询。
