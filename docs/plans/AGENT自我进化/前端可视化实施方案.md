# 自主进化 Agent 前端可视化实施方案

> **日期**：2026-09-04  
> **依赖**：P0/P1 后端已完成  
> **目标**：提供用户可见的自主进化状态和交互界面  
> **预计工时**：37 小时（分 3 个阶段）

---

## 一、问题分析

### 1.1 当前状态

**已完成（后端）：**
- ✅ 满意度评分引擎
- ✅ 目标生成器
- ✅ 能力追踪器（P1）
- ✅ 反思引擎（P1）
- ✅ 数据库表和 CRUD

**缺失（前端）：**
- ❌ 用户无法看到 Agent 的状态
- ❌ 无法批准/拒绝自主目标
- ❌ 无法观察能力进化过程
- ❌ 无法查看反思结果

### 1.2 核心需求

| 用户角色 | 核心需求 | 优先级 |
|---------|---------|-------|
| 终端用户 | 看到 Agent 在进化、能批准目标 | P0 |
| 产品经理 | 验证功能是否正常工作 | P0 |
| 开发者 | 调试和监控自主行为 | P1 |
| 研究人员 | 分析进化数据和趋势 | P2 |

---

## 二、架构设计

### 2.1 技术栈

**前端框架：** 根据现有项目选择（假设 React/Next.js）

**状态管理：** 
- Zustand 或 Context API（轻量）
- SWR 或 React Query（数据获取）

**可视化库：**
- Recharts（满意度趋势图、能力雷达图）
- D3.js（可选，用于高级可视化）

**UI 组件库：** 
- Shadcn/ui 或 Radix UI（现代、可访问）
- Tailwind CSS（样式）

### 2.2 组件结构

```
apps/web/src/
├── pages/
│   └── autonomous/
│       ├── index.tsx                    # 仪表板主页
│       ├── goals/[id].tsx              # 目标详情页
│       └── history.tsx                 # 历史记录页
├── components/
│   └── autonomous/
│       ├── SatisfactionChart.tsx       # 满意度趋势图
│       ├── GoalCard.tsx                # 目标卡片
│       ├── GoalApprovalDialog.tsx      # 批准对话框
│       ├── CapabilityRadar.tsx         # 能力雷达图（P1）
│       ├── CapabilityProgressBar.tsx   # 能力进度条（P1）
│       ├── ReflectionCard.tsx          # 反思卡片（P1）
│       ├── PromptVariantStats.tsx      # Prompt 统计
│       └── PersonalityChart.tsx        # 人格图表
├── hooks/
│   └── autonomous/
│       ├── useAutonomousStatus.ts      # 获取状态
│       ├── useGoalApproval.ts          # 目标批准
│       └── useCapabilities.ts          # 能力数据（P1）
└── lib/
    └── autonomous/
        ├── api.ts                      # API 客户端
        └── types.ts                    # 类型定义
```

### 2.3 API 设计

**端点清单：**

```typescript
// GET /api/autonomous/status
// 获取自主进化总体状态
interface AutonomousStatus {
  enabled: boolean;
  satisfaction: {
    overall: number;
    trend: 'improving' | 'stable' | 'declining';
    breakdown: {
      taskCompletion: number;
      userFeedback: number;
      efficiency: number;
      knowledgeGrowth: number;
    };
    lastUpdated: string;
  };
  pendingGoalsCount: number;
  capabilities?: {  // P1
    [dimension: string]: {
      level: number;
      confidence: number;
      testCount: number;
    };
  };
  lastReflection?: {  // P1
    timestamp: string;
    primaryIssue: string;
    recommendationCount: number;
  };
}

// GET /api/autonomous/goals?status=pending
// 获取目标列表
interface Goal {
  id: string;
  type: 'learning' | 'proactive-message' | 'capability-improvement';
  description: string;
  triggerReason: string;
  status: 'pending' | 'approved' | 'rejected' | 'executing' | 'completed';
  priority: number;
  userValueScore: number;
  feasibility: number;
  metadata?: Record<string, any>;
  createdAt: string;
  approvedAt?: string;
  completedAt?: string;
}

// POST /api/autonomous/goals/{id}/approve
// 批准目标
interface ApproveGoalRequest {
  note?: string;  // 用户备注
}

interface ApproveGoalResponse {
  success: boolean;
  goal: Goal;
}

// POST /api/autonomous/goals/{id}/reject
// 拒绝目标
interface RejectGoalRequest {
  reason?: string;
  neverAskAgain?: boolean;  // 永不再提醒同类目标
}

// GET /api/autonomous/satisfaction/history?window=7d
// 获取满意度历史
interface SatisfactionHistory {
  dataPoints: Array<{
    timestamp: string;
    score: number;
    windowType: 'short' | 'medium' | 'long';
  }>;
}

// GET /api/autonomous/capabilities  (P1)
// 获取能力详情
interface CapabilityDetail {
  dimension: string;
  level: number;
  confidence: number;
  boundary: number;
  testCount: number;
  recentTests: Array<{
    taskSummary: string;
    difficulty: number;
    result: 'success' | 'partial' | 'failure';
    timestamp: string;
  }>;
}

// GET /api/autonomous/reflections?limit=10  (P1)
// 获取反思记录
interface Reflection {
  id: string;
  triggerReason: string;
  diagnosis: {
    primaryIssue: string;
    affectedDimensions: string[];
    rootCause: string;
  };
  recommendations: Array<{
    type: string;
    description: string;
    targetDimensions: string[];
    feasibility: number;
    impact: number;
  }>;
  suggestedGoals: Array<{
    type: string;
    description: string;
    priority: number;
  }>;
  createdAt: string;
}

// GET /api/autonomous/prompt-variants
// 获取 Prompt 变体统计
interface PromptVariantStats {
  fragmentKey: string;
  variants: Array<{
    id: string;
    isBaseline: boolean;
    trialCount: number;
    successCount: number;
    avgSatisfaction: number;
    ucbScore: number;
  }>;
}
```

---

## 三、分阶段实施

### 阶段 1：核心可见性（P0，2 天，16h）

**目标：** 用户能看到 Agent 在工作，能批准目标

#### Task 1.1：API 层实现（6h）

**文件：** `packages/api/src/routes/autonomous.ts`

```typescript
import { Router } from 'express';
import { autonomousCoordinator } from '@lumii/agent-runtime';

const router = Router();

// 获取状态
router.get('/status', async (req, res) => {
  try {
    const status = await autonomousCoordinator.getStatus();
    res.json(status);
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

// 获取待批准目标
router.get('/goals', async (req, res) => {
  const { status = 'pending' } = req.query;
  try {
    const goals = await autonomousCoordinator.getGoals({ status });
    res.json({ goals });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

// 批准目标
router.post('/goals/:id/approve', async (req, res) => {
  const { id } = req.params;
  const { note } = req.body;
  
  try {
    const result = await autonomousCoordinator.approveGoal(id, { note });
    res.json(result);
  } catch (error) {
    res.status(400).json({ error: error.message });
  }
});

// 拒绝目标
router.post('/goals/:id/reject', async (req, res) => {
  const { id } = req.params;
  const { reason, neverAskAgain } = req.body;
  
  try {
    const result = await autonomousCoordinator.rejectGoal(id, { 
      reason, 
      neverAskAgain 
    });
    res.json(result);
  } catch (error) {
    res.status(400).json({ error: error.message });
  }
});

export default router;
```

**验收标准：**
- [ ] 所有端点返回正确的 JSON 格式
- [ ] 批准/拒绝目标后数据库状态正确更新
- [ ] 错误处理返回合适的 HTTP 状态码

#### Task 1.2：前端 API 客户端（2h）

**文件：** `apps/web/src/lib/autonomous/api.ts`

```typescript
const BASE_URL = '/api/autonomous';

export async function getAutonomousStatus(): Promise<AutonomousStatus> {
  const res = await fetch(`${BASE_URL}/status`);
  if (!res.ok) throw new Error('Failed to fetch status');
  return res.json();
}

export async function getPendingGoals(): Promise<Goal[]> {
  const res = await fetch(`${BASE_URL}/goals?status=pending`);
  if (!res.ok) throw new Error('Failed to fetch goals');
  const data = await res.json();
  return data.goals;
}

export async function approveGoal(id: string, note?: string): Promise<Goal> {
  const res = await fetch(`${BASE_URL}/goals/${id}/approve`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ note }),
  });
  if (!res.ok) throw new Error('Failed to approve goal');
  const data = await res.json();
  return data.goal;
}

export async function rejectGoal(
  id: string, 
  options?: { reason?: string; neverAskAgain?: boolean }
): Promise<void> {
  const res = await fetch(`${BASE_URL}/goals/${id}/reject`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(options),
  });
  if (!res.ok) throw new Error('Failed to reject goal');
}
```

#### Task 1.3：简化版仪表板页面（6h）

**文件：** `apps/web/src/pages/autonomous/index.tsx`

```typescript
import { useQuery, useMutation } from '@tanstack/react-query';
import { getAutonomousStatus, getPendingGoals, approveGoal, rejectGoal } from '@/lib/autonomous/api';
import { SatisfactionMeter } from '@/components/autonomous/SatisfactionMeter';
import { GoalCard } from '@/components/autonomous/GoalCard';

export default function AutonomousDashboard() {
  const { data: status, isLoading } = useQuery({
    queryKey: ['autonomous-status'],
    queryFn: getAutonomousStatus,
    refetchInterval: 30000, // 每 30 秒刷新
  });

  const { data: goals = [] } = useQuery({
    queryKey: ['pending-goals'],
    queryFn: getPendingGoals,
    refetchInterval: 10000,
  });

  const approveMutation = useMutation({
    mutationFn: (id: string) => approveGoal(id),
    onSuccess: () => {
      queryClient.invalidateQueries(['pending-goals']);
    },
  });

  const rejectMutation = useMutation({
    mutationFn: (id: string) => rejectGoal(id),
    onSuccess: () => {
      queryClient.invalidateQueries(['pending-goals']);
    },
  });

  if (isLoading) return <div>加载中...</div>;

  return (
    <div className="container mx-auto p-6">
      <div className="flex justify-between items-center mb-8">
        <h1 className="text-3xl font-bold">自主进化仪表板</h1>
        <span className={`badge ${status?.enabled ? 'badge-success' : 'badge-error'}`}>
          {status?.enabled ? '✓ 已启用' : '✗ 未启用'}
        </span>
      </div>

      {/* 满意度概览 */}
      <section className="mb-8">
        <h2 className="text-xl font-semibold mb-4">📊 满意度评分</h2>
        <SatisfactionMeter 
          score={status?.satisfaction.overall || 0}
          trend={status?.satisfaction.trend}
          breakdown={status?.satisfaction.breakdown}
        />
      </section>

      {/* 待批准目标 */}
      <section>
        <h2 className="text-xl font-semibold mb-4">
          🎯 待批准目标 ({goals.length})
        </h2>
        {goals.length === 0 ? (
          <p className="text-gray-500">暂无待批准目标</p>
        ) : (
          <div className="space-y-4">
            {goals.map((goal) => (
              <GoalCard
                key={goal.id}
                goal={goal}
                onApprove={() => approveMutation.mutate(goal.id)}
                onReject={() => rejectMutation.mutate(goal.id)}
              />
            ))}
          </div>
        )}
      </section>
    </div>
  );
}
```

#### Task 1.4：目标卡片组件（2h）

**文件：** `apps/web/src/components/autonomous/GoalCard.tsx`

```typescript
import { Goal } from '@/lib/autonomous/types';

interface GoalCardProps {
  goal: Goal;
  onApprove: () => void;
  onReject: () => void;
}

export function GoalCard({ goal, onApprove, onReject }: GoalCardProps) {
  const typeLabels = {
    learning: '学习目标',
    'proactive-message': '主动消息',
    'capability-improvement': '能力提升',
  };

  const priorityStars = '⭐'.repeat(Math.ceil(goal.priority * 5));

  return (
    <div className="border rounded-lg p-6 bg-white shadow-sm hover:shadow-md transition">
      <div className="flex justify-between items-start mb-4">
        <div>
          <span className="badge badge-primary">{typeLabels[goal.type]}</span>
          <h3 className="text-lg font-semibold mt-2">{goal.description}</h3>
        </div>
        <span className="text-sm">{priorityStars}</span>
      </div>

      <div className="space-y-2 text-sm text-gray-600 mb-4">
        <p>• 触发原因: {goal.triggerReason}</p>
        <p>• 用户价值: {(goal.userValueScore * 100).toFixed(0)}%</p>
        <p>• 可行性: {(goal.feasibility * 100).toFixed(0)}%</p>
      </div>

      <div className="flex gap-3">
        <button
          onClick={onApprove}
          className="btn btn-primary flex-1"
        >
          批准并执行
        </button>
        <button
          onClick={onReject}
          className="btn btn-outline flex-1"
        >
          拒绝
        </button>
      </div>
    </div>
  );
}
```

**验收标准：**
- [ ] 仪表板能显示当前满意度评分
- [ ] 能看到所有待批准目标
- [ ] 点击批准/拒绝按钮后目标状态正确更新
- [ ] 页面每 30 秒自动刷新数据

---

### 阶段 2：能力可视化（P1，1.5 天，12h）

**目标：** 显示 P1 的能力追踪和反思功能

#### Task 2.1：能力追踪 API（2h）

**文件：** `packages/api/src/routes/autonomous.ts`（扩展）

```typescript
// 获取能力详情
router.get('/capabilities', async (req, res) => {
  try {
    const capabilities = await capabilityTracker.getAll();
    res.json({ capabilities });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});

// 获取能力维度详情
router.get('/capabilities/:dimension', async (req, res) => {
  const { dimension } = req.params;
  try {
    const detail = await capabilityTracker.getDetail(dimension);
    res.json(detail);
  } catch (error) {
    res.status(404).json({ error: 'Capability not found' });
  }
});

// 获取反思记录
router.get('/reflections', async (req, res) => {
  const { limit = 10 } = req.query;
  try {
    const reflections = await reflectionEngine.getHistory({ limit: Number(limit) });
    res.json({ reflections });
  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
```

#### Task 2.2：能力雷达图组件（4h）

**文件：** `apps/web/src/components/autonomous/CapabilityRadar.tsx`

```typescript
import { RadarChart, PolarGrid, PolarAngleAxis, PolarRadiusAxis, Radar, Legend } from 'recharts';

interface CapabilityRadarProps {
  capabilities: Record<string, { level: number; confidence: number }>;
}

export function CapabilityRadar({ capabilities }: CapabilityRadarProps) {
  const data = Object.entries(capabilities).map(([dim, state]) => ({
    dimension: dimensionLabels[dim] || dim,
    level: state.level * 100,
    confidence: state.confidence * 100,
  }));

  return (
    <div className="flex justify-center">
      <RadarChart width={500} height={400} data={data}>
        <PolarGrid />
        <PolarAngleAxis dataKey="dimension" />
        <PolarRadiusAxis angle={90} domain={[0, 100]} />
        <Radar
          name="能力水平"
          dataKey="level"
          stroke="#8884d8"
          fill="#8884d8"
          fillOpacity={0.6}
        />
        <Radar
          name="置信度"
          dataKey="confidence"
          stroke="#82ca9d"
          fill="#82ca9d"
          fillOpacity={0.3}
        />
        <Legend />
      </RadarChart>
    </div>
  );
}

const dimensionLabels = {
  code_generation: '代码生成',
  document_analysis: '文档分析',
  web_search: '网络搜索',
  data_processing: '数据处理',
  api_integration: 'API集成',
  creative_writing: '创意写作',
  logical_reasoning: '逻辑推理',
  multi_step_planning: '多步规划',
};
```

#### Task 2.3：能力进度条组件（2h）

**文件：** `apps/web/src/components/autonomous/CapabilityProgressBar.tsx`

```typescript
interface CapabilityProgressBarProps {
  dimension: string;
  level: number;
  confidence: number;
  testCount: number;
  trend?: 'up' | 'down' | 'stable';
}

export function CapabilityProgressBar({
  dimension,
  level,
  confidence,
  testCount,
  trend,
}: CapabilityProgressBarProps) {
  const trendIcon = trend === 'up' ? '↑' : trend === 'down' ? '↓' : '→';
  const trendColor = trend === 'up' ? 'text-green-500' : trend === 'down' ? 'text-red-500' : 'text-gray-500';

  return (
    <div className="mb-4">
      <div className="flex justify-between items-center mb-2">
        <span className="font-medium">{dimensionLabels[dimension]}</span>
        <span className={`text-sm ${trendColor}`}>
          {(level * 100).toFixed(0)}% {trendIcon}
        </span>
      </div>
      <div className="relative h-6 bg-gray-200 rounded-full overflow-hidden">
        <div
          className="h-full bg-blue-500 transition-all duration-500"
          style={{ width: `${level * 100}%` }}
        />
        {/* 置信度指示器（虚线边框） */}
        <div
          className="absolute top-0 left-0 h-full border-2 border-dashed border-gray-400"
          style={{ width: `${confidence * 100}%` }}
        />
      </div>
      <div className="flex justify-between text-xs text-gray-500 mt-1">
        <span>测试次数: {testCount}</span>
        <span>置信度: {(confidence * 100).toFixed(0)}%</span>
      </div>
    </div>
  );
}
```

#### Task 2.4：反思卡片组件（2h）

**文件：** `apps/web/src/components/autonomous/ReflectionCard.tsx`

```typescript
import { Reflection } from '@/lib/autonomous/types';

interface ReflectionCardProps {
  reflection: Reflection;
}

export function ReflectionCard({ reflection }: ReflectionCardProps) {
  return (
    <div className="border rounded-lg p-6 bg-white shadow-sm">
      <div className="flex justify-between items-start mb-4">
        <h3 className="text-lg font-semibold">💭 反思记录</h3>
        <span className="text-sm text-gray-500">
          {new Date(reflection.createdAt).toLocaleString('zh-CN')}
        </span>
      </div>

      <div className="space-y-4">
        {/* 问题诊断 */}
        <div>
          <h4 className="font-medium text-red-600 mb-2">问题诊断</h4>
          <p className="text-sm">{reflection.diagnosis.primaryIssue}</p>
          <p className="text-xs text-gray-500 mt-1">
            影响维度: {reflection.diagnosis.affectedDimensions.join(', ')}
          </p>
        </div>

        {/* 改进建议 */}
        <div>
          <h4 className="font-medium text-blue-600 mb-2">
            改进建议 ({reflection.recommendations.length})
          </h4>
          <ul className="space-y-2">
            {reflection.recommendations.slice(0, 3).map((rec, i) => (
              <li key={i} className="text-sm flex items-start">
                <span className="mr-2">{i + 1}.</span>
                <div className="flex-1">
                  <p>{rec.description}</p>
                  <div className="flex gap-3 text-xs text-gray-500 mt-1">
                    <span>可行性: {(rec.feasibility * 100).toFixed(0)}%</span>
                    <span>预期影响: {(rec.impact * 100).toFixed(0)}%</span>
                  </div>
                </div>
              </li>
            ))}
          </ul>
        </div>

        {/* 生成的目标 */}
        {reflection.suggestedGoals.length > 0 && (
          <div>
            <h4 className="font-medium text-green-600 mb-2">
              建议目标 ({reflection.suggestedGoals.length})
            </h4>
            <ul className="text-sm space-y-1">
              {reflection.suggestedGoals.map((goal, i) => (
                <li key={i}>• {goal.description}</li>
              ))}
            </ul>
          </div>
        )}
      </div>
    </div>
  );
}
```

#### Task 2.5：集成到仪表板（2h）

在 `apps/web/src/pages/autonomous/index.tsx` 中添加：

```typescript
// 添加查询
const { data: capabilities } = useQuery({
  queryKey: ['capabilities'],
  queryFn: getCapabilities,
});

const { data: reflections = [] } = useQuery({
  queryKey: ['reflections'],
  queryFn: () => getReflections({ limit: 5 }),
});

// 添加到页面
<section className="mb-8">
  <h2 className="text-xl font-semibold mb-4">🧠 能力状态</h2>
  <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
    <CapabilityRadar capabilities={capabilities} />
    <div>
      {Object.entries(capabilities).map(([dim, state]) => (
        <CapabilityProgressBar key={dim} dimension={dim} {...state} />
      ))}
    </div>
  </div>
</section>

<section>
  <h2 className="text-xl font-semibold mb-4">💭 最近反思</h2>
  {reflections.map((ref) => (
    <ReflectionCard key={ref.id} reflection={ref} />
  ))}
</section>
```

**验收标准：**
- [ ] 能力雷达图正确显示 8 个维度
- [ ] 能力进度条显示水平和置信度
- [ ] 反思卡片显示问题诊断和建议
- [ ] 数据实时从后端获取

---

### 阶段 3：完整体验（P2，1 天，9h）

**目标：** 满意度历史、Prompt 统计、通知系统

#### Task 3.1：满意度趋势图（3h）

**文件：** `apps/web/src/components/autonomous/SatisfactionChart.tsx`

```typescript
import { LineChart, Line, XAxis, YAxis, CartesianGrid, Tooltip, Legend } from 'recharts';

interface SatisfactionChartProps {
  history: Array<{ timestamp: string; score: number; windowType: string }>;
}

export function SatisfactionChart({ history }: SatisfactionChartProps) {
  const data = history.map((point) => ({
    time: new Date(point.timestamp).toLocaleDateString('zh-CN', { 
      month: 'short', 
      day: 'numeric' 
    }),
    score: (point.score * 100).toFixed(0),
  }));

  return (
    <LineChart width={600} height={300} data={data}>
      <CartesianGrid strokeDasharray="3 3" />
      <XAxis dataKey="time" />
      <YAxis domain={[0, 100]} />
      <Tooltip />
      <Legend />
      <Line 
        type="monotone" 
        dataKey="score" 
        stroke="#8884d8" 
        name="满意度 (%)" 
        strokeWidth={2}
      />
    </LineChart>
  );
}
```

#### Task 3.2：Prompt 变体统计（2h）

**文件：** `apps/web/src/components/autonomous/PromptVariantStats.tsx`

```typescript
interface PromptVariantStatsProps {
  stats: PromptVariantStats[];
}

export function PromptVariantStats({ stats }: PromptVariantStatsProps) {
  return (
    <div className="space-y-6">
      {stats.map((stat) => (
        <div key={stat.fragmentKey} className="border rounded-lg p-4">
          <h3 className="font-semibold mb-3">{stat.fragmentKey}</h3>
          <div className="space-y-2">
            {stat.variants
              .sort((a, b) => b.avgSatisfaction - a.avgSatisfaction)
              .map((variant) => (
                <div key={variant.id} className="flex items-center justify-between">
                  <div className="flex items-center gap-2">
                    <span className="text-sm">{variant.id}</span>
                    {variant.isBaseline && (
                      <span className="badge badge-sm">基线</span>
                    )}
                  </div>
                  <div className="flex items-center gap-4 text-sm">
                    <span>成功率: {((variant.successCount / variant.trialCount) * 100).toFixed(0)}%</span>
                    <span>使用次数: {variant.trialCount}</span>
                    <span>满意度: {(variant.avgSatisfaction * 100).toFixed(0)}%</span>
                    {variant.avgSatisfaction === Math.max(...stat.variants.map(v => v.avgSatisfaction)) && (
                      <span className="text-green-500">⭐</span>
                    )}
                  </div>
                </div>
              ))}
          </div>
        </div>
      ))}
    </div>
  );
}
```

#### Task 3.3：桌面通知系统（3h）

**文件：** `apps/web/src/lib/autonomous/notifications.ts`

```typescript
export async function requestNotificationPermission() {
  if (!('Notification' in window)) {
    return false;
  }
  
  const permission = await Notification.requestPermission();
  return permission === 'granted';
}

export function sendGoalNotification(goal: Goal) {
  if (Notification.permission !== 'granted') {
    return;
  }

  const typeLabels = {
    learning: '学习目标',
    'proactive-message': '主动消息',
    'capability-improvement': '能力提升',
  };

  new Notification('🎯 Agent 生成了新目标', {
    body: `[${typeLabels[goal.type]}] ${goal.description}`,
    icon: '/icons/autonomous-agent.png',
    tag: `goal-${goal.id}`,
    requireInteraction: true,
    actions: [
      { action: 'approve', title: '批准' },
      { action: 'reject', title: '拒绝' },
    ],
  });
}

// 在应用启动时请求权限
export function initializeNotifications() {
  if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('/sw.js').then((registration) => {
      console.log('Service Worker registered');
    });
  }
  
  requestNotificationPermission();
}
```

**集成到应用：**

```typescript
// apps/web/src/pages/_app.tsx
import { initializeNotifications } from '@/lib/autonomous/notifications';

useEffect(() => {
  initializeNotifications();
}, []);

// 轮询新目标
useEffect(() => {
  const interval = setInterval(async () => {
    const goals = await getPendingGoals();
    const newGoals = goals.filter(g => !seenGoalIds.has(g.id));
    
    newGoals.forEach(goal => {
      sendGoalNotification(goal);
      seenGoalIds.add(goal.id);
    });
  }, 60000); // 每分钟检查一次
  
  return () => clearInterval(interval);
}, []);
```

#### Task 3.4：导航和入口（1h）

在主导航中添加入口：

```typescript
// apps/web/src/components/Navigation.tsx
<nav>
  {/* ...其他菜单项 */}
  <a href="/autonomous" className="nav-link">
    <span className="relative">
      🧠 自主进化
      {pendingGoalsCount > 0 && (
        <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs rounded-full w-4 h-4 flex items-center justify-center">
          {pendingGoalsCount}
        </span>
      )}
    </span>
  </a>
</nav>
```

**验收标准：**
- [ ] 满意度趋势图显示最近 7 天数据
- [ ] Prompt 变体统计显示各变体效果
- [ ] 新目标生成时发送桌面通知
- [ ] 主导航显示待批准目标数量徽章

---

## 四、测试计划

### 4.1 单元测试

```typescript
// apps/web/src/components/autonomous/__tests__/GoalCard.test.tsx
import { render, fireEvent } from '@testing-library/react';
import { GoalCard } from '../GoalCard';

describe('GoalCard', () => {
  const mockGoal = {
    id: 'goal-1',
    type: 'learning',
    description: '测试目标',
    priority: 0.8,
    // ...
  };

  it('should render goal information', () => {
    const { getByText } = render(<GoalCard goal={mockGoal} onApprove={() => {}} onReject={() => {}} />);
    expect(getByText('测试目标')).toBeInTheDocument();
  });

  it('should call onApprove when approve button clicked', () => {
    const onApprove = jest.fn();
    const { getByText } = render(<GoalCard goal={mockGoal} onApprove={onApprove} onReject={() => {}} />);
    fireEvent.click(getByText('批准并执行'));
    expect(onApprove).toHaveBeenCalled();
  });
});
```

### 4.2 集成测试

```typescript
// apps/web/src/pages/autonomous/__tests__/index.test.tsx
import { render, waitFor } from '@testing-library/react';
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import AutonomousDashboard from '../index';

describe('AutonomousDashboard', () => {
  it('should load and display status', async () => {
    const queryClient = new QueryClient();
    const { getByText } = render(
      <QueryClientProvider client={queryClient}>
        <AutonomousDashboard />
      </QueryClientProvider>
    );

    await waitFor(() => {
      expect(getByText(/满意度评分/)).toBeInTheDocument();
    });
  });
});
```

### 4.3 E2E 测试

```typescript
// e2e/autonomous.spec.ts
import { test, expect } from '@playwright/test';

test('autonomous dashboard workflow', async ({ page }) => {
  await page.goto('/autonomous');
  
  // 验证页面加载
  await expect(page.locator('h1')).toContainText('自主进化仪表板');
  
  // 验证满意度显示
  await expect(page.locator('[data-testid="satisfaction-score"]')).toBeVisible();
  
  // 批准一个目标
  await page.click('[data-testid="goal-approve-btn"]:first-of-type');
  await expect(page.locator('[data-testid="goal-approved-toast"]')).toBeVisible();
});
```

---

## 五、性能优化

### 5.1 数据获取优化

```typescript
// 使用 SWR 的 revalidate 策略
const { data } = useSWR('/api/autonomous/status', fetcher, {
  refreshInterval: 30000,
  revalidateOnFocus: false,
  dedupingInterval: 10000,
});
```

### 5.2 组件懒加载

```typescript
// 雷达图较大，懒加载
const CapabilityRadar = lazy(() => import('@/components/autonomous/CapabilityRadar'));

<Suspense fallback={<Skeleton />}>
  <CapabilityRadar capabilities={capabilities} />
</Suspense>
```

### 5.3 虚拟滚动（如果目标很多）

```typescript
import { FixedSizeList } from 'react-window';

<FixedSizeList
  height={600}
  itemCount={goals.length}
  itemSize={120}
>
  {({ index, style }) => (
    <div style={style}>
      <GoalCard goal={goals[index]} />
    </div>
  )}
</FixedSizeList>
```

---

## 六、部署和灰度

### 6.1 Feature Flag

```typescript
// packages/agent-runtime/src/autonomous/config.ts
export const AUTONOMOUS_CONFIG = {
  enabled: process.env.AUTONOMOUS_ENABLED === 'true',
  frontend: {
    dashboard: process.env.AUTONOMOUS_DASHBOARD_ENABLED === 'true',
    notifications: process.env.AUTONOMOUS_NOTIFICATIONS_ENABLED === 'true',
  },
};

// 前端中检查
if (config.frontend.dashboard) {
  // 显示自主进化菜单
}
```

### 6.2 灰度策略

```typescript
// 按用户 ID 灰度
function isAutonomousEnabledForUser(userId: string): boolean {
  const hash = hashCode(userId);
  const rolloutPercent = 10; // 10% 用户
  return (hash % 100) < rolloutPercent;
}
```

---

## 七、监控和告警

### 7.1 前端埋点

```typescript
// 记录用户交互
analytics.track('autonomous_goal_approved', {
  goalId: goal.id,
  goalType: goal.type,
  priority: goal.priority,
});

analytics.track('autonomous_dashboard_visited', {
  timestamp: Date.now(),
});
```

### 7.2 性能监控

```typescript
// 记录页面加载时间
const start = performance.now();
// ...加载数据
const end = performance.now();

if (end - start > 3000) {
  logger.warn('Autonomous dashboard slow load', { duration: end - start });
}
```

---

## 八、文档交付

### 8.1 用户文档

- ✅ 已完成：`用户指南-如何使用和观测自主进化Agent.md`
- 需补充：仪表板使用截图和 GIF 演示

### 8.2 开发文档

```markdown
# 自主进化前端开发指南

## 添加新的能力维度

1. 在后端 `CapabilityDimension` 枚举中添加
2. 在前端 `dimensionLabels` 中添加中文标签
3. 更新雷达图数据映射

## 添加新的目标类型

1. 在 `GoalType` 枚举中添加
2. 在 `GoalCard` 的 `typeLabels` 中添加标签
3. 更新通知文案
```

---

## 九、总结

### 完成后用户可以：

1. ✅ 在仪表板看到 Agent 的满意度评分和趋势
2. ✅ 批准或拒绝 Agent 生成的自主目标
3. ✅ 观察 8 个能力维度的水平和置信度（P1）
4. ✅ 查看 Agent 的自我反思记录（P1）
5. ✅ 了解 Prompt 进化的效果统计
6. ✅ 通过桌面通知及时响应新目标
7. ✅ 追踪 Agent 的进化历程

### 工时总结

| 阶段 | 任务 | 工时 |
|------|------|------|
| 阶段 1 | API 层 + 简化仪表板 + 目标卡片 | 16h |
| 阶段 2 | 能力可视化（雷达图、进度条、反思卡片） | 12h |
| 阶段 3 | 趋势图、Prompt 统计、通知系统 | 9h |
| **总计** | | **37h** |

### 下一步

1. **立即开始**：实施阶段 1（核心可见性）
2. **并行准备**：准备测试数据和 Mock API
3. **迭代优化**：根据用户反馈调整 UI/UX
4. **性能监控**：部署后持续监控性能指标

---

**文档版本：** v1.0  
**创建日期：** 2026-09-04  
**状态：** 待评审
