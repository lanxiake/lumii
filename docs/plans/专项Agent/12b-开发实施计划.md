# 12b · 开发实施计划

> 创建：2026-09-16
> 状态：**实施中** —— B1 ✅ · B2 ✅（含 B1 遗留两条）· B3 ✅（实际 33 → 31，非 27）· B4 ✅
> 上位：[12 · 一级公民的积累与演化](./12-一级公民的积累与演化.md)（调研）· [12a · 详细实施方案](./12a-详细实施方案.md)（方案与拍板）
> 体例：每个批次 = 一次可独立提交、独立验证、可单独回滚的改动

---

## 零、范围与拍板前提

**已拍板**：`bash` **保留**（12a §3.3-decision）。由此：
- 工具收敛只做**无争议档 33 → 27**，家族合并档不做
- 收敛的定性从「限制能力」改为「整理语义重叠」，**优先级低于有实测数据的项**

**B1 开工后据实测重定靶**（2026-09-16）：原定的「`web_search` 43% 失败率」经核实是
**历史存量**（2026-09-02 之前无内置 provider 时期的必然抛错），近 7 天实测为 0%。
真正在失败的是 `web_fetch`（35.1%），且 76% 是网络出口而非工具设计。
用户拍板：**改靶 web_fetch + 收尾 B1.4**，代理/网络出口部分另行决定。
详细证据见 B1「实施结果」。

**本计划覆盖**：B1 `web_fetch` 失败可用化 + 搜索用法固化 · B2 测量补全 · B3 工具收敛 · B4 自述注入 · B5 「少推 X」四件套 · B6 从发现到动作 · B7 事件级去重 + 来源质量

**明确不做**（12a §四 P2）：提示词进化（信号量门槛未到）、加新 Agent、家族合并

---

## 一、批次总表

| 批次 | 内容 | 体量 | 前置 | 提交边界 | 状态 |
|---|---|---|---|---|---|
| **B1** | ~~`web_search` 失败归因~~ → **`web_fetch` 失败可用化 + 搜索用法固化** | 小 | 无 | 1–3 笔 | ✅ **已完成**（3 笔：`42728ba2` `bb9c3f7e` `dada7e4d` + 快照修复 `ebff003f`） |
| **B2** | 测量补全：统计加 agent 维度 + 审计补 definition_id + 回读 UI | 小 | 无 | 1 笔 | ✅ **已完成**（`2c341408`；B2.3 已并入 B1） |
| **B3** | 工具面收敛：**33 → 31**（原定 27，4 条复核后不成立） | 中（牵动全仓引用） | B2（数据支撑） | 1–2 笔 | ✅ **已完成**（`863fc069`） |
| **B4** | 执行时注入「自述」 | 小 | 无 | 1 笔 | ✅ **已完成**（`d10b3dab`） |
| **B5** | 「少推 X」四件套（预览 / 优先级 / 规则日志） | 中 | 无 | 2–3 笔 | 待开工 |
| **B6** | 维护的「从发现到动作」 | 中大 | 无（地基已有） | 3–4 笔 | 待开工 |
| **B7** | 事件级去重（轻量版）+ 来源质量 | 中 | 无；与 B4/B6 共享「事件」概念 | 2 笔 | 待开工 |

**执行顺序**：`B1 → B2 → B3 → B4 → B5 → B6 → B7`。下一步是 **B3**。

> B1 排在 B2 前，是因为它最贵（情报每两次搜索白跑一次）。但 **B1 的第一步本身就是最小测量**——不是「为测量而测量」，是「为了修这一处，先看清它怎么坏的」。B2 是把这套测量补成通用能力。

---

## B1 · ✅ 已完成（原定 `web_search`，实施中重定靶为 `web_fetch`）

### B1.R 实施结果（2026-09-16）

**原计划假设**：`web_search` 43% 失败率是本机工具面最大的单点问题（数据来自
`tool_usage_stats` 的 418 调用 / 181 失败）。

**实测推翻了它**（12b §四 的确认点，写这份计划时就预置在「失败主因与预期不符时先停下来对齐」）：

| 证据 | 结果 |
|---|---|
| 机制 | `85667e90`（2026-09-02 12:23）之前 `web_search` 没有内置 provider，未配 `LANGSEARCH_API_KEY`/`SEARXNG_BASE_URL` 时**每次调用必抛** `No search provider configured` |
| 统计交叉验证 | `count` 418 → 472（**+54 次调用**）而 `error_count` **纹丝不动停在 181** |
| 日志实测（09-10~09-16） | `web_search` **165 次调用 / 0 次失败**，全走内置 Bing，每次 4~10 条结果，无一次 `provider=unknown` |

→ **181 次是 09-02 之前那个时期攒下的存量，当前失败率为 0%。**
→ 根因是「统计表没有时间维度，把历史存量当成当前状态读了」——B2.1 加 agent 维度时，
一并考虑给统计加时间窗，否则同一个坑还会再踩。

**真正在失败的是 `web_fetch`**（近 7 天全工具 4100 次调用 / 164 次失败 = 4.0%）：

| 工具 | 调用 | 失败 | 失败率 |
|---|---|---|---|
| **`web_fetch`** | 313 | **110** | **35.1%**（占全部失败的 67%） |
| `task_complete` | 52 | 8 | 15.4% |
| 其余全部 | — | — | < 7% |
| `web_search` | 165 | 0 | 0% |

归因（87 条可解析）：**76% 连接层失败**（`zh.wikisource.org` 18、`arxiv.org` 16、
`github.com` 4、`en.wikipedia.org` 3、`huggingface.co` 3）、13% 404、8% 429（`api.semanticscholar.org` 7）、3% 403。

**用户拍板**：改靶 `web_fetch` + 收尾 B1.4；代理/网络出口部分**另行决定**（未做）。

### B1.1 ✅ 让失败可归因（提交 `42728ba2`）

`tool-usage-hook` 在 `onError` / `afterExecute(isError)` 两个失败出口各写一条
`tool_audit_log`（`is_error=1` + 错误文本压单行后截断 300 字）。放在 hook 而非各工具内部——
它是所有工具的统一出口，一处接线全工具受益（原计划的 B2.3 一并覆盖，故 B2 只剩两件事）。

压单行不只是省空间：归因要按错误文本 `GROUP BY`，多行会让同一种失败分裂成多组。
只记失败不记成功——成功没有归因价值，全量落库只会把审计表淹掉。

> 原计划的验收「跑一轮资讯任务后 `tool_audit_log` 里出现 `web_search` 的失败行」
> **已不成立**（它不失败了），改用真机 `web_fetch` 失败验证，见下。

### B1.2 ✅ 归因（原计划的「跑一轮看分布」被日志复盘取代）

不必等新代码跑数据：`~/.lumii/logs/app/mtbot-*.log` 里的渲染层 `tool:end` 事件
（含 `isError` 与 `resultPreview`）已覆盖近 7 天全部 4100 次调用，直接出分布表。
这比原计划的「等一轮 cron」更快也更全。**但注意**：主进程的 `[ToolRunner] ←` 只在
`afterExecute` 记，抛错路径整条不可见——这正是 B1.1 要补的洞。

### B1.3 ✅ 按分布修（重定靶到 `web_fetch`，提交 `bb9c3f7e`）

诊断信息是在**两处**被扔掉的，只修一处都不够：

1. `local-web.ts` 的 `classifyFetchError` 只读 `err.message`，而 undici 的 message 永远是
   笼统的 `fetch failed`，真正的原因（`UND_ERR_CONNECT_TIMEOUT` / `ECONNRESET` /
   `ENOTFOUND`）挂在 `err.cause.code` 或 `AggregateError.errors[]` 上。
   实测本机 133 次抓取失败日志全是「请求失败: fetch failed」，等于没记。
2. `web-fetch-tool` 抛 `HTTP ${status}: Failed to fetch ${url}`，把宿主已分类好的原因
   （放在 body 里）整个丢了；`status=0` 时模型只看到一个 0。

修法：递归挖 `cause`/`errors[]` 的 code + 码表译人话；`status=0` 带出原因并明说
「该站点不可达，换来源或用 web_search 找镜像」；404/403/429/5xx 各给一句可执行的下一步。
**只有 `status=0` 取 body**——其它状态码下 body 是站点错误页 HTML，塞进去纯属噪声。

**真实网络验证**（vitest node 环境，非 mock）：
- `zh.wikisource.org` →「无法访问 …：连接超时（10 秒内没建立起连接）（UND_ERR_CONNECT_TIMEOUT）。
  该站点在当前网络下不可达——换一个来源，或用 web_search 找它的镜像与转载。」
- 失效的 gov.cn 链接 →「HTTP 404: … 页面不存在（链接已失效，或 URL 有误）。
  先用 web_search 确认真实链接，不要凭记忆拼 URL。」

**真机验证**：重启客户端 → 新建会话 → 让 assistant 抓 `zh.wikisource.org` →
`tool_audit_log` 出现 `id=12318 / tool_name=web_fetch / is_error=1`，
摘要即上面那句；会话消息里模型的 tool result 也是同一句。

### B1.4 ✅ 把情报摸出来的经验固化（提交 `dada7e4d`）

情报用两轮摸出「web_search 结果被日历/导航类页面严重污染，可靠路径是直接抓列表页」，
但这条经验只躺在一条自由文本记忆里，下一轮靠 `memory_search` 在 228 条里碰运气。

**先实测再写**（走工具的真实链路：axios + cheerio 解析 `cn.bing.com`）：

| 用例 | 结果 |
|---|---|
| `OpenAI 发布 新模型` / 英文查询 | ✅ 5/5 相关，英文官网页排第一 |
| `刘隆 度田 后汉书`（生僻中文专名） | ❌ 返回「刘」字百科、刘姓源流——被拆成单字 |
| 带引号的古文原文 | ❌ 同样退化成字典页 |
| `site:zh.wikisource.org` | ❌ **被完全忽略**，返回「度」字的百科页 |
| `2026年9月10日 今日重大新闻 科技 财经` | ❌ 首条是「2026年大事、重要节日一览表（附放假安排）」——情报说的日历页污染，复现 |
| `36氪 快讯` / `澎湃新闻 要闻` | ✅ 首条即目标站；36kr 列表页 `web_fetch` 实测 HTTP 200 / 94KB / 正文可解析 |

固化到两处：`web_search` 的 description（从「介绍 provider」改成「讲清擅长什么、
什么时候该换路子」）+ `INFO_CURATOR_PROMPT` 新增「怎么搜」一节。

> `web_search` 的**结果质量**问题（212 条结果 URL 里 84 条是 `baike.baidu.com`，40%）
> 只做了「教模型绕开」，没做「从源头改善」——后者要换搜索源，属另一件事。

### B1 遗留

| 事项 | 数据 | 状态 |
|---|---|---|
| **失败的 fetch 太慢** | 一次失败实测 **23.5 秒**（2 次尝试 × 10 秒连接超时 + 2 秒退避），110 次/周 ≈ **43 分钟纯等待** | ✅ **已修**（`61525a26`）：只对「拿到响应后读正文才断掉」重试，连接层建不起来直接放弃。一次失败降到 ~10.7 秒。<br>改前先做了实验：对同一被拦站点连续 4 次（含间隔 45 秒的第 4 次）**结果完全一致**，重试零价值 |
| **网络出口（代理）** | 76% 的失败是境外站点 | ⏸ **仍未做，等你拍板**。新证据：这些站点**不是「不可达」——TCP 握手成功（zh.wikisource.org 114ms），被拦在 TLS**。这改变了选项的含义：代理不再是「绕过网络不通」，而是「换一条 TLS 出口」。要走需先定：全站走还是白名单、从设置页读还是环境变量 |
| **`web_search` 的 0 结果静默成功** | `items.length===0` 且无 provider 报错时，以 `provider="unknown"` 返回「未找到」并计为成功 | ✅ **已修**（`9f9bcb4c`）：标成独立结局 `provider='none'` + warn 日志 + 一句可执行的下一步。**不谎称是错误**——那会污染失败率 |

---

## B2 · ✅ 已完成（提交 `2c341408`）

### B2.R 实施结果（2026-09-16）

一句话：**现在能回答「某个 Agent 用过哪些工具、各几次、失败几次、最后什么时候用的」。**

真机验证（真实库，迁移前先整库备份到 `~/.lumii/data/backup-before-v44-<ts>/`）：

| 项 | 迁移前 | 迁移后 |
|---|---|---|
| `schemaVersion` | 43 | **45** |
| `tool_usage_stats` 行数 / 计数合计 / 失败合计 | 125 / 8932 / 423 | **125 / 8932 / 423**（一字不差） |
| `agent_id` 分布 | 无此列 | 125 行全为 `unknown` |
| 中间表 `tool_usage_stats_v43` | — | 已清理 |
| `tool_audit_log` | 12336 行，无 `definition_id` | 12336 行，`definition_id` 列就位、旧行 NULL |
| 新写入（真机发一条消息触发 `web_fetch`） | — | `tool_usage_stats`: `assistant / web_fetch = 1`；`tool_audit_log` 新行 `definition_id='assistant'` |

回读侧（`lumii-ui command tools:usage-by-agent` 实拉）：
```
未归因（V44 之前的存量）（unknown）  共 8932 次 / 125 个工具
    bash 1940 · file_write 991 · file_read 970 · web_fetch 683(失败 167) …
系统默认（assistant）  共 1 次 / 1 个工具
    web_fetch 1
```

### B2.1 ✅ `tool_usage_stats` 加 agent 维度（V44）

原计划写的 SQL 一字未改地用上了。两处与计划的偏离，都是**简化**：

| 计划 | 实际 | 为什么 |
|---|---|---|
| 注入 `getDefinitionId(instanceId)` 反查定义 id | 直接用 `def.id` | `createInstance` 里 `def` 本来就在作用域内。反查多一个「查不到」的失败态，且结果与 `def.id` 必然相同 |
| 「取不到时写 `unknown`」作为兜底 | `agentId` 做成**必填** | 兜底会掩盖接线漏掉；漏传就静默全员 `unknown`，而那正是这张表要修的毛病 |

另外补了一条原计划没写的守卫：**V44 是重建表，重放不会报错，而是静默把 agent 维度压回 `unknown`**——
比报错更糟。故在 `local-database` 的 `isMigrationAlreadyApplied` 里按 `agent_id` 列存在判定。

### B2.2 ✅ `tool_audit_log` 补 `definition_id`（V45）

单独一个迁移版本（没并进 V44）：两条 DDL 各自独立、各自的守卫也独立。
现有 `agent_id` 语义**不改**（它确实记的是实例 id，可能有人拿它对时间线），只是补一个可解的维度。
LLM 调用的审计行（占该表大头，12283 行里的绝大多数）也一并带上 `definition_id`——
这样「哪个 Agent 烧了多少次模型调用」也答得了。

### B2.3 ✅ 失败原因落库（已在 B1.1 一并完成，提交 `42728ba2`）

B1.1 直接做成了通用形态（放在 hook 的统一出口），不是给 `web_search` 开小灶。
真机已验：`web_fetch` 抓不可达站点 → `tool_audit_log` 出现带真实原因的行。

### B2.5 ✅ 结论：时间窗**不该加在统计表上**

B1 的靶子之所以搞错，根因确实是这张表没有时间维度。但**正确的解法不是给它加一维**：

- 时间窗与失败原因用 `tool_audit_log` 回答更合适——它从 B1.1 起由 hook 在所有工具的
  失败出口写入，每行自带 `timestamp` 与原因，本来就是事件级的。
- 往统计表塞 `(agent_id, tool_name, day)` 会同时带来两个新问题：
  ① 行数按天膨胀（8 Agent × 124 工具 × 180 天 ≈ 18 万行）；
  ② `flushToDb` 现在是**整表 UPSERT**，每 2 秒重写几万行会卡——必须先改写成脏键集合。
- 分工定案：**累计次数与「最近用过吗」在这张表；时间窗与失败原因在 audit 表。**

> 尚未做的：UI 上还没有「最近 7 天」这个筛选。数据侧已经具备（audit 表有 timestamp），
> 但既然 B1 的教训是「看数不看窗会误判」，这个筛选值得在 B3 收敛前补上——
> 否则 B3 又会拿累计数字做逐 Agent 决策。

### B2.4 ✅ 回读侧

| 项 | 内容 |
|---|---|
| 新命令 | `tools:usage-by-agent`——与 `tools:list` **分开**：后者有 MCP 面板 / 技能页 / 输入框加号菜单三个调用方，让它们都多背一份按 Agent 明细是白花成本 |
| 处理位置 | Agent 显示名与排序在主进程就做好（内建取定义名，自定义回落 id，**不编名字**），渲染层只管画 |
| UI | 工具页「内建工具」tab 加「全部 / 按 Agent」切换；按 Agent 时选成员，看它用过哪些工具、各几次、失败几次、最后什么时候用的 |

> 已知未验：行的**像素级外观**没能在本机自证（截图读不进模型）。结构与数据经 CLI refs
> 与命令返回值验证；外观留待人工扫一眼。

### B2 验收

- [x] `pnpm typecheck` 干净；相关单测绿
- [x] 库表结构：`tool_usage_stats` 主键为 `(agent_id, tool_name)`；旧数据落在 `unknown`
- [x] 工具页能按 Agent 切换
- [~] 按时间窗筛选：**数据侧具备、UI 未做**（见 B2.5 的说明）
- [x] **B3 的前置达成**：能回答「某个 Agent 用过哪些工具、各几次、失败几次」

---

## B3 · ✅ 已完成（提交 `863fc069`）——**实际 33 → 31，不是 27**

### B3.R 实施结果（2026-09-16）

**一句话：处置表给了 6 条，逐条对着代码与近 7 天实测复核后，只有 2 条站得住。**

这张表是照着**工具名与描述**列的，没读实现、也没查谁在依赖它们。复核方法与结论：

| # | 计划处置 | 复核后的事实 | 判定 |
|---|---|---|---|
| 1 | `list_dir` 并入 `glob` | **不是语义重叠**：`glob` 只回路径、不区分目录；`list_dir` 有 `[DIR]/[FILE]` 前缀 + 工作区/项目目录解析 + 越界与截断处理。两者描述**互相指名对方**（glob 上写着「单层目录列表请用 list_dir」）。近 7 天 `list_dir` 90 次、`glob` 102 次，都在热路径 | ❌ 不做 |
| 2 | 删 `app_goto_and_screenshot` | 同仓库的 screen-tutorial-pipeline 设计**正是靠这批高层工具**把教程任务的工具调用从 40~60 次压到 8~15 次，文档明确要求「goto → screenshot 分两步 ❌ 改用本工具」。删它是把既定优化反向推回去 | ❌ 不做 |
| 3 | `memory_read` 并入 `memory_manage` | 读的是 **MemPalace 归档抽屉**，`memory_manage` 管的是**工作记忆**——两个子系统；而它天然的搭档 `memory_search` 反而保留。合了会让 `memory_manage` 的 description 自相矛盾 | ❌ 不做 |
| 4 | `skill_list` 并入 `skill_search` | 名副其实：原 `skill_list` 的全部实现就是「无过滤条件地列举」 | ✅ **已做** |
| 5 | `app_scroll_to_bottom` 并入 `app_scroll_to_text` | 是**并集**不是降级：加 `to='bottom'` 即覆盖 | ✅ **已做** |
| 6 | `app_fill_form` 并入 `app_act` | **不是合并是删除**：`app_act` 的 `type` **必须给 ref**（来自截图；`label` 只用于 select 的选项文案），而 `fill_form` 能按字段 label 定位、无需 ref。近 7 天 0 次，但它是唯一路径 | ❌ 不做 |

> **第 2 条差点又踩 B1 那个坑**：`app_goto_and_screenshot` 累计 82 次看着像核心工具，
> 近 7 天其实只 5 次——82 是历史存量。是「查了近 7 天」这个动作把它挡下来的，
> 但真正否掉它的是**读了同仓库的设计文档**（那里有量化目标），不是调用次数。

**收敛结果**（运行时展开计，含 `...APP_UI_TOOL_NAMES`）：

| Agent | 前 | 后 | 说明 |
|---|---|---|---|
| system-keeper | 33 | **31** | 少 `skill_list` + `app_scroll_to_bottom` |
| code-dev | 19 | **18** | 少 `skill_list` |
| chronicler | 11 | **10** | 少 `skill_list` |
| info-curator | 15 | **14** | 少 `skill_list` |

**12a §3.2 的「33 → 27」这个数字作废**——它建立在「6 条都成立」的前提上。
按 12a §3.3-decision 自己的话，收敛的价值是「整理语义重叠」不是「进 15–20 安全区」，
所以 31 这个数字本身不构成问题；**但它也不是「达标」**，别记成达标。

### B3.1 ✅ 落地的两条

- **`skill_list` → `skill_search`**：`query` 变为可选，不带即列全部。实现里那句
  「这不是顺手加的分支，原工具的全部实现就是无过滤列举」写在代码注释里，防止以后被当成冗余分支删掉。
- **`app_scroll_to_bottom` → `app_scroll_to_text`**：新增 `to='bottom'` 分支走
  `controller.scrollToBottom`；`text` 随之变可选，两个都没给时返回 usage 错误而不是静默失败。

### B3.2 ✅ 引用清理

按计划列的 7 处逐一处置，实际还多出几处计划没列的：
`tool-names.ts` 常量、`tools/built-in/index.ts` 与 `src/index.ts` 两处导出、
`tooling-section.ts` 的摘要与 `SKILL_TOOLS` 分组、`goal-executor.ts` 自主档白名单（2 处）、
`tool-quality-gate.ts` 的保留名单、`skills-section.ts` + `system-prompt-builder.ts` 的
`hasSkillTools` 判定、`prompt-sections.test.ts` 与 `router/integration.test.ts` 的测试夹具、
`tool-labels.ts`、维护手册、screen-tutorial-pipeline。
系统提示词快照随之更新（diff 仅此一处语义变化，人工核对过）。

### B3.3 ✅ 守卫测试（`packages/agent-runtime/src/agent/builtin/__tests__/tool-budget.test.ts`）

计划把它放在 `apps/windows/src/test/`；实际放在**定义所在的包内**——夹具与断言都在
`definitions.ts` 旁边，改起来不容易漏。

比计划多两条：
- **「预算不是摆设」**：断言 `预算 − 实际 ≤ 2`。只卡上限的话，预算写 99 也永远绿。
- **「已收敛的名字不在任何白名单里」**：把本次删掉的两个名字钉住。

**已验证会红**：把 system-keeper 预算临时压到 28，测试报
「system-keeper 现有 31 个工具，超预算 28。若确实要加，请连同本文件的预算一起改并在提交信息里说明理由。」

### B3 遗留（需拍板）

| 事项 | 证据 | 说明 |
|---|---|---|
| 剩下 4 条要不要做 | 见上表 | 我的判断都是**不做**，但 2/3/6 各有各的取舍（尤其 6：`app_fill_form` 近 7 天 0 次，是「低频」还是「唯一路径」取决于你还要不要用维护代填表单） |
| 还想继续降的话 | — | 真正剩下的候选是**家族合并档**（`wiki_*` / `skill_*` / `memory_*`），但 12a §3.4 记着反例（ToolRet Recall@10 < 52%、API-Bank 59.40% → 38.52%），且我们**没有**「高频工具不延迟加载 + 其余可搜」那套基础设施 |

### B3 验收

- [x] 工具面收敛（**33 → 31**，非 27；原因见上）
- [x] 全仓 grep：被删工具名**无残留引用**（剩下的都是说明性注释）
- [x] 守卫测试通过，且**故意压预算会变红**（已实测）
- [x] 真机：重启后 `tools list` 确认 `skill_list` / `app_scroll_to_bottom` 已下线，
      `skill_search` / `app_scroll_to_text` / `app_goto_and_screenshot` / `app_fill_form` / `list_dir` 均在
- [ ] 真机：维护跑 `asset_checkup` + 记忆体检 + app 截图，确认常用路径仍通（**未做**）
- [ ] 情报 / 记事 / 开发各跑一次常规任务，确认无回归（**未做**）

> 后两条没做：它们要各跑一轮真实 LLM 任务，而本次改动只动了「工具的入口个数」，
> 没有改任何工具的执行体（`skill_search` 的改动有 5 例单测覆盖无 query 路径）。
> 归属到这里是**未验证**，不是「已验证无回归」。

---

## B4 · ✅ 已完成（提交 `d10b3dab`）

### B4.1 现状

cron 执行是全新实例、跑完即销毁（`restoreHistoryForInstance` 只在压缩时调），**会话里累积的记录 Agent 看不到**。
实测 `cron:seed-focus-check` 已堆了 32 条产出，而每轮执行都读不到——每轮都从零开始。

### B4.2 ✅ 做成了什么样

在 `driveAgent` 拼 prompt 时，任务指令**之前**加一段：

```
=== 你上次的情况（供接续参考，不是本轮任务）===
上次执行：09-16 18:02（32 分钟前）
上次产出：顺延的三件事里，sjzh-service 那次故障还没归档。确认下此刻手上的这件，正是现在最该推进的。
（以上是你自己上一轮留下的记录。本轮任务见下方分隔线之后。）
```

| 项 | 计划 | 实际 |
|---|---|---|
| 改哪 | `cron-scheduler.ts` 的 `driveAgent` | 同 |
| 数据 1：上次执行/产出 | 「已有查询路径」 | **不完全是**：已有的 `readLatestAssistantText(…, since)` 回答的是「本轮产生了什么」，自述要的是「上一次留下了什么」。新增 `readPreviousAssistantTurn(convId)` 返回正文+落库时刻 |
| 数据 2：上次发现 | `readLatestMaintenanceReport()`，只在维护类任务时拼 | 同，判定用 `agentId === 'system-keeper'` |
| 拼装函数 | `buildSelfBriefing(jobId, agentId): Promise<string>` | `buildSelfBriefing(input): string`——**同步纯函数**，IO 留在 scheduler 侧。这样单测不用摆 mock |
| 体量 | 小 | 小（4 文件 / 374 行，含测试） |

**几处刻意加的（计划里没写，但不加会出问题）**：

- **首次执行什么都不注入**，不给一个空的「你上次…」块——空块会让模型以为上轮出了问题。
- **抬头点明「不是本轮任务」**：上次产出常带结论与数字，不点明的话模型很容易把上一轮的事再答一遍，或以为任务已经做完。
- **绝对 + 相对时间都给**（`09-16 18:02（32 分钟前）`）：绝对回答「什么时候」，相对回答「隔了多久」，后者更能让模型判断「算不算同一批」。
- **产出压单行、截断 300 字**：它是「上次说到哪」的提示，不是要复述整篇。
- **`NO_REPLY` 哨兵不算产出**（那是「本轮无话可说」），但「上次跑过」本身有信息量，所以时间行照给。

### B4.3 为什么不是「恢复历史」

- 全量历史贵且噪声大；调研的证据是「朴素摘要式巩固会破坏信息」（35.3% vs 98.0%），但那是**替代**场景——这里是**加法**（补一句摘要，不动原文）
- 且 cron 会话的历史会无界增长，恢复它很快会撞上压缩

### B4 验收

- [x] 连跑两次同一 cron 任务，第二次能看到「你上次的情况」段
- [x] 内容来自**第一次的真实产出**，且排在任务指令之前

> 计划写的验证手段是查 `[llm-prompt:full:begin]` 日志。**实际查不到**——那个转储只含系统提示词，
> 不含用户消息，所以注入段不在里面。改用两条更硬的证据：
> ① e2e 用例走完整 `runLocalCronJob` 生产路径，直接断言第二次收到的 message 内容与顺序；
> ② 真机连跑两次 `seed-focus-check`，日志长度 `132 字` → `136 字`，跟着上一轮产出（51 字）变。
>
> 顺带验到一个 e2e 测不到的风险：生产会话的 `content_json` 是 `assistant_parts` 形状，
> 而测试夹具写的是 `{type:'text'}`。已用真实库确认 `extractTextFromContentJson` 能从
> parts 里提出正文（实测 156 字），否则这个功能在生产里会静默拿到空串。

---

## B5 · 「少推 X」四件套

现状只有第 ① 件（`news_preference` 的四个字段）。补 ②③④。

### B5.1 预览命中集（第 ② 件）

| 项 | 内容 |
|---|---|
| 做什么 | 偏好卡上每条规则一个「看命中」：拿它去匹配**历史已推条目**，列出命中（含已读过的），命中处高亮 |
| 数据 | `dashboard_feed_items`（片 11 已有期与条目） |
| 新 IPC | `news-preference:preview` → `{ field, value, matched: [{title, source, batchLabel}] }` |
| 匹配口径 | 与运行时一致（子串 / 分词），**同口径**是关键——预览与实际筛选不是一套就等于没预览 |
| 边界（要如实告知） | 预览**只对确定值开放**；自然语言规则不提供预览（NewsBlur 自己也这么写） |
| 体量 | 中 |
| 验收 | 写「少推：融资」，预览列出历史上含「融资」的 N 条；改一个字预览随之变化 |

### B5.2 显式冲突优先级（第 ③ 件）

| 项 | 内容 |
|---|---|
| 问题 | 「关注 AI」与「少推 AI 融资稿」同时命中时谁赢，**现在没有定义**——这正是「说了少推 X 还是推过来」的成因 |
| 做什么 | 定一条链并存进偏好：建议 `明确少推 > 明确关注 > 来源偏好 > 默认排序`；把这条链**显示在偏好编辑处**（不是只写在文档里） |
| 改哪 | `apps/windows/src/main/news-preferences.ts`（加 `priority` 概念）+ 渲染层显示 |
| 体量 | 小 |
| 验收 | 构造同时命中两边的条目，验证按链裁决；界面上能看到裁决顺序 |

### B5.3 规则日志（第 ④ 件）

| 项 | 内容 |
|---|---|
| 做什么 | 每条规则记「命中过多少篇、最近 10 次命中的是什么」 |
| 存哪 | 新表 `news_preference_hits(rule_field, rule_value, item_id, hit_at)` 或复用计数表 |
| 写入点 | 资讯筛选时（情报运行时）落一条 |
| 体量 | 小 |
| 验收 | 跑一轮后日志里能看到每条规则的命中数 |

### B5 验收

- [ ] 四件套齐备：规则对象化 ✅ / 预览 ✅ / 优先级 ✅ / 日志 ✅
- [ ] 端到端：说「少推融资稿」→ 预览看到会命中哪几条 → 下一轮产出确实少了融资稿 → 日志记录了命中

---

## B6 · 维护的「从发现到动作」

### B6.1 地基（已有）

`maintenance_reports.findings` 带 `key` / `severity` / `title` / `evidence` / `suggestion`，跨期 key 差分已在做。**缺的是「从发现到动作」。**

### B6.2 四条不变式（逐条落到实现）

| # | 不变式 | 实现 |
|---|---|---|
| ① | 预览**零副作用** | 生成变更预览时不写任何东西；预览只是一段结构化描述 |
| ② | 执行的是**被审过的那一份** | 新表 `maintenance_actions(id, report_id, finding_key, kind, payload, preview, status, created_at, executed_at, result)`；确认后执行 `payload`，**不重新生成** |
| ③ | 结果写回**同一条** | `maintenance_actions.result` + 回指 `report_id`；概览页卡片上该条 finding 显示「已处理 / 已跳过」 |
| ④ | 可回滚或至少可追溯 | 记忆改动已有 `.bak`；Wiki 改动要有快照（B6.4） |

### B6.3 动作分级

| 级 | 动作 | 交互 |
|---|---|---|
| L1 | **只加标记**（标记重复记忆 / 标记过期页） | 可直接做，不改内容 |
| L2 | **合并 / 归档** | 出预览 → 确认 → 执行；**合并要改写所有引用**（只删不转会留断链） |
| L3 | **清除** | 前置预告期 + 可恢复窗口 |

### B6.4 交互细节（都有实证来源）

| 细节 | 来源 | 实现 |
|---|---|---|
| **跳过不丢** | Guru | `status='skipped'` 的记录留在队列，下次仍出现 |
| **冷却期 7 天** | Guru | 同一 `finding_key` 7 天内不重复提醒；否则用户会整体静音，**比不提醒更糟** |
| **倒计时比「已过期」更驱动** | Slab | 卡片上显示「还有 3 天到期」而不是「已过期」 |
| 合并要改写引用 | Obsidian Note Composer | 合并两条记忆时，指向被合并条目的引用一并改 |

### B6.5 分批

| 子批 | 内容 |
|---|---|
| B6-1 | `maintenance_actions` 表 + 从 finding 生成预览（只读，不执行） |
| B6-2 | 概览页卡片：finding 上挂「处理 / 跳过」，展示预览 |
| B6-3 | 执行 L1（只加标记）+ 结果写回 + 冷却期 |
| B6-4 | 执行 L2（合并 / 归档，含引用改写与快照）+ 可回滚 |

### B6 验收

- [ ] 构造一条重复记忆 → 体检发现 → 卡片上出「处理」→ 预览显示会删哪条留哪条 → 确认后执行 → **结果写回同一条报告**
- [ ] 跳过的不消失；同一问题 7 天内不重复提醒
- [ ] 合并后**没有断链**（指向被合并条目的引用已改写）
- [ ] 回滚：改动可从 `.bak` / 快照恢复

---

## B7 · 事件级去重（轻量版）+ 来源质量

### B7.1 事件级去重

**结构**（抄 GDELT 的 Events/Mentions 分裂）：

```sql
-- V45
CREATE TABLE news_events (
  id              TEXT PRIMARY KEY,   -- 稳定 id
  feed_id         TEXT NOT NULL,
  canonical_title TEXT NOT NULL,
  first_seen      TEXT NOT NULL,
  last_seen       TEXT NOT NULL,
  source_count    INTEGER NOT NULL DEFAULT 1,
  created_at      TEXT NOT NULL
);
ALTER TABLE dashboard_feed_items ADD COLUMN event_id TEXT;
```

| 项 | 内容 |
|---|---|
| 聚类（轻量版） | 标题 n-gram 相似度 + 时间窗，**不上 embedding** |
| 参数 | 相似度阈值 **0.7**；文章与事件时间差 **≤18h**；事件存活 **≤36h** |
| 参数来源 | 线上验证值（$100/月、180+ 源的公开实践） |
| 落点 | `dashboard_feed_write` 写入时先做事件匹配，再落 `event_id` |
| 渲染 | **一件事一行**，来源折叠（「36氪、澎湃、界面 等 4 家」），点开看各家标题 |
| 已知失效 | 带强烈本地语境的地区新闻会漏并；**宁可漏并也不降阈值换误并**（原作者的取舍，照抄） |

### B7.2 来源质量（不自建）

| 做 | 不做 |
|---|---|
| 一份**人工小名单**（常看的 20–30 个源，手工标三档） | ❌ 用 LLM 打分（与人类专家 ρ=0.50，且指派角色会稳定偏斜） |
| 用评分做**降权**与**背景提示** | ❌ 把评分当「这篇可信」的判据（评分是**按出版物整体**的） |
| 记「这个源最近 10 条被推了几条、被滤了几条」 | ❌ 自建九维度评分流程（NewsGuard 那种） |

### B7 验收

- [ ] 构造同一事件的 3 条不同来源稿件 → 落成 1 个 event + 3 条 item → 卡片渲染成一行
- [ ] 地区新闻的漏并率抽样看一次（接受漏并，但不接受误并）
- [ ] 来源名单生效：被降权的源在同等条件下排后

---

## 二、回归与门禁（每批都要过）

1. `pnpm typecheck` 干净
2. `pnpm --filter ./apps/windows test:all` 全绿（当前基线 **260 文件 / 2268 passed / 51 skipped / 0 failed**）
3. `packages/agent-runtime` 全量绿（当前 **193 文件 / 1969 例**）
4. 真机冒烟：改主进程后 `pnpm dev:restart`（注意**另一会话可能同时在跑 dev**，用 `stop-dev.ps1 -KillAllElectron` 清干净再起）
5. CK 套件（`docs/test/lumii-cli/agent-curation/`）每批收尾跑一次，新增用例并入
6. **提交卫生**：这个仓库有并行会话，**按文件显式 `git add`，不要 `git add -A`**

---

## 三、每批的提交信息要求

沿用本仓惯例：中文 conventional commit + 结构化正文，含
- **起因**（带数据或 file:line）
- **改动**（做了什么、为什么这么做）
- **验证**（跑了什么、结果如何；如实写失败的与跳过的）
- 不含任何署名尾注

---

## 四、途中需要确认的点

| 时点 | 要确认什么 |
|---|---|
| ~~B1.2 归因出结果后~~ | ✅ **已触发并对齐**（2026-09-16）：失败主因确实是网络出口而非工具设计，且原靶子整个是历史存量。用户拍板改靶 `web_fetch` + 收尾 B1.4。这条确认点值回票价——没有它，B1 会在一个不存在的 43% 上白花力气 |
| ~~重试策略~~ | ✅ **已定并实施**（`61525a26`）：改前先做实验——对同一被拦站点连续 4 次（含间隔 45 秒的第 4 次）结果完全一致，故连接层失败不重试 |
| **代理决策** | ⏸ **仍未定，两处已有新证据**：① 这些站点不是「不可达」而是被拦在 TLS（TCP 握手成功）；② 一次失败现在降到 ~10.7 秒。要定的是「全站走还是白名单」「从设置页读还是环境变量」 |
| ~~B2 执行前~~ | ✅ 已定并实施：失败原因走 `tool_audit_log` 单一权威源；时间窗也归它，**不往统计表加维度**（见 B2.5） |
| **B3 执行前（新增一条）** | 是否先补「最近 7 天」筛选再收敛。B1 的教训就是「看累计数会误判」——B3 若仍拿累计数字做逐 Agent 决策，是同一个坑的第二次 |
| B3 执行前 | `list_dir` 是「保留注册但从白名单移除」还是「一并下线」——取决于还有谁引用它 |
| B6-3 前 | L1「只加标记」具体标记什么（记忆没有标记字段，可能要加列） |
| B7.1 前 | 是否接受「地区新闻会漏并」这个已知代价 |

---

## 五、体量总估

| 批次 | 新增/修改文件（估） | 实际 | 说明 |
|---|---|---|---|
| B1 | 2–5 | **8** | 4 笔提交：审计 hook（1 改 + 1 新测）、web_fetch 失败信息（2 改 + 2 新测）、用法固化（2 改）、快照修复（1） |
| B2 | 6–8 | **21** | 1 笔提交：2 个迁移 + store 重写 + hook 接线 + 命令/处理器/UI + 4 个测试文件。超出估算是因为回读侧（命令契约 + IPC + 新组件 + CSS）比预想的大 |
| B3 | 10–15 | — | 主要是引用清理，机械但面广 |
| B4 | 2–3 | **4** | 1 笔提交：纯拼装函数 + 接线 + 各自测试（e2e 里还加了 3 例跨轮次用例） |
| B5 | 6–10 | — | 2 IPC + 1 表 + UI |
| B6 | 8–12 | — | 1 表 + IPC + UI + 执行器 |
| B7 | 6–10 | — | 1 迁移 + 聚类 + 渲染 |

**合计约 40–60 个文件**（含测试与文档），跨 7 个独立提交批次。
B1 实际 8 文件 / 4 笔（含重定靶）、B2 实际 21 文件 / 1 笔、外加 B1 遗留 2 笔（4 文件）。
