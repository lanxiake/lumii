# 10 · 渠道会话路由重构（接续逻辑梳理）

> 创建：2026-09-15
> 触发：用户反馈「自动接续现在的逻辑有些混乱，渠道会话接续逻辑需要重新梳理」。
> 前置：[渠道在场设计 §5.4 跨渠道连续性](../../design/2026-09-09-user-presence-channel-design.md)（L231-265）、`5a1ee617`（接续初版）、`e7b638f5`（2026-09-14 补丁）。
> 原则：**先把语义理清、再动代码**；每片独立可回滚。
> 状态：**S1 代码完成**（开关搬到实验功能）；S2-S6 待拍板。

---

## 一、用户诉求

1. **开关归位**：「自动接续会话」开关放到 **设置 → 实验功能**；
2. **逻辑重梳**：给出重构方案，使渠道会话接续更合理、稳定、好用。

---

## 二、现状事实（2026-09-15 工作区核实，全部带行号）

### 2.1 硬实证：`sessionKey` 前缀**不代表**此刻服务它的渠道

同一会话键 `qbot:964A476991442C6FA18DC32EF18AFEFC`，同一天：

| 时间 | 日志 | 谁在说话 |
|---|---|---|
| 09:42 | `[WeixinChannelAdapter] [handleMessage] sessionKey=qbot:964A…` | 微信 |
| 10:00 / 10:02 | `[QbotChannelAdapter] [handleMessage] sessionKey=qbot:964A…` | QQ |

用户可以续聊任何会话，于是「会话键的来处」与「这轮是谁在说话」分离。**用前缀推断渠道的代码，此刻必然推断错。**

### 2.2 一次渠道消息的会话键，来自四层优先级

```
buildSession(msg) → getActiveSessionKey(channelUserId)
   ① 内存 Map  activeSession                      进程内，重启即丢
   ② ChannelSessionStore.getActive                runtime_state（落库）
   ③ [仅微信] WeixinSessionBindingManager          runtime_state（落库，/link 专用）
   ④ 兜底 `{channelType}:{channelUserId}`
```

- 四层只在 **weixin** 成立（`weixin-channel-adapter.ts:495-511`）；qbot/feishu/wecom 是三层（`qbot:174-181`、`feishu:211-218`、`wecom:199-209`）；
- 四层里有**两套持久化**都在回答同一个问题（「这个用户该路由到哪个会话」）：`weixin:binding:{uid}`（`weixin-session-binding.ts:18`）与 `channel:active:weixin:{uid}`（`channel-session-store.ts:30`）。`/link` 同时写两者（`link.ts:38,41`），接续只写后者（`cross-channel-continuity.ts:336`）——**「接续不写 binding」只靠注释约束**，无类型或测试保证。

### 2.3 四个适配器：同一功能，四份拷贝

| 项 | weixin | qbot | feishu | wecom |
|---|---|---|---|---|
| `getActiveSessionKey` 层数 | **4**（含 /link） | 3 | 3 | 3 |
| `setActiveSessionKey` 日志 | 有 | 无 | 无 | 无 |
| `resetToChannelSession` 解绑 /link | **有**（`:526-536`） | 无 | 无 | 无 |
| `/new` 实现 | 共享 `new.ts`（**硬编码 `weixin:` 前缀**，`:9`） | 本地副本 | 本地副本 | 本地副本 |
| ACP / `/project` `/claude` 等 | 有 | **无** | 有 | **无** |
| `ChannelSessionStore` 实例 | 各自 `new` 一份（**非单例**） | 同 | 同 | 同 |

`qbot:174-197` / `feishu:211-234` / `wecom:202-225` 是三段近乎逐字的重复。`wecom:199-202` 还留着**重复的 JSDoc 块**（复制粘贴物证）。

### 2.4 接续状态机：把「询问」与「扣住消息」焊在一起

`cross-channel-continuity.ts` 一个类同时管：候选判定、询问推送、改写路由、持久化绑定、**扣住当前消息**、60 秒超时（默认接续）、答复后**重放**、以及「问过没问过」。

关键机制：
- 扣住的消息只能靠 `replay: () => void this.handleMessage(msg)` 重放（四渠道同款，`weixin:307` 等），**重放不走 `userQueues`**；
- 所有作废路径都要想清楚 `replayHeld` 传什么（`abandon` `:295`、`tryConsumeReply` `:262`、`clear` `:273`、`abandonWithReplay` `:283`）；
- 「只问一次」用 `asked: Set<sessionKey>`（`:188`），键是**路由**而非**用户**，所以 `resolve()` 里手工补两处 `asked.add`（`:338`、`:356`）防重放时再问。

### 2.5 一条入站文本有 6 个潜在消费者

顺序即代码顺序（`channel-interaction-hub.ts:193-227` 的插队路径 + 各 adapter 的 `handleMessage`）：

| # | 消费者 | 接受词 | 何时 |
|---|---|---|---|
| 1 | 斜杠命令（先作废挂起项） | `/…` | 最先——否则命令会被当成「答复」吞掉 |
| 2 | `ChannelInteractionHub`（审批 / 提问） | `1` `2` `3`… | 非斜杠且该会话有挂起项 |
| 3 | `CrossChannelContinuity`（接续） | `1`/`y`/`是`/`接续`；`0`/`n`/`否`… | 非斜杠且该会话有挂起项 |
| 4 | `/stop` `/abort` | 命令词 | 插队路径末尾 |
| 5 | `tryConsumeHandoffConfirm`（转交，**已架空**） | `1`/`确认`/`ok`… | `handleMessage` 内、普通消息之前 |
| 6 | 普通消息 | — | — |

「1」既是审批词也是接续词；挂起项同时存在时归属由顺序决定，**用户无法表达自己在回答哪一个**。

### 2.6 命令面（渠道侧）

`slash-commands/` 12 个文件：`new` `resume` `back` `link` `/unlink` `clear` `compact` `stop` `project` `backend` `switch-backend`（`/claude` `/codex`… `/lumii`）`help`。注册矩阵：weixin 15 项、feishu 同集；**qbot / wecom 没有 ACP 相关命令**（无 `/project` `/backend` `/claude` `/codex` `/opencode` `/cursor` `/lumii`）。

`/back` 的实现是 `adapter.resetToChannelSession`——**四渠道语义不同**，`back.ts:16-32` 对此无区分。

---

## 三、混乱点清单（按主题，编号供拍板与排期引用）

### A. 渠道身份：靠前缀猜

| # | 问题 | 证据 | 后果 |
|---|---|---|---|
| **A1** | 「sessionKey → 渠道」有 **5 套实现**，命名还不一致 | `channelOfSessionKey`（`cross-channel-continuity.ts:52-76`）、`channelLabelOf`（`channel/types.ts:131-140`）、`resolveConversationChannel`（`conversation-commands.ts:648-661`，用 `'wechat'` 而非 `'weixin'`）、`resolveChannelFromSessionKey`（`bridge-tool-registrar-cron.ts:34-37`）、`isChannelSession`（`handoff-store.ts:70-73`，**生产代码已无调用方**） | 新增渠道要改四处；渠道偏好（`scene-resolver.ts:59-66`）、侧栏标签（`session-list-rows.ts:54`）、`/resume` 分组全部按错渠道呈现 |
| **A2** | 前缀 = channelType 是隐式约定，默认键在 **8 处**硬编码 | `weixin:510,531`、`qbot:179,192,61`、`feishu:216,229,65`、`wecom:207,220,50`；`isOwnChannelSessionKey`（`channel-session-store.ts:56-63`）依赖该约定 | 任何别名（如 `qq:`）静默失配；`/new` 的通用实现里写死 `weixin:` |

### B. 会话路由：三份状态、两套存储

| # | 问题 | 证据 | 后果 |
|---|---|---|---|
| **B1** | 两套持久化串成优先级链（`/link` 绑定 vs 接续路由） | 见 §2.2 | 「接续不污染 /link」只靠注释；顺序变了行为就变 |
| **B2** | 内存 Map 与 store cache 是两份状态，无反向刷新；**四个 adapter 各 new 一份 store** | `weixin:82` / `qbot:84` / `feishu:99` / `wecom:76`；store cache 只在构造时 `load()` | 一处的写入另一处看不到；进程内多写点不同步 |
| **B3** | `ChannelSessionStore.clear()` 是**死代码** | `:137-140` 注释称「/unlink 解绑时调用」，生产零调用 | 读者被误导；`/unlink` 实际不清路由记录 |
| **B4** | `/back` 四渠道语义不同（微信会解绑 `/link`，其余不会） | `weixin:526-536` vs `qbot:190-197` 等 | 同一命令不同副作用，用户无法预期 |
| **B5** | `/unlink`、`/back`、接续拒绝三条路径对 `/link` 处理各异 | `link.ts:62`（强制解绑）、`weixin:528-530`（条件解绑）、`new.ts:20`/`resume.ts:155`（完全不动） | 「切走」的副作用取决于是哪条路进来的 |
| **B6** | 会话存在性**只在载入时**校验 | `conversationExists` 仅 `load()` 用（`:158-190`）；`setActive`/`getOwn` 不校验 | 指向已删会话的路由在运行期仍会把空会话重新建出来——正是该 store 想根治的问题 |
| **B7** | `dev-context`（项目）与 backend 选择是 **per-sessionKey**，切路由后不迁移 | `coding-dev-dev-context.ts:59-61`（`${channelUserId}:${sessionKey}`）、`switch-backend.ts:41` | 接续/`/resume` 之后「项目与编码工具跟着丢」，且客户端用 `local-user:` 键、渠道用渠道键，两边不通 |

### C. 接续状态机自身

| # | 问题 | 证据 | 后果 |
|---|---|---|---|
| **C1** | `maybeAsk` 的 `bind` 参数是**死参数**（生产四处调用都没传） | `cross-channel-continuity.ts:202-208` + 长注释；`resolve()` 的 `bind?.()`（`:335`）恒空操作 | 读代码的人以为接续会写 /link 绑定 |
| **C2** | `clear()` 的文档与调用事实不符；`/stop` 实际会**补投**被扣消息 | `:270-275` 注释 vs `channel-interaction-hub.ts:245`；且 `/stop` 先走 `abandonWithReplay`（`:226-229`） | 「打断后旧消息又冒出来」 |
| **C3** | 重放**不走 `userQueues`**，与紧随的斜杠命令并发 | `weixin:182-187`（入队）vs `:307`（重放直调 `handleMessage`） | 与 `/clear` 竞态：清空后旧消息又落库 |
| **C4** | `asked` **只增不减**，且键是路由不是用户 | `:188, :238`（仅 `clear()` 会 delete，而 `clear()` 只在 `/stop` 路径） | 无界增长；`/clear` 后想被重新询问只能发 `/stop` |
| **C5** | 前置守卫判「当前会话不属于本渠道」→ **不问**，且没有人收尾 | `:107`；`resetToChannelSession` 只在「不接续」分支调用（`:353`） | 用户被长期留在借来的键上（§2.1 的真实场景就是这个），桌面侧栏、渠道偏好、cron 推送全部按错的渠道走 |

### D. 入口与协议

| # | 问题 | 证据 | 后果 |
|---|---|---|---|
| **D1** | 裸「1」多义（审批 / 接续 / 转交） | §2.5 | 用户无法表达回答对象；成败取决于代码顺序 |
| **D2** | `tryHandleChannelOutOfBand` **零测试** | 四个 adapter 的插队路径；`channel-interaction-hub.test.ts` 只覆盖 `pushText` | 该函数是「答复归属」的唯一裁决点，改动无护栏 |
| **D3** | 同一张「置顶优先」列表被四套魔数各自重排 | `OWN_LOOKUP_LIMIT=200`、`CANDIDATE_SCAN_LIMIT=50`、`resume LOOKUP_LIMIT=100`、`SESSION_SCAN_LIMIT=200` | 到处 `sort by updatedAt`；谁取多少没有统一口径 |

### E. 代码卫生（随手可清）

- **E1** 命名遮蔽：四个 adapter 的 `handleMessage` 里都有 `const activeSession = {...}`，与类字段 `activeSession: Map` 同名（`weixin:397`/`qbot:305`/`feishu:369`/`wecom:323`）；
- **E2** `wecom:199-202` 重复 JSDoc；
- **E3** `ChannelFeatureSettings` 有 **4 份定义**（main 权威、preload 导出但无人 import、renderer 自留 DEFAULTS、preload/index.ts 内联字面量）；
- **E4** qbot/wecom 的 ACP 能力缺口，`project.ts:6` 的注释却按「本会话」口径写，未说明只对两个渠道生效。

---

## 四、目标模型：三层分离

一句话：**身份归身份、路由归路由、交互归交互**，各有单一真相源。

### 4.1 身份层：渠道归属写进数据

```
conversations 增 channel_type（+ channel_user_id），创建时写入，存量按前缀 + /link 回填
channel/channel-identity.ts：唯一一份 CHANNEL_PREFIXES / NON_USER_PREFIXES / labelOf()
```

- 五个消费点（A1 的五套实现）改读数据；前缀函数降级为迁移期兼容；
- 顺带满足 07 计划的守卫 c（`onboarding:` 必须进非用户前缀，`07-新手指引.md:201`）。

### 4.2 路由层：一个 `ChannelRoute`

```ts
interface ChannelRoute {
  own: string          // 本渠道本人的会话（显式，不再靠「约定 + 兜底」）
  active: string       // 当前生效（可能借自别处）
  borrowedFrom?: 'link' | 'continuity'
}
```

- 读写集中一处（`ChannelSessionStore` 已有雏形，改成**单例**），adapter 只提供「own 怎么算」；
- `/new` `/resume` `/back` `/link` `/unlink` 全部改成对该结构的操作，**一张表说清每种切换对 `borrowedFrom` 的副作用**（消 B4/B5）；
- 运行期写入前校验目标会话存在（消 B6）；
- 明确 `dev-context`/backend 是否随路由迁移（B7）——**建议：随 active 走**，即把 dev-context 的键从 `sessionKey` 改为「路由解析后的会话」。

### 4.3 交互层：接续不再扣消息

**方案 A（推荐）**：提示式
- 检测到候选 → 发提示 + **当前消息照常处理**（留在本渠道会话里回答）；
- 用户回 `1` / `/resume <n>` → **后续消息**走目标会话；
- **无超时、默认保持现状**（安全默认值）；`asked` 改为按 `(渠道, 用户)` 记录，且可被 `/clear`、`/new` 重置。

**方案 B（备选）**：维持询问式，但把扣留/重放补全
- 保留扣消息 + 重放；补齐 C2/C3 的路径与测试，重放走 `userQueues`，`clear` 语义与调用点对齐。

| | A | B |
|---|---|---|
| 当前消息 | 本会话回答 | 带到目标会话回答 |
| 复杂度 | 删掉 pending/replay/timer 三件事 | 保留并补全 |
| 风险 | 用户可能觉得「答非所问」 | 重入 `handleMessage` 的老问题继续存在 |
| 默认值 | 不变（安全） | 1 分钟默认接续（会「自己动」） |

### 4.4 入口层：一个分发器 + 消歧

`ChannelInboundRouter`：挂起交互 → 挂起接续 → 斜杠命令 → 普通消息，**规则集中、有测试**（补 D2）；挂起项**互斥**（进入挂起态时拒绝叠加），文案不再共用裸数字（消 D1）。

---

## 五、切片计划

| 片 | 范围 | 体量 | 风险 | 依赖 |
|---|---|---|---|---|
| **S1 · 开关归位** ✅ | 开关搬到 设置 → 实验功能（用户诉求 1） | renderer 3 文件 | 低 | 无 |
| **S2 · 身份显式化** | `channel_type` 落库 + 回填 + 5 个消费点 + 前缀清单集中（A1/A2） | 中 | 中（迁移） | 无 |
| **S3 · 路由单一真相源** | `ChannelRoute` 收口（B1-B7）；`/back` 四渠道语义统一 | 中 | 中 | S2 |
| **S4 · 接续瘦身** | 按 §4.3 拍板结果实施（C1-C5） | 中 | 中 | S3 |
| **S5 · 入口统一** | `ChannelInboundRouter` + 「1」消歧 + 补测试（D1-D3） | 中 | 低-中 | S4 |
| **S6 · 卫生** | E1-E4 随手清 | 小 | 低 | 可随时 |

**S1 独立可合；S2 是后面三片的地基。**

---

## 六、待拍板

1. **接续交互**：§4.3 方案 A（提示式、默认不动）还是方案 B（询问式、默认接续）？
2. **渠道身份落库**：新增 `conversations.channel_type` 列（干净、要迁移）还是复用 `session_config` JSON（零迁移、语义弱）？
3. **`/link` 与接续**：合并成一套路由（`borrowedFrom` 区分来源），还是维持两套存储？
4. **B7 的取向**：切路由时 `dev-context`（项目）与编码工具后端是否跟随迁移？（建议跟随）

---

## 七、不做

- 不改桌面会话列表的视觉分组（数据正确后自然变对）；
- 不引入渠道级「在场路由」（设计 §5.3 已决策保持现状）；
- 不做接续的「意图自动识别」（设计 §8：显式触发优先）；
- 不为 qbot/wecom 补 ACP 命令面（E4 只补注释说明，能力对齐另立项）。

---

## 八、实施记录

### S1 · 开关搬到「设置 → 实验功能」（2026-09-15 代码完成）

**改动（纯渲染层，主进程与 IPC 零改动）**

- `ChannelsSection/useChannelFeatures.ts` → `ExperimentalSection/useChannelFeatures.ts`（`git mv`；渠道分区不再使用）；
- `ExperimentalSection/index.tsx`：`ExperimentalView` 加 `'channelContinuity'`；新增 `ChannelContinuityDetail`（`DetailHeader` + `setting-row` + `ui/Switch`）；列表新增一行「跨渠道会话接续」，summary 显示 已启用 / 已关闭；
- `ChannelsSection/index.tsx`：删掉旧的 `.experimental` 开关块与随之无用的 import；
- `ChannelsSection.module.css`：删掉 `.experimental*` 五条规则（留一行注释指向新位置）。

**为什么保留主进程 JSON 存储**：渠道 adapter 每条消息现读 `getChannelFeatures()`（四个 adapter 的 `continuity()`），与渲染窗口存活无关。改成 localStorage 就得再复制一份 `executeJavaScript` 兜底（`main/index.ts:1043-1062`），是退步。**搬 UI，不搬存储。**

**验证**

- `ExperimentalSection.test.tsx` +3 例（列表状态 / 进入详情开启并写主进程 / 读取失败回落「已关闭」不阻塞其余项）；`ChannelsSection.test.tsx` 3 例回归；定向 11 例绿；4 包 typecheck 干净。

**待人工验收（重启应用）**

- [ ] 设置 → 实验功能 → 「跨渠道会话接续」可开关，渠道设置里不再有此开关
- [ ] 切换后下一轮渠道消息即按新值生效（无需重启）
