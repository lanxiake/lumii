# H3 精灵图动作组与桌宠行为补全实施记录

**状态**：实施中 —— 14 个动作组已全部装进客户端、冷启动验证通过；续跑要点见 §五、未完成见 §六。

设计见 [`../../design/客户端UI/2026-09-21-桌宠动作生成方案调研.md`](../../design/客户端UI/2026-09-21-桌宠动作生成方案调研.md)（第五节「六个可直接借鉴的技术」里的 H3 视频模型路线）。
渲染侧契约见 [`../../design/客户端UI/2026-09-20-虚拟人精灵图渲染后端设计.md`](../../design/客户端UI/2026-09-20-虚拟人精灵图渲染后端设计.md)。

> **方法学正本不在本文档，在技能里**：[`apps/windows/bundled-skills/设计与可视化/pet-sprite-h3/SKILL.md`](../../../apps/windows/bundled-skills/设计与可视化/pet-sprite-h3/SKILL.md)。
> 七条硬规则、画布算法、命令速查都在那边；本文只记**这一批交付了什么、状态如何、换机器要带走什么**。

## 一、起因

用户报「宠物还不会自己走动和爬墙，我没看到这些动作」。

查下来**不是功能坏了，是素材缺组**：客户端 `PetOrchestrator.resolveAmbientGroup` 找不到动作组时
**静默回落到基础待机**——日志里每一次 `[setAmbientActivity] → 组 "(基础待机)"` 就是一次缺失，
表现为「一边平移一边播呼吸」，看起来像在滑行。

仓库里其实有**两套「九个动作」规范**（交互那套 `Idle/Talk/Jump/Wave/Nod/Shake/Picked/Land/PlayBall`、
行为那套 `Idle/Walk/Sit/Talk/Fall/Picked/Jump/Climb/Crawl`），团子当时只有前者的一部分。
两套都要装。

另外用户明确要求：**走 / 攀爬 / 爬行要侧身**（"走路的图片记得是左右侧身哦，不是正面的"），
其余保持正面。坐下按参考素材定，见 §六。

## 二、方法：两段式

H3 的 `first_frame` 是**机位标定**，且循环闭合靠 FL2VA（同一张图同时接 `first_frame` 和 `last_frame`）。
于是「用正面立绘直接生成侧身循环」会在末帧被**强制拉回正面**，循环里出现"转过去→被拉回来"的鬼畜。

所以非正面姿势走两段：

```
正面立绘 ──pose(I2VA，尾帧自由)──▶ 姿势拼条 ──pose-pick──▶ 挑一帧
   └─▶ stage-frame --keep-scale ──▶ staged/<角色>-<姿势>.png
        └─▶ sheet(FL2VA，首帧=该姿势) ──▶ 8 格动作表 ──▶ install-pet ──▶ 客户端
```

⚠ 两条路的 `freeEnd` **别接反**：循环必须接 `last_frame`，姿势图必须**不接**。
接反任一条都**静默**毁掉整轮，而出图看着完全正常。

## 三、代码落在哪

| 位置 | 是什么 |
| --- | --- |
| `apps/windows/bundled-skills/设计与可视化/pet-sprite-h3/` | **技能**（`SKILL.md` + `run.ts` + `characters/*.mjs` + `lib/cutout.mjs`） |
| `verify/pet-sprite/characters/` | 同一批脚本的仓库副本（技能内那份是**逐字节相同的拷贝**，只有 `install-pet.mjs` 例外——技能版带 `findRepoRoot`） |
| `verify/pet-sprite/refs/pet-sprite-h3-*.api.json` | 三条 ComfyUI 工作流的 API 格式导出件 |
| ComfyUI 用户库（**服务端**） | 同名图可在网页界面直接打开，随服务器走，不随机器走 |

⚠ `run.ts` 那一层存在的唯一理由是**定位仓库根**：工具脚本要 `import sharp`，而 ESM 的裸模块从
脚本自身位置向上找 `node_modules`，技能被播种到 `~/.lumii/workspace/skills/` 之后那条路径上
**没有 sharp**（实测 `MODULE_NOT_FOUND`）。所以跑的一定是仓库里那份。

## 四、当前实机状态（2026-09-22）

```
demo_cartoon_cat（团子）  14 组 / 104 帧 / 图集 4480×5824 / 画布 560×448 / scale 0.2437
Idle Talk Wave Jump Nod Shake Picked Land PlayBall      ← 交互那套
Walk Fall Climb Crawl Sit                               ← 行为那套（Walk/Fall/Climb/Crawl/Sit 是 loop）
```

冷启动 + 进入宠物模式后的实机判据：

```
[SpritePetRenderer] [loadTextures] 图集条目 104 个
[PetCanvas] [loadModel] 模型就绪 demo_cartoon_cat
[PetOrchestrator] [setAmbientActivity] walk  → 组 "Walk"     ✓
[PetOrchestrator] [setAmbientActivity] sit   → 组 "Sit"      ✓
[PetOrchestrator] [setAmbientActivity] climb → 组 "Climb"    ✓（补 Sit 之前那一轮验的）
[PetOrchestrator] [setAmbientActivity] stand → 组 "(基础待机)" ✓（stand 本来就是待机）
```

**判据就是这条**：日志里打 `→ 组 "(基础待机)"` 而动作不是 stand/sit 之外的基础姿态，就是缺组。

已装的图集逐帧量过包围盒：**没有一帧贴到格边**（横向最紧 crawl 余 17px；纵向 4–6px 是设计值——
anchor 在 `ch − 6`）。

## 五、⚠ 换机器要继续，必须带走的仓库外产物

**这是本文档最要紧的一节。** 素材、成品、注册表**都不在仓库里**，只在这台机器的 `~/.lumii/` 下。

| 路径 | 大小 | 要不要带 | 说明 |
| --- | --- | --- | --- |
| `~/.lumii/workspace/outputs/pet-motion/staged/` | 1.7M | **必带** | 各动作的首帧（`<角色>.png` + 同名 json）。**没有它就装不了**，重摆要重跑姿势图 |
| `~/.lumii/workspace/outputs/pet-motion/<角色>-<动作>-sheet/` | 24M（含 staged） | **必带** | 13 张动作表。`install-pet` 按 `<角色>-<动作>-sheet/` 这个约定找 |
| `~/.lumii/pet-models/demo_cartoon_cat/` | 16M | 建议 | 装好的成品（atlas + manifest + atlas.json + pet.json）。带了就不用重装 |
| `~/.lumii/pet-models/registry.json` | <1K | **必带** | ⚠ 每进程**只读一次**，且**同 id 用户侧优先**于内置模型 |
| `~/.lumii/workspace/outputs/pet-demo_cartoon_cat/` | 16M | 不用 | 构建中间产物 |
| `pet-motion/tuanzi-pose-*/` 里 56 帧的旧 dump | ~110M | 不用 | 旧 `gen` 路径的原始帧，只有 `f0050.png` 一个被引用 |

**最小迁移集 = `staged/` + `*-sheet/` + `registry.json`（约 25M）**，拷过去之后：

```bash
# 1) ComfyUI 地址（在 ~/.claude.json 的 mcpServers['comfyui-remote'].env.COMFYUI_URL，或用 COMFYUI_URL 覆盖）
# 2) 装（客户端必须**在运行**——这一步经 /pet/asset 控制口走技能链，只有它写用户目录）
node verify/pet-sprite/characters/install-pet.mjs --id demo_cartoon_cat \
  --dir <pet-motion 路径> --canvas 560x448 --bg 00ccff
# 3) 冷启动（registry.json 每进程只读一次，点 UI 开关不算重启）
.\scripts\stop-dev.ps1 ; .\scripts\start-dev.ps1 -Force
```

⚠ `--canvas` 的值**不要照抄 560**——它是量出来的（见 SKILL.md 第五节）。换角色、加动作都要重新量：

```bash
node verify/pet-sprite/characters/sheet-canvas.mjs --dir <pet-motion 路径> --char <角色> --canvas 560x448
```

## 六、未完成 / 已知取舍

1. **`sit` 是正面、`walk` 是侧身，客户端没有转身过渡** → 从走到坐会**瞬间翻面**。
   参考素材（Shimeji）同样如此、也没有过渡，所以按参考来了。
   真要改：`POSES` 里加一条 `from: 'side'` 的侧身坐姿，代价是坐着的猫一直侧对着你。
2. **`fall` 的循环里角色仍会上下漂 ~104px**（模型"下落＝画面里要动"的先验改不掉，改措辞实测无效）。
   靠**头顶留足余量**装下（`--baseline` 0.856 → 0.95，余量 125 → 167px），**不是消除了漂移**。
   ⚠ 想再压它时注意：**头一旦被裁到 0，真实位移就被截断了**，量出来的漂移是**下界**——
   "再换个更狠的措辞"这个方向本身不可证伪，别在这上面迭代。
3. **`crawl` / `fall` 的组解析这一轮没实测到**（爬行要爬到屏幕上边缘、下落要把它扔下去）。
   组都装好了、机制同一套、`walk`/`climb`/`sit` 都验过，但严格说这两条是推断。
4. **宠物窗口的实时画面没能自动截图取证**：`lumii-ui screenshot --target pet` 抓到的是**全黑**
   （透明窗口 + WebGL 画布，`capturePage` 抓不到）。要像素级证据得用
   `scripts/lumii-cdp.mjs`，而它要求客户端带 `--remoteDebuggingPort` 启动，`start-dev.ps1` 没这个口子。
   本轮的视觉验证是**把图集里的帧拼成对照图**看的（`verify/pet-sprite` 下没有现成脚本，当时是临时 node -e）。
5. **图集 4480×5824（26M 像素，RGBA ≈ 104MB）**——14 组 8 帧的代价。再往上加动作要留意。

## 七、这一路的坑（都已修，细节在 SKILL.md）

只列**方法学之外**的、属于本次交付的：

- **画布宽度连错四版**：`421 × 宽高比` → 错（忽略"组内最高"才是分母）；只按包围盒算 → 偏小
  （客户端按**重心**对齐，两侧余量要分开算）；按最小 16 倍数取 → **装完爬行距左边只剩 1px**。
  最终 `560×448`，工具已按四条一并实现。
- **`stage-frame --keep-scale` 保的是"像素"不是"比例"**：对整幅帧等价，对**拼条裁出来的格**
  会再缩 0.667 倍——爬行的猫比走路那格**连头都小**。**这是看出来的不是算出来的**。
- **`fall` 不能用 `displacement`**（"may rise, drop, or crouch within the frame" 会让模型照做，
  角色升出格线、头顶信息丢失），也不能用 `static`（"does not leave the ground" 与"在空中"矛盾）
  → 新增第四类 `suspended`。⚠ 同一个句子在不同动作上对错不同（`static` 对 `sit` 是对的），
  **逐个看，别按类名套**。
- **归因写错过一次并已更正**：`accc3d02` 说"改措辞之后不再升出格线"，实测三次底边漂移
  149 / 131 / 104px，**没变小**，过关的是余量。更正见 `cf91c658`。
  ⚠ 那次的教训值得留着：头被裁到 0 之后**漂移量是下界**，"再试一次更狠的"不可证伪；
  要断言措辞有效得用**同一批抽样帧**做 A/B。

## 八、相关提交

```
2a963248  fix(verify): sheet-canvas 多留一格余量
627c8ef1  feat(verify): 补上 Sit 组（正面坐姿）
4bccf831  fix(verify): --keep-scale 要保持的是「比例」不是「像素」
cf91c658  docs(verify): 更正上一提交的归因
accc3d02  fix(verify): suspended 的"不许动"改抄 locomotion 的措辞
29f38d3c  fix(verify): 下落循环用新的 suspended 运动分类
ce2d5601  fix(verify): sheet-canvas 按重心口径算
1e887ff4  feat(skills): 收录 pet-sprite-h3 技能与 H3 工作流导出件
9ca92299  feat(verify): 侧身动作帧的两段式生成与挑帧工具
```
