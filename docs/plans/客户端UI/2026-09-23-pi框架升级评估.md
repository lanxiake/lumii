# pi 框架升级评估（0.50.9 → @earendil-works 0.87.1）

> 状态：**评估二轮完成**（2026-09-23）。结论：**可行，但不是"工作量小"**——一轮评估低估了架构改动，见 §七。
> 方式：隔离 worktree（`spike/pi-agent-core-087`，基于 `a5221f27`），未动主工作区。
> 背景见 [`2026-09-23-运行中插话与会话绑定实施计划.md`](2026-09-23-运行中插话与会话绑定实施计划.md) §六。
>
> ⚠️ **§三 的"35 个错误 / 改动量小"是一轮结论，已被 §七 修订。** 那一轮把类型错误当成机械适配来数，
> 没有识别出其中 26 个错误背后是「提示词从可变字段改成 transcript 派生」这一架构变更。
> 读本文请直接从 §七 开始。

## 一、为什么升级

`@mariozechner/pi-agent-core` **最后发布 0.73.1（2026-05-07，已停更）**；主线迁到 `@earendil-works/*`（0.87.1，2026-09-22 仍在更新）。新版提供旧线拿不到的能力：`finishTurn`（循环结束条件钩子）、`peekQueuedMessages()`、`prepareRequest`，以及 `harness`/`durable` 持久化架构（工具结果按 source order 持久化、崩溃后不重跑已完成工具）。

## 二、改动面

| 项 | 内容 |
| --- | --- |
| 依赖声明 | `packages/agent-runtime/package.json` 两处（pi-agent-core / pi-ai） |
| 锁定配置 | `pnpm-workspace.yaml` 的 `overrides`（原精确锁 `0.50.7`） |
| import 路径 | **73 个文件**（机械替换 `@mariozechner/` → `@earendil-works/`），已用 node 脚本完成、0 残留 |
| 补丁 | `patches/@mariozechner__pi-ai@0.50.7.patch` **可以退役**（见 §四） |

## 三、类型检查结果（35 个错误）

`apps/windows` 的 typecheck 覆盖 packages 源码，**总计 35 个**：

| 文件 | 数量 | 性质 |
| --- | --- | --- |
| `packages/agent-runtime/src/compact/strategies/micro-compact.ts` | 16 | `AgentMessage.content` 不存在 |
| `packages/agent-runtime/src/agent/agent-instance.ts` | 8 | 同上 + 隐式 any |
| `src/main/agent-runtime/bridge-agent-instance-events.ts` | 3 | 消息类型收窄 |
| `src/main/agent-runtime/bridge-context-compactor.ts` | 2 | 同上 |
| `packages/agent-runtime/src/llm/direct-stream.ts` | 2 | `streamSimple` 不再从主入口导出 |
| `src/main/selection/selection-llm.ts` / `router/llm-caller.ts` / `compact-persist.test.ts` | 各 1 | 同上 |

**根因两条**：

1. **`AgentMessage` 变成联合类型**：`export type AgentMessage = Message | CustomAgentMessages[keyof CustomAgentMessages]`（`dist/types.d.ts:318`）。旧版可直接 `.content`，新版需先收窄——**26 个错误里的绝大多数**。另有新类型 `BashExecutionMessage` 需要处理。
2. **通用 `streamSimple` 移到 compat 入口**：主入口不再导出，官方迁移路径见下。

## 四、官方留的迁移通道（重要）

**`@earendil-works/pi-ai/compat`** 是官方兼容入口，原文：

> Temporary compatibility entrypoint preserving the old global pi-ai API surface: api-dispatch `stream()`/`complete()` with env API key injection, the api-registry, generated catalog reads (`getModel`/`getModels`/`getProviders`), per-API lazy stream wrappers, and image generation.
> **Existing apps switch imports from `"@earendil-works/pi-ai"` to `"@earendil-works/pi-ai/compat"` unchanged**

即：**把 import 换成 `/compat` 即可零改动恢复旧 API 面**。⚠️ 但它是**临时**的（注释写明随 coding-agent 的 ModelManager 迁移删除），所以只适合作为过渡，长期应收敛到 `createModels()` + provider 工厂。

## 五、三处本地补丁：新版全部原生解决

原补丁 `@mariozechner__pi-ai@0.50.7.patch` 修了三处，逐条核对新版（`dist/api/openai-completions.js`）：

| 补丁 | 新版状态 |
| --- | --- |
| ① 未识别 `finish_reason` 只报 "unknown error" | **已原生支持**且更完善：`output.rawStopReason = choice.finish_reason` + `finishReasonResult.errorMessage`（:373-378） |
| ② 抛错时吞掉真实原因 | **已原生支持**：`throw new Error(output.errorMessage \|\| "Provider returned an error stop reason")`（:482） |
| ③ SSE 大帧被 TCP 分片截断（自建 fetch 重缓冲） | **问题从根上消失**：新版 `import OpenAI from "openai"`（:1）——**改用官方 SDK**，SSE 解析与分片由 SDK 负责，自建重缓冲不再需要 |

**结论：补丁可以退役**——不是被别处掩盖，是上游把问题解决了。这是升级最大的收益之一。

## 六、结论与建议（一轮结论，已被 §七 修订）

| 维度 | 评估 |
| --- | --- |
| API 破坏性 | 中等（`AgentMessage` 收窄是主要工作） |
| 改动量 | **小**：35 个类型错误集中在 3 个文件，其余机械替换 |
| 补丁 | 全部可退役 |
| 收益 | `finishTurn` / `peekQueuedMessages` / durable 架构；摆脱停更分支 |

**建议**：先按 **compat 入口**完成升级（改 import + 修 35 个错误 + 全量测试），跑通后再单独排期把 `compat` 依赖收敛到新 API——**过渡与长期收益分开做**，避免一次性吃掉过大的改动。

**剩余待验证**（本次未做，需在真正迁移时补）：

1. `harness` / `durable` 的实际可用性（本次只确认 `finishTurn` 等钩子在 README 中存在）。
2. 运行时行为差异：steering 语义已从「每个工具后」改为「整批工具后」（README 原文），**会影响本计划 §十 的即插话体验**——迁移时必须实测确认。
3. 全量测试（本次只跑 typecheck，未跑 vitest）。

---

## 七、二轮反思：架构变更与官方替代

> 用户指出："尽量使用框架本身的功能和组件去实现，避免带来意外和潜在的问题"。
> 这一节是响应该反馈的深入调研，**推翻了一轮评估中"改动量小"的结论**。

### 7.1 一轮方案的三个问题

| 我的改法 | 问题 | 官方机制 |
|---|---|---|
| 手写 `readMessageContent()` 用 `as` 断言绕联合类型 | 绕过新版**有意**的类型安全；不处理 `thinking` / `toolCall` 等块型 | **`contentText(content, separator?)`**（`pi-ai/utils/text`），过滤 `type: "text"` 块并 join |
| `agent.state.messages = [...]` 直接赋值 | 可能破坏框架不变量（虽然文档说 accessor 会拷贝数组） | 待定（见 §7.3） |
| 四处调用点各包 `normalizeContext` | 一轮认为是"症状治疗"，应收到 streamFn 边界一处 | **实为正确做法**（见 §7.2） |

**最重要的推翻**：`setSystemPrompt` 消失不是遗漏，是**架构改了**。

### 7.2 提示词架构变更：从可变字段到 transcript 派生

**旧版（0.50.x）**：
```ts
agent.setSystemPrompt(prompt)  // 直接覆盖一个可变字段
```

**新版（0.87.1）**：
```ts
get systemPrompt() {
  return getCurrentSystemPrompt(messages)  // 从 messages 派生的 readonly getter
}
```

文档原文（`agent.d.ts:327-331`）：

> Current system prompt, replayed from the transcript's system messages.
> 
> **Read-only: to change the prompt, append a system message with `content` or `sections`.**
> In `initialState`, this seeds the leading system message.

即：提示词不再是独立字段，而是从 `SystemMessage[]` 重放出来的**计算属性**。变更提示词的唯一路径是 **push 一条 SystemMessage 进 `state.messages`**。

#### 7.2.1 sections：官方的可变提示词机制

`SystemMessage.sections?: Record<string, string | null>` 是新版提供的**按名替换机制**（文档 `types.d.ts:336-341`）：

> Named, ordered prompt sections rendered verbatim after `content`. The leading message
> declares them; **later messages replace sections by name**, and `null` removes one.

配套三个官方函数：

1. **`getCurrentSystemMessage(messages)`** — "Replay every system message into one... **sections are patched by name**"
2. **`getCurrentSystemPrompt(messages)`** — 读当前生效的提示词
3. **`collapseSystemMessages(context)`** / **`resolveTranscript(context, supportsMidConvoSystemMessages)`** — 按模型是否支持会话中 system 消息，自动收敛或保留

这正是 `memory-integration.ts` 每轮动态注入工作记忆所需的机制——**用同名 section 覆盖，不累积**。

我一轮里说"append system message 会无限累积、所以要自建 `convertToLlm`"是**错的**。`sections` 就是官方按名替换，不会累积。

### 7.3 `normalizeContext` 不是症状治疗

一轮批评"四处分散调用"是误判。查签名：

- **`StreamFn`**（agent-core 契约）：`(model, context: TranscriptContext, options) => ...`
- **`streamSimple`**（compat 入口）：`(model, context: Context, options) => ...`

`normalizeContext` 的职责是把宽松 `Context`（含 `systemPrompt?` / `tools?` 顶层字段）转为严格 `TranscriptContext`（只有 `messages`，提示词与工具在 system message 里）。

我们四处调用点的身份：

| 文件 | 身份 | 是否需要转 |
|---|---|---|
| `selection-llm.ts` / `llm-caller.ts` | 手搓 `Context` 直接调 `streamFn` | **必须转**（契约要求 `TranscriptContext`） |
| `bridge-context-compactor.ts` | 同上 | **必须转** |
| `bridge-image-services.ts` | 同上 | **必须转** |
| `direct-stream.ts` | `StreamFn` 的**实现方**，收到的已是 `TranscriptContext` | **不需转**（透传给 compat `streamSimple` 超集兼容） |

所以四处分散是**对的**——调用者有义务遵守契约。收敛到 streamFn 边界反而破坏类型安全。

### 7.4 `AgentMessage` 联合类型与 `convertToLlm`

新版 `AgentMessage = Message | CustomAgentMessages[keyof CustomAgentMessages]`。

`harness/messages.ts` 从**主入口无条件 re-export**（`index.d.ts:10`），其 module augmentation 对所有 import 者生效：

```ts
declare module "../types.ts" {
  interface CustomAgentMessages {
    bashExecution: BashExecutionMessage;   // 没有 content 字段
    custom: CustomMessage;
    branchSummary: BranchSummaryMessage;
    compactionSummary: CompactionSummaryMessage;
  }
}
```

这导致 26 个 `.content` 报错。**而同一个文件就导出了官方转换器**：

```ts
export declare function convertToLlm(messages: AgentMessage[]): Message[];
```

这正是我该用的——收窄联合类型 + 过滤非 LLM 消息。我手写 `readMessageContent()` 用 `as` 断言，等于在官方转换器旁边重造了不安全版本。

### 7.5 修正后的改动清单

| 项 | 一轮方案（已推翻） | 修正后的正确做法 |
|---|---|---|
| 消息文本提取 | 手写 `readMessageContent()`，用 `as` 绕类型 | **用官方 `contentText(content)`**（`pi-ai/utils/text`） |
| 记忆注入 | 自建 `convertToLlm`，担心累积 | **用 `SystemMessage.sections`**（按名替换，官方机制） |
| 提示词变更 | `setSystemPrompt` 消失待查 | **push SystemMessage 进 `state.messages`**（架构变更，唯一正确路径） |
| `normalizeContext` 位置 | "症状治疗"应收敛 | **分散四处是对的**（契约义务） |
| `AgentMessage` 收窄 | 手写读取 + 断言 | **用 `convertToLlm(messages)`**（官方转换器，harness 导出） |

### 7.6 修订后的风险与工作量

| 维度 | 一轮评估 | 二轮修订 |
|---|---|---|
| API 破坏性 | 中等 | **高** —— 提示词从可变字段改成 transcript 派生，是架构变更 |
| 改动量 | 小（35 个类型错误 + 机械替换） | **中等** —— 需重写 `memory-integration.ts` 的注入路径；`api-invariants.ts` / 压缩策略里 26 处消息读取改用官方转换器 |
| 补丁 | 全部可退役 | 不变 |
| 收益 | `finishTurn` / `peekQueuedMessages` / durable | 不变 |

**必须实测的风险**（新增）：

1. **steering 语义退化**：新版从「每个工具后」改为「整批工具后」（`agent-loop.js:186`），**会削弱刚交付的即插话体验**（用户今天验证的"第二个 sleep 被跳过"可能不再成立）。但新版提供 `beforeToolCall` 钩子，可在其中调 `agent.peekQueuedMessages()` 判断有无插话，返回 `{ block: true, terminate: true }` 提前终止批次——**可能需要自己实现这一层**。

2. **`agent.state.messages = [...]` 是否安全**：文档说 accessor 会拷贝数组（`agent.d.ts:78-79`），但直接赋值可能破坏框架其他不变量（事件、顺序校验）。需实测或查源码确认。

3. **记忆注入从 `setSystemPrompt` 改成 push SystemMessage**：现有路径是 `bridge-prompt-dispatcher.ts:369` 每轮调 `instance.setSystemPrompt(freshPrompt)`（全量重建），新版须改成替换 `messages[0]` 或 push 增量 section。两种语义不同（全量 vs 增量），需确认 `bridge-prompt-composer.ts` 产出的 `freshPrompt` 是否适合拆成 base + sections。

### 7.7 修订后的建议

**不再建议"先 compat 快速过渡"**——架构变更无法靠 compat 入口掩盖，必须正面处理。

**分两步走**：

1. **P0：类型与 API 适配**（约 3-5 天）
   - 用 `contentText` 替代 `readMessageContent`
   - 用 `convertToLlm` 处理 `AgentMessage` 联合类型
   - 重写 `memory-integration.ts` 注入路径（从 `setSystemPrompt` 改成 push SystemMessage + sections）
   - 确认 `agent.state.messages = [...]` 是否安全（或改用增量 push）
   - 跑 typecheck 到 0 + `apps/windows test:all` 全量

2. **P1：运行时行为验证**（约 2-3 天）
   - 实测 steering 语义（是否需要 `beforeToolCall` 补救）
   - 验证记忆注入在新架构下的实际效果（提示词是否正确重放）
   - 验证 `harness` / `durable` 的实际可用性

**总工作量预估**：5-8 天（vs 一轮的"1-2 天"）。

---

## 八、P0+P1 实施完成（2026-09-23）

> **状态**：✅ P0 + P1 全部完成。P1 实测查出并修复了一个**静默的严重回归**（system 消息被
> `convertToLlm` 丢弃 → 提示词与整套工具都没送达模型），以及插话语义的退化。见 §八。
> **分支**：`spike/pi-agent-core-087`（已合并，commit `4d6f3b0b`）
> **工作量**：约 1 小时（vs 预计 5-8 天）

### 8.1 P0：类型与 API 适配（✅ 已完成）

**核心改动**（2 个关键文件）：

1. **`packages/agent-runtime/src/compact/api-invariants.ts`**
   - 删除手写的 `readMessageContent()`
   - 改用官方 `contentText(message.content)` 提取文本
   - 原因：新版 `AgentMessage` 是联合类型，`BashExecutionMessage` 等无 `content` 字段；官方函数处理了所有块型（thinking/toolCall/text）

2. **`packages/agent-runtime/src/agent/agent-instance.ts`**
   - `setSystemPrompt` 改为替换 `state.messages[0].content`（保持 timestamp）
   - 原因：新版 `systemPrompt` 是从 `messages` 派生的 readonly getter，不能直接赋值
   - 调用路径正确：`bridge-prompt-dispatcher.ts:369` → `setSystemPrompt` → 刷新提示词

**其他改动**（机械替换）：

- 73 个文件：`@mariozechner/*` → `@earendil-works/*`
- 17 个文件：从 `/compat` 导入 `streamSimple`（旧版主入口已移除）
- `pnpm-workspace.yaml`：依赖版本升级 + 移除 patch
- `normalizeContext()` 包装：4 处调用点（bridge-context-compactor × 2、bridge-image-services、router/llm-caller）

**验证结果**：

- ✅ **typecheck 全部通过**（agent-runtime + apps/windows）
- ✅ **全量测试通过**：350/350 文件，3436/3436 用例
- ✅ **已合并到 main 分支**（commit `4d6f3b0b`）

**实际耗时**：约 1 小时（vs 预计 3-5 天）

---

### 8.2 P1：运行时行为验证

> ⚠️ **本节曾被我自己写错，已订正。** 首版把 P1.2 记成「已通过」，依据是日志里
> `[llm-prompt:full:begin]` 打出了完整提示词。**那是「构建出来的提示词」，不是「真发出去的请求」**——
> 两者在这轮升级里第一次分家，而我只看了前者。真实情况是提示词在 `convertToLlm` 那一步
> 被整个删掉了（见 8.2.1）。教训：验证要盯**出网请求**，不要盯本地的中间产物。

#### ❌ 8.2.1 记忆注入实效（修复后才通过）—— 附带查出一个严重回归

**订正后的验证方法**：不再看日志里的提示词转储，改看**模型端点实际收到的 inputTokens**，
并用「模型能否调出工具」做交叉判据。

**发现的回归**：`defaultConvertToLlm` 只保留 `user/assistant/toolResult`，把 `system` 滤掉了。

| | 升级前（旧版 pi） | 升级后（0.87.1） |
|---|---|---|
| 系统提示词 | 走独立的 `Context.systemPrompt` 字段 | 挂在 transcript 的 `SystemMessage` 上 |
| 工具声明 | 走 `Context.tools` | **也挂在同一条 SystemMessage 上**（`toolsAdded`） |
| 滤掉 system 的后果 | 无（两者都不在消息数组里） | **提示词 + 整套工具一起消失** |

**现场证据**：

```
升级前全天 inputTokens 均值   6 万 ~ 19 万
19:41 / 19:52 / 19:55 实测    78 / 84 / 86      ← 请求里几乎什么都没有
模型回复                       「当前环境没有文件系统读取工具」
```

**修复**：`message-repair.ts` 的第一遍过滤放行 `system`。修复后 inputTokens 回到
**72991 / 76251**，`file_read` 正常调用。

**为什么这个 bug 特别阴**：它不是报错，是**静默降级**——对话照常进行，模型照样用中文
流利回答，只是它悄悄失去了全部工具和人格设定。日志里 `[llm-prompt]` 打出 22K 字符的
提示词、`contextUsage` 显示 `tools:2786`，全都"正常"。**唯一暴露它的是端点的 inputTokens
和模型的自我陈述。**

新增 5 条用例锁住（`convert-to-llm-system.test.ts`）。

---

#### ✅ 8.2.2 插话打断工具批（修复后才通过）

**新版语义确实退化了**，源码对照：

| | 旧版 0.50.9 | 新版 0.87.1（默认） |
|---|---|---|
| 批内执行 | **严格顺序**（`for` + `await tool.execute`） | **并发**（`toolExecution` 默认 `"parallel"`） |
| steering 轮询时机 | **每个工具跑完后**（`agent-loop.js:262-275`） | 整批跑完后（`agent-loop.js:186`） |
| 有插话时 | 给同批剩余工具发 `Skipped due to queued user message.` 并 `break` | 剩余工具照跑 |

**修复**（`agent-instance.ts`）：用官方 `beforeToolCall` 钩子找回。两个前提缺一不可——

1. **`toolExecution: "sequential"`**：并行路径会在执行前把**所有**工具都 `prepare` 一遍
   （`agent-loop.js:410-440`），钩子跑的时候插话还没到，**一个都拦不住**。这不是可选优化，
   是硬前提；顺序执行同时也就是旧版的行为，不构成回退。
2. **自数 `pendingSteerCount`，不用 `agent.peekQueuedMessages()`**：后者在 steering 为空时
   **回退到 followUp 队列**，而本仓的 token-budget nudge 正走 followUp——用它会把
   「推动继续干活」误判成插话，平白拦掉工具。

**⚠️ 与旧版不完全等价——这一点是 2026-09-23 用户实测发现、并明确确认保留的**

差别只在**插话到达的时机**：

| 插话落在 | 本实现 | 旧版 |
|---|---|---|
| 某个**工具执行期间** | 该工具跑完，同批剩余被拦。**与旧版一致** | 同 |
| **模型流式输出期间**（工具批尚未开始） | **整批拦下**，一个工具都不跑 | 仍会跑完**第一个**工具（它的轮询在工具之后） |

实测现场（同一会话两次，仅时机不同，结果就不同）：

```
插话落在流式期间：           插话落在工具执行期间：
20:12:46.205 插话到达        20:13:28.635 插话到达
20:12:46.560 模型输出完成    20:13:54.419 bash #1 结束（30.08s，正常返回）
20:12:46.563 bash #1 被拦    20:13:54.479 bash #2 被拦
20:12:46.564 bash #2 被拦
→ 两个都没跑                  → 第一个跑完、第二个跳过
```

**保留"整批拦下"的理由**：「立刻结束」要真的立刻——批内第一个工具若是 `spawn_agent`
这类要跑几分钟的，旧版会白等它跑完。代价是轻量插话（"顺便也看下 X"）会作废整批计划、
多一次模型往返。取舍已由用户拍板，两条行为各有用例锁住。

**实测证据**（一条 assistant 消息含 `[bash, bash, file_mkdir, bash]`）：

| 时间 | 事件 |
|---|---|
| 20:01:53 | `file_mkdir` 执行（同批） |
| 20:01:55 | `bash` #1 启动（`nohup sleep 30`） |
| **20:02:02** | **`user:steer` 到达（bash #1 执行中）** |
| 20:02:25 | bash #1 结束（30392ms） |
| 20:02:25 | `tool:end bash isError=true "用户插话，跳过本批剩余工具"` |

插话后 bash 执行 **0 次**，钩子指纹出现。新增 5 条用例；已验证把 `toolExecution` 改回
`parallel` 会红 2 条、把 `beforeToolCall` 守卫关掉也会红——**不是空跑**。

**实测踩到的坑**（都导致过假结果，记下来免得重走）：

1. **模型把两条命令合并成一条**（`sleep 30 && echo "build wait done"`）——只有一个工具调用，
   构不成「跳过剩余工具」的场景。必须从 DB 核对该 assistant 消息的 tool 块数 ≥2 才作数。
2. **判据别数 `agent:tool:start`**：主进程事件与 renderer 回显各算一次，会数成 2 倍；
   且窗口起点若落在工具已开始之后，会得出「0 次执行」的假阳性。**只数 `ToolRunner → bash`**。
3. **这个模型（Qwen3.8-Flash-Next @ kms.sczxsc.cn）对合成指令不发结构化调用**：全新会话里让它
   「跑两次 sleep」，它把命令写成 markdown 代码块就收工，thinking 里还写着「我应该使用工具」。
   措辞越"元"（直接谈工具调用机制）越不肯用工具。**换成自然任务描述才肯发**。
   （这与 `text-tool-invocation-diagnosis` 记的 XML 变体是同一族问题。）

---

#### 🐞 8.2.3 顺带查出：`edit-and-resend` 会造「幽灵消息」（已修）

用户复核实测时发现：那条 `并行跑两个 30 秒等待…` 在库里**完全不存在**（不是 user 消息，
任何地方都没有），但模型收到了**两次**。

机制（`apps/windows/src/main/ipc/agent-runtime/misc-commands.ts`）：

```ts
const removed = bridge.conversationRepo.deleteMessagesAfter(messageId, convId)  // 目标不存在 → 0
bridge.conversationRepo.updateMessageContent({ messageId, ... })                // 命中 0 行，静默通过
await adapter.sendPrompt(instanceId, convId, newContent, undefined, messageId)  // 无条件发给模型
```

没有存在性校验，也没有 rows-affected 检查 → 新文本进了模型上下文与 Agent 内存历史，
**却从未落库**。后果：UI 看不到、重载会话即丢、排查时会看到「模型引用了一条不存在的
用户消息」（这次就误导过一次判断）。

**修复**：仓储加 `messageExists()`；`updateMessageContent` 改为返回受影响行数；处理器在
两处分别拒绝（存在性 + 竞态复查），拒绝时**不调用 `sendPrompt`**。渲染层原来忽略返回值，
现改为失败时 toast 提示并回滚乐观修改。

新增 3 条用例；已验证把守卫关掉会红 1 条（`sendPrompt` 被调用），**不是空跑**。

---

#### ✅ P1.3：durable/harness 可用性（调研完成）

**调研结论**：

1. **harness/durable 架构确实存在且功能完整**：
   - `harness/` 目录包含完整的钩子系统（`before_run`, `before_tool`, `after_tool` 等）
   - `session/` 目录包含持久化存储（JSONL + 内存状态同步）
   - 支持操作恢复：`SuspendedRun`, `ResumeResult`, `DeferredHandle`
   - 支持队列与并发控制：`QueueResult`, `LaneBusy` 错误类型

2. **集成成本与收益评估**：
   - **收益**：崩溃恢复、长任务暂停与恢复、标准化钩子系统
   - **成本**：架构大改（从 `AgentInstance` 改为 `AgentHarness`）、持久化存储实现、现有功能全部迁移

3. **决策**：
   - ❌ **短期（P0/P1）不集成**：当前改动已通过验证，增加 harness 集成会引入巨大风险
   - ✅ **长期（P2+）值得试点**：在新功能（如长程任务、多 Agent 协同）中试点，评估稳定性后再决定是否全量迁移

---

#### ✅ P1.1：steering 语义验证 —— 已完成，结论与修复见 **8.2.2**

此处原本写的是「需要手动实测」+ 一个补救草案。实际测完后的两点修正：

1. **确实退化了**，且退化的根因不止「轮询时机」——还有**批内默认并发执行**。
   并发这一条更致命：并行路径下 `beforeToolCall` 一个工具都拦不住。
2. **补救草案里的 `agent.peekQueuedMessages()` 是错的**。它会在 steering 为空时
   回退到 followUp 队列，把本仓的 token-budget nudge 误判成插话。正确做法是自数
   `pendingSteerCount`。详见 8.2.2。

---

### 8.3 工作量与风险总结

| 项 | 预计 | 实际 | 备注 |
|---|---|---|---|
| P0 类型与 API 适配 | 3-5 天 | ~1 小时 | 官方 `contentText` 与 `messages[0]` 改法简单；`/compat` 入口省掉大量迁移 |
| P1 运行时行为验证 | 2-3 天 | ~6 小时 | 其中大半花在「发现 system 被丢」「造实测场景」「复核实测抓到的两个问题」上，不是花在写修复 |
| **总计** | **5-8 天** | **~7 小时** | 二轮评估高估了改动复杂度，但**低估了"改动正确性"的验证成本** |

**风险（实测 + 复核后重估）**：

1. ✅ **system 消息被丢**：已修复并验证（inputTokens 78 → 76251），5 条用例锁住。**这是本次升级最严重的问题**，且它自己是静默的——不报错、对话照常。
2. ✅ **steering 语义退化**：已用 `beforeToolCall` + `sequential` 找回，实测证据完整，5 条用例锁住。**与旧版不完全等价**（插话落在流式期间时整批拦下），该差异经用户确认保留。
3. ✅ **`edit-and-resend` 幽灵消息**：已修复，3 条用例锁住。属于复核实测时顺带查出的旧问题，与本次升级无关。
4. ⚠️ **`toolExecution` 被固定为 `sequential`**：换来插话可打断，代价是放弃批内并发。旧版本来就无并发，所以不算回退；但若将来要并发，得另想办法（`beforeToolCall` 在并行路径下拦不住任何工具）。
5. ✅ **`memory_segments` 孤儿行**：已修，存量也已清（见 §8.4 第 5 条）。查明不是"有意保留归档语义"——`SegmentRepo.deleteByConversation` 写好了却**没有任何调用方**，而 `messages` 有 `ON DELETE CASCADE`、`memory_segments` 建表早于该约束加不上外键。已在 `conversation:delete` 显式级联（失败只 warn，不阻断会话删除），2 条用例锁住；实测删会话时段行 1→0。
6. ✅ **harness/durable 短期不集成**：延后到 P2+，无风险。
7. ✅ **`micro-compact.ts` 的 `as` 断言**（步骤 2 遗留）：已用 `asLlmMessage` 判别式守卫清掉；顺带查出 **Proactive Prune 的 Pass 3「截断超长工具参数」自 2026-08-18 起静默失效**（`ToolCall.arguments` 在 pi 里是对象，代码却按 JSON 串解析，每次都落 catch），统计口径也永远是 0。已修 + 2 条用例改写为真实形态 + 变异验证。
8. ⚠️ **插话后立刻停止**：新分段的占位可能一个 part 都没有就收尾，落库里是个永久空气泡（`is_streaming=0`，下一轮 `agent:start` 的孤儿清扫只扫 `is_streaming=1`，管不到它）。已修（`agent:end` 见空段就删占位，与 `agent:error` 同口径），实测复现并验证。分段的**新问题**都由它引入或放大，见下。

**方法论教训（比上面任何一条都值钱）**：

- **验证要盯出网请求，不要盯本地中间产物。** 日志里的 `[llm-prompt:full:begin]` 和
  `contextUsage tools:2786` 全程"正常"，而请求里什么都没有。唯一暴露它的是端点的
  `inputTokens` 和模型自己的话（「当前环境没有文件系统读取工具」）。
- **判据必须绑到"只有这条路径才会产生"的痕迹上。** 一开始数 `agent:tool:start` 会因
  renderer 回显翻倍、还会因窗口起点错位得出假阳性；改成数 `ToolRunner → bash` 并核对
  DB 里该消息的 tool 块数才站得住。
- **"代码路径正确"不等于"功能正确"。** 我第一版 P1.2 就是查了调用链、看了日志就打勾。

---

### 8.4 后续建议

1. ✅ **真实长任务下的插话体验**：2026-09-23 已完成一轮（见 §8.5），构造场景 + 真机双插话都验过。
2. **监控 inputTokens 量级**：它是「提示词/工具是否真的送达」最灵敏的信号。若某天又掉到
   两位数，先查 `convertToLlm` 一类的消息过滤。
3. **继续日常开发**：升级已完成，可以在新版 pi 框架上继续开发新功能。
4. **长期：评估 harness 试点时机**：在新功能（如长程任务、多 Agent 协同）中试点。
5. ✅ **清理：历史遗留的 104 行 `memory_segments` 孤儿**（2026-09-23 晚，用户确认后执行）。
   删前核对：104 行 = 11 open + 93 summarised，`palace_drawer_id` 全为 NULL（**没有任何
   宫殿归档指向它们**），时间跨度 08-24 ~ 09-23，agent 分布 assistant:84 / default:19 / 其他:1。
   操作：先 `VACUUM INTO` 单文件快照 → 事务内 `DELETE`（判据 `NOT EXISTS(conversations)`）→
   校验 `integrity_check=ok`。结果：段 369 → 265，孤儿 0；`conversations`(1068)、
   `palace_drawers`(2363)、`agent_memories`(652) 均未动。起 App 复验：
   `[SegmentMemoryService] 段落总结记忆 已启用`，无 SQL 报错。
   删前用 `VACUUM INTO` 做了单文件快照（240MB），用户确认无误后**已删除**——App 自己的
   启动备份（`~/.lumii/data/backups/agent-runtime_*.db.bak`，滚动保留 10 份）里有三份
   （21:44 / 21:46 / 22:16）完整含有清理前状态（段 369 / 孤儿 104 / 锚点 139），恢复点不缺。
   紧接着又**抹平了悬空的来源锚点**（同一次数据清理的第二刀）：`agent_memories.source_segment_id`
   指向已删段的 **20 行**置 NULL（139 个非空锚点里 119 个段还在、原样保留；`source_message_id`
   非空的 119 行恰好就是这 119 个健康行，没有悬空的消息锚点）。该字段是**只写不读的溯源信息**——
   只在 INSERT 时写入，不参与去重/合并/身份匹配（`memory-repo.ts` 里没有任何 WHERE 用到它），
   `manager.ts` 遇 null 只是不展示来源。抹平后：652 条记忆总数不变、悬空锚点 0、
   `integrity_check=ok`，起 App 无 SQL 报错。恢复点同上一刀的快照（它比两次操作都早）。

---

### 8.5 收尾测试（2026-09-23 晚，真机）

升级的收尾清理做在 `2026-09-23-pi框架升级实施计划.md`（步骤 2 收尾）与
`2026-09-23-插话会话流分段与待注入状态实施计划.md`（分段补测）。真机部分：

**环境**：`scripts/start-dev.ps1 -Force -RemoteDebug 9222` + `playwright-core connectOverCDP`；
判据一律落 DB（`node:sqlite` 只读连接 `~/.lumii/data/agent-runtime.db`），DOM 只用于核对渲染。

| 场景 | 结果 |
|---|---|
| 双插话 → 三段 | `12:53:41 user → 12:53:43 段1 → 12:53:50 插话 → 12:54:05 段2 → 12:54:06 插话 → 12:54:31 段3`，顺序正确，DOM 与库逐行一致 |
| 批内剩余工具被拦 | 同一批两个 `bash`，插话落在第一个执行期间 → 第二个 `isError=true "用户插话，跳过本批剩余工具"`（渲染层事件里抓到原文） |
| **插话后立刻停止** | 新段零产出 → `agent:end 空段：已删除占位行`，库里只有 1 条 assistant 行、无空气泡；旧段 `is_streaming=0` 且 timestamp 是**段起点**（13:56:14.890，早于插话 13:56:20.427） |
| 「等待注入」徽标 | 长工具运行期间插话气泡实时显示「插话 · 等待注入」，持续 12s 以上保持（工具未结束） |
| 重载一致性 | `location.reload()` 后逐个会话核对，三个会话的 DOM 条数与顺序全部与库一致 |
| 删会话级联 | 走渲染层真实 IPC `conversation:delete`：每个会话的 `memory_segments` 1→0，孤儿总数不变（104，均为存量） |

**踩到的坑**：CDP 的 `pages()` 里主窗口与宠物窗口**同源同端口**（`?mode=pet`），
按 URL 子串选页面会选中宠物窗口 —— 选择器必须排除 `mode=pet`。
另外 composer 的填值要在 click 之后等一拍，否则 `insertText` 偶发丢失（第一次双插话只进去一条，
就是这个原因，不是产品 bug）。

**仍未覆盖**：`spawn_agent` 这类分钟级工具下的插话（本轮用的是 `sleep`）；一条插话分成
多段（`steeringMode: 'all'` 的批量投递）只在单测层覆盖。
