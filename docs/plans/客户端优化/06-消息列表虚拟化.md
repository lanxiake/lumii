# 第 6 片 · 消息列表虚拟化 — 实施计划

> 创建：2026-09-15 · 状态：**已完成**（2026-09-15，3 个提交，含 /code-review 两轮复核修复，见 §六/§七）
> · **09-16 补实测复核**（修复后首个完整运行日，结论见 §六之二）
> 上位文档：[客户端优化切片计划](./README.md)
> 数据来源：`logs/perf/perf-2026-09-15.jsonl`（根因）、`perf-2026-09-16.jsonl`（修复后复核）；
> 渲染进程内存采样，preload 侧 `process.getBlinkMemoryInfo()` 原生口径
>
> **一句话结论**：崩溃根因是渲染进程 Blink 分配器的碎片（`blinkTotal` 1GB vs 活跃 145MB），
> 不是 JS 堆 OOM；对策是消息列表窗口化（离屏行卸载 DOM）+ 离屏行 `content-visibility`。
> 复核确认 **12 条真缺陷**（含两轮修复自身引入的 2 条）已全部修掉，**7 条**判定不成立或属
> 固有取舍（已记录依据），**4 项**留在 §七"后续观察项"——**不是待办，真出问题再优化**。
> **修复已由 09-16 实测确认生效**：10.5 小时内容涨 3 倍，RSS 只涨 3%、`blinkTotal` 持平
> （修复前同类运行 2.9 小时 RSS 涨 161%、`blinkTotal` 到 1020MB）。同日暴露的两笔新开销
> （dev 模式的 detached DevTools、大会话首次挂载的解析峰值）**不属于本片范围**，已在 §六之二登记。
> **真实滚动观感仍需人工冒烟**，清单见 §六。

---

## 一、根因（perf 日志实证）

渲染进程私存无界增长，涨的全是 Blink 原生内存，不是 JS 堆。同一渲染进程 pid 3092 存活 3 小时：

| 指标 | 16:54 首采 | 19:59 末采 | 增长 | 占 RSS 增长 |
|---|---|---|---|---|
| **RSS**（进程私存） | 394 MB | **1737 MB** | +1334 MB | 100% |
| **`blinkTotal`**（Blink 分配器预留总量） | 44 MB | **1020 MB** | +976 MB | **73%** |
| `blinkAllocated`（真正活跃分配） | 32 MB | 145 MB | +113 MB | — |
| `jsHeapUsed`（V8 堆） | 89 MB | 116 MB | +27 MB | 2% |
| DOM 节点（峰值 6619）/ 图片 1.7MB / canvas 5MB | — | — | 可忽略 | — |

三点结论：

1. **不是 V8 堆 OOM**。`jsHeapUsed` 峰值 146 MB，离 4GB 上限差两个数量级。`renderer-memory-probe.ts` 开头注释里"V8 堆撞 4GB 上限"是旧归因——当时日志只有 `rss` 一个读数，看不到 Blink 侧，缺口只能瞎猜。本次补上 `native` 口径后实测推翻。
2. **是 Blink 分配器碎片化**。`blinkTotal`（1 GB）远大于 `blinkAllocated`（145 MB），中间 **875 MB 是 Chromium PartitionAlloc 已经扩出去、但没在用的碎片**。典型成因：频繁增删大量 DOM 节点后各 bucket 扩张了就不归还系统。
3. **增长是阶梯式的，与"大内容加载"严格同步**，且每跳一次不再回落：

   | 时刻 | blinkTotal | 同一采样触发 |
   |---|---|---|
   | 16:54 | 44 MB | 冷启动 |
   | 17:05 | 319 MB | 第一波加载 |
   | 18:29 | 610 MB | `domNodes` 3053→5794，`fileEvents` 65→318，`contentChars` 375K→1335K |
   | 19:43 | **1120 MB（单跳 +431）** | `fileEvents` 332→1091，`contentChars` 1509K→3132K |

**DOM 来源**：`ChatContainer` 对消息列表**全量挂载**（`index.tsx:527` 的 `chatItems.map(...)`），全仓无 `react-window` / `react-virtuoso` / `@tanstack/react-virtual`。每条消息跑一遍完整 Markdown 管线（remark-math + rehype-katex + rehype-highlight），`markdown-limits.ts` 的注释自证量级：*"43 条消息 / 826KB 正文全量解析需 2.4s，产出 8.9 万个 DOM 元素（其中 4.3 万个是 highlight.js 的 `<span>`）"*。这些 span 每个都要在 PartitionAlloc 里分配，正是 `blinkTotal` 无界扩张的直接来源。"加载更早的消息"懒加载只控制**何时加载**，加载过的消息**永远保持挂载**。

**"现在没崩"是自愈兜底，不是没事**：perf 日志里今天有 4 个 renderer pid 接力（3092 → 35160 → 39760 → 37372），每次涨高后消失、新 pid 从 ~350MB 重新开始 = 至少 3 次崩溃重启。主窗口 `render-process-gone` 自愈（`main-window.ts:195`，5 分钟 3 次限流 + 递增延迟 reload）在兜底，用户看到的是"闪一下"而不是"崩死"。最新 pid 37372 的斜率比 3092 初期还陡（15 分钟 362→914MB）。

---

## 二、技术方案

两条腿走路，合在一个 `.message-row` 容器上实现（两处判定互斥，见下）：

### 前置：`.message-row` 行容器

每条消息/压缩卡片外包一层 `div.message-row`，同时承载 A、B 两条手段与 ResizeObserver 的测高目标。**它必须是 `display:flex; flex-direction:column`**：`.message` 依赖父级是 flex 上下文才有 `align-self`（user 靠 `flex-end` 右对齐）与"宽度收缩到内容"的语义，换成普通块级容器后 user 气泡会被撑满 `max-width`。flex 容器同时保证子元素外边距不与容器折叠（CompactionCard 的 8px 上下边距计入本层高度），实测高度才能原样用作占位高度。

顺带的两处配套改动（不改则静默退化）：

| 项 | 原状 | 改动 | 原因 |
|---|---|---|---|
| React key | `${item.id}-${index}` | `m:${id}` / `c:${id}`（前缀区分消息与压缩卡片） | 位置型 key 会让「前插历史」把所有行错位重挂，高度缓存随之作废 |
| 入场错峰 | `.message:nth-child(1..5)` | `.message-row[data-enter-index='0..4'] > *` | `.message` 在行内恒为第 1 个子元素，旧规则全部退化为 0ms；而 `.message-row:nth-child(N)` 也不行 —— `:nth-child` 按所有兄弟计数，列表里还有 history-loader 等节点，会把行序挤偏一位 |

### 方案 2 · `content-visibility`（跳过布局绘制，缓解）

在窗口带内、但**确实在视口外**、且**已被看见过**的行上加 `.message-row--skippable`：

```css
.message-row--skippable {
  content-visibility: auto;
  contain-intrinsic-block-size: auto var(--row-h, 240px);
}
```

`--row-h` 是 ResizeObserver 的实测值（内联 style 下发）。**这个值不是可选的**：跳过期间元素盒子取 `contain-intrinsic-size`，若用固定的兜底值，RO 会把这个假高度写回高度缓存，下一次占位就会用假高度替换真实高度 —— 正是我们要避免的滚动跳动。（CSS 里那个 240px 兜底不参与判定：类只在"已测高"的行上才加，它纯粹是防变量缺失导致属性整体失效。）

`seen` 这个前置条件同理不可省：跳过期间子树不跑动画，若让"挂载时就在屏外"的行（前插的历史页）跳过，它的入场动画会推迟到用户滚到它时才播。只对已经滚过去看过的行动手，既避开这个坑，也让"跳过"始终是纯优化。

### 方案 1 · 窗口渲染（卸载 DOM，治本）

自己实现一个**无第三方依赖**的轻量虚拟化，只对消息列表这一个场景特化，避免为它引入锁文件变更与联网安装（本地优先项目，且本机 `pnpm view` 联网探测已不可靠）：

- **高度缓存**：每个 `.message-row` 用 `ResizeObserver` 实测高度，存进 `Map<id, height>`。
- **两个 IntersectionObserver**（同一 `root` = 滚动容器，各司其职）：
  - 窗口带 observer（`rootMargin: ±2000px`）→ 维护 `bandHidden`，**判占位**；
  - 视口 observer（`rootMargin: 0`）→ 维护 `viewportHidden` 与 `seenKeys`，**判可否跳过**＋**判是否看过**。
- **占位替换**：`bandHidden && 已测高 && 非 pinned` 时，整行换成 `<div style={{height}}>`，
  真实子树卸载；高度取实测值，滚动位置不变。
- **未测过的行永远渲染真实内容**：新前插的历史、流式新增的消息都在此列，天然避免"估算高度 ≠ 实际高度"导致的滚动跳动。初始小列表全量渲染，滚过一遍后各行依次测得真实高度，再离开窗口带才换成占位。
- **决策全部收敛为"只信明确报告"**：两个 hidden 集合都从空开始、只被 `isIntersecting === false` 写入。尚未收到报告的行一律当作可见——否则行会在首帧、测高与观察回调之间的窗口里被误判离屏，造成闪烁。
- **pinned**：正在流式输出的那条永不占位。它高度每帧都在变，占位/回填会来回打架。

决策依据：不引 `@tanstack/react-virtual` 的理由是（a）本地优先、离线友好的仓库原则；（b）本项目滚动语义特殊（粘底自动跟随 + 前插历史锚点补偿 + 流式增长），特化实现比通用库更贴合；若特化实现后续证明不足，再评估换库（风险低，接口只封在 `useWindowedRows.ts` 一个文件）。

### 两条手段的分工

| | 方案 2（content-visibility） | 方案 1（窗口渲染） |
|---|---|---|
| 作用范围 | 窗口带内、视口外 | 窗口带外 |
| 对 DOM | **保留** | **卸载** |
| 回收内存 | ❌ | ✅ 治本 |
| 降渲染开销 | ✅ | ✅ |
| 前置条件 | 已测高 + 曾被看见 | 已测高 |

二者条件互斥（`bandHidden` 是与非），不会同时命中一行。

---

## 三、批次

两处手段**合为一次改动**交付（原计划分 6a/6b）：它们共用同一个 `.message-row` 容器与同一套高度缓存，拆开交付需要先引入一个只为 content-visibility 服务的临时包裹层，反而多一次结构变更。

| 批次 | 内容 | 验证 |
|---|---|---|
| 6 | `.message-row` 容器 + React key 稳定化 + 入场错峰迁移 + `useWindowedRows.ts`（两条手段）+ 接入 `ChatContainer` | typecheck ✓ / `pnpm test` 391 passed ✓ / `pnpm test:all` 2150 passed, 0 failed ✓ / 单测 28 例 ✓ / 生产构建 ✓ |

收尾：README 切片表新增第 6 片行 + 本文档执行记录 + 待人工冒烟。

---

## 四、明确不做

- **不改消息内容渲染本身**：markdown 闸门（`markdown-limits.ts` 的 16KB/8KB/12KB 阈值）已存在，属"单条极端值"缓解，不是本片对象。
- **不动侧边栏会话列表 / Wiki 文件列表等其它长列表**：它们体量远小于消息列表，本片聚焦崩溃主因。
- **不改主进程 `render-process-gone` 自愈**：那是兜底，不是根因，保持现状。
- **不引入虚拟化三方库**（理由见 §二）。

---

## 五、风险与对策

| 风险 | 对策 |
|---|---|
| 滚动跳动（占位高度 ≠ 实际高度） | 只有"已实测高度"的行才占位；未测行永远渲染真实内容。占位高度直接取 RO 实测值，**且不取整**（行高常是小数，逐行 0.5px 同号累积就是几十像素的误差） |
| `content-visibility` 的 `contain: paint` 裁掉子元素溢出 | 只加在"视口 observer 明确报告不可见"的行上，可见行永不加 —— 入场动画的 `translateY(20px)` 溢出因此不会被切 |
| 跳过期间的假高度污染高度缓存 | `contain-intrinsic-block-size` 用实测值（内联 `--row-h`）而非固定兜底值，RO 回报的高度与缓存一致 |
| 跳过期间不跑动画 → 入场动画被推迟到滚到时才播 | `seen` 门槛：只对已经进过视口的行动手 |
| 跳过期间行高被 size containment 冻结 | `pinnedKeys` 对 A、B **都**生效：占位与跳过都排除流式行（它的正文每帧都在长） |
| 全局重排（改字号 / Ctrl+滚轮 zoom / 窗口缩放 / 拖面板）后折叠行的高度是旧值 | `useLayoutEpoch`：字号或容器宽度一变即作废整个高度缓存并**强制重测一次**。三个防坑点：① 信号带 200ms 防抖 —— 拖动窗口/分隔条时宽度每帧都在变，逐帧作废会让折叠行按帧走「展开→重测→再折叠」，等于按帧重挂整份会话；② 宽度取 border box，滚动条出现/消失不触发；③ 作废时对每行 `unobserve + observe` 强制补投一次初始条目 —— 否则高度与宽度无关的行（纯代码块等）重新展开时盒子尺寸没变、RO 不再回调，记录永远回不来、该行从此不再折叠 |
| `unobserve` 不清空已排队条目 → 已 prune 的 key 被迟到条目写回（切回会话时首帧闪烁） | 观察回调统一经 `rowKeyOfEntry` 过滤，只认「当前仍在列表里」的 key |
| 流式期间每 token 全量重算（决策 memo、prune effect、pinned 集合） | `rowKeys` 走 `useStableMapById`（key 序列只在增删消息时变，流式 delta 复用同一数组）；pinned 先取签名再建 Set，引用在流式期间稳定 |
| `ResizeObserver` 回调触发渲染循环 | 只在高度真正变化时写 state |
| 粘底自动跟随 / 前插历史锚点补偿被破坏 | 两者基于 `scrollHeight` 差值，占位高度=实测高度，差值不变；且新前插的行当帧未测高、必为真实渲染，锚点补偿拿到的是真实高度 |
| 观察器尚未回调时误判离屏 → 闪烁 | 两个 hidden 集合都只被 `isIntersecting === false` 写入；未报告 = 可见 |
| 包一层行容器破坏消息对齐 / 错峰动画 | 行容器用 `flex-direction:column` 保住 `.message` 的 `align-self` 语义；错峰改由 `data-enter-index` 按行序下发（`:nth-child` 会被 history-loader 等兄弟挤偏） |
| 位置型 key 导致前插历史时全列重挂 | 行 key 改成 `m:${id}` / `c:${id}`；**行内 `<ChatMessage>` 上那个带 index 的 key 也一并去掉** —— 它同样会整树重挂并击穿 memo |
| 切换/压缩使集合随会话数无界增长 | prune 覆盖全部五个集合（含 `everCollapsed`） |
| 无 observer 环境（jsdom）行为变化 | 整块不启用，退化为全量渲染；既有测试不受影响 |
| 真实滚动观感 | 见 §六"待人工冒烟"清单 |

---

## 六、执行记录（2026-09-15）

三个提交（每个都可单独回滚）：

| 提交 | 内容 |
|---|---|
| `3cd988af` | 首次交付：窗口化 + content-visibility 两条手段接入 |
| `5e50ae20` | 复核第一轮：key 稳定化补全、pinned 覆盖手段 B、布局纪元失效、IO 迟到条目过滤、prune 补全、每 token 重算、错峰改 `data-enter-index`、hook 改名 |
| `766a8430` | 复核第二轮：上表两条自身引入的缺陷（防抖 + 强制重测）+ TTS 掐断 + 空行 gap |

| 文件 | 改动 |
|---|---|
| `ChatContainer/useWindowedRows.ts` | **新增**：高度缓存 + 双 IntersectionObserver + 布局纪元失效（含防抖与强制重测）+ 两个决策纯函数 + prune 工具 |
| `ChatContainer/index.tsx` | 行容器接入：`rowKeyOf`、`scrollRoot` state、`renderRowBody`、行包裹与占位；`rowKeys` 走 `useStableMapById`、pinned 走签名 memo |
| `ChatContainer/ChatContainer.module.css` | `.message-row` / `.message-row--skippable` / 空行隐藏 / `data-enter-index` 错峰 |
| `ChatMessage/ChatMessage.module.css` | 移除失效的 `.message:nth-child()` 错峰规则（留注释指向新位置） |
| `MessageActions/index.tsx` | 卸载清理只停"本组件发起的那一次"朗读，不再掐断别人的 |
| `src/test/components/useWindowedRows.test.tsx` | **新增**（28 例）：决策纯函数 + 假观察器驱动接线 + 防抖/强制重测 |

**验证结果**：

- `pnpm typecheck` 干净
- `pnpm test`（默认门禁）：44 文件 / **391 passed**（首次交付 386 → 复核修复 390 → 二轮 391）
- `pnpm test:all`（全量）：252 文件 / **2150 passed, 51 skipped, 0 failed**
- `electron-vite build` 通过；确认 `content-visibility: auto` 与 `contain-intrinsic-block-size` 已进产物 CSS
- 超大文件守卫：本片文件均未触线（`ChatContainer/index.tsx` 680 行，阈值 800）。**注意该守卫当前整体是红的**：15 项超基线（`ChatPage.tsx` 1900 行、`bridge.ts` 3043 行等），全部与本次改动无关，属仓库存量

**已知未覆盖（需人工冒烟）**：

- 长会话（数百条）连续上滑/下滑：无空白闪烁、无滚动跳动、滚动条长度稳定
- 流式输出中上滑再滑回：流式气泡不被占位、不被跳过（行高应持续增长）
- 前插历史（"加载更早的消息"）：视口锚点不跳
- 会话切换：入场动画不重复播放、不逐条弹入
- 点「朗读」后上下滚动：不应被打断（别的行滚出窗口带不再掐断朗读）
- 拖动窗口边缘/工作台分隔条：不应卡顿（高度缓存作废已防抖）
- 含空 assistant 消息的会话（如 `394c9ea3...`）：相邻消息间距不出现 32px 双份 gap
- 改字号（TitleBar A−/A+）后继续滚动：不应出现逐行补偿式的位移
- 观察 `blinkTotal` 是否随滚动收敛（对照 `renderer-memory-probe` 的 native 采样）

---

## 六之二、09-16 实测复核（修复后首个完整运行日）

数据：`logs/perf/perf-2026-09-16.jsonl`。主窗口渲染进程 `pid 500` 连续运行 **10.5 小时**
（09-15 22:42 起，约在三个修复提交之后；09-16 09:08 结束），正文体量涨 3 倍而内存不动：

| 运行 | 时长 | 消息 / 正文 | RSS | blinkTotal | 碎片比 |
|---|---|---|---|---|---|
| **修复后** `pid 500` | 10.5 h | 73 → 204 条 / 326K → 1045K | 472 → **486 MB（+3%）** | 86 → **85 MB** | 3.3× → 2.7× |
| 修复前 `pid 3092` | 2.9 h | → 244 条 / 3186K | 663 → **1728 MB（+161%）** | → **1020 MB** | 7.0× |

**同类负载下 RSS 低 3.5 倍、Blink 预留低 12 倍，且不再随内容增长**——这正是本片要的结果。

整机口径：09-15 峰值 **12327 MB**（单个渲染进程 6943 MB、GPU 4178 MB）；09-16 峰值 7144 MB；
夜间空闲段由「2.8–3.5 GB 持续上行」变为「2.2–2.9 GB 走平」。

当天三次应用重启（09:09 / 09:23 / 10:06）**都不是内存崩溃**：每次前一个进程都留下
`应用即将退出，等待清理完成...` 的优雅退出记录，且 09:07:39 有 `[vite] hot updated`、
09:22 的日志显示当时正在查 `run-dev.cjs` 的 PID —— 是改代码后的 dev 重启。

### 同日暴露的两笔新开销（**不属于本片范围，登记备查**）

1. **dev 模式自动打开的 detached DevTools，是当天最大的单个进程。**
   `apps/windows/src/main/window/main-window.ts:279` 在 `ELECTRON_RENDERER_URL` 分支里无条件
   `openDevTools({ mode: 'detach' })`（打包版没有这一支）。进程表里除主窗口外恒定存在的
   第二个 `Tab` 就是它：一夜从 126 MB 爬到 ~400 MB，09:08 一分钟内跳到 **4463 MB**，34 秒后
   应用被重启；09-15 同位置也曾到 **6943 MB**。
   *依据*：全天应用日志里只有主窗口被创建过（宠物窗、文件预览窗均未出现），而进程表恒定两个 `Tab`；
   该 `Tab` 不是上报 `renderer.memory` 的那个（探针的 `pid` 由主进程按
   `event.sender.getOSProcessId()` 回填，且载荷带 `sessions/messages` 等会话数据，必为主窗口）。
   *证伪办法*：注掉该行重启，第二个 `Tab` 应消失。
2. **大会话首次挂载的解析开销，成为当前最大的一笔单次成本。**
   09:38 切入 249 条消息的会话（`switchSession → loadOlderMessages loaded=60 → loaded=47`）时：
   `blinkAlloc` 193 → **454 MB**（瞬时峰值）后回落到 190 MB，而 `blinkTotal` 386 → 618 MB
   **不回落**、RSS 968 → **1245 MB 不回落**；同批日志里 KaTeX 告警成串（全天 258 条，236 条挤在
   这一小时）。即 §七 第 1 项观察项的症状，也是「首帧全量挂载」取舍的实测代价：
   **进程内首次打开大会话，一次性 +300MB RSS 常驻**（当天重启后的两个渲染进程 1.4 GB / 1.2 GB
   走的都是这条路）。

---

## 七、复核结论（2026-09-15，/code-review 10 角度）

复核方式：10 个角度并行 finder 找线索 → 逐条核实（**line 号与结论都要能落到代码或数据上**）
→ 真缺陷进"已修"，其余进"不改"或"后续观察项"。两类判断都有反例：finder 关于"空 assistant
消息"的量级估计与关于"每 token MB 级垃圾"的量级估计都被实测修正过（前者我一开始判错了，
见下）。

### 已修（核实为真缺陷，12 条）

| 问题 | 性质 | 修法 |
|---|---|---|
| `everCollapsed` 未参与 prune | 与同段注释自相矛盾；只增不减，且切回旧会话时该批行的入场动画永久失效 | 补进 prune |
| 行内 `<ChatMessage key={\`${id}-${index}\`}>` 仍是位置型 key | 抵消了行 key 去位置化的收益：前插历史 / 压缩卡插入时整棵消息子树重挂，markdown+高亮全量重解析 | 删掉该 key（单元素上的 key 无意义），顺带去掉只服务于它的 `index` prop（它本身也在击穿 memo） |
| `selectSkippableKeys` 未排除 pinned | 流式行不占位、却可能被跳过；跳过期间 size containment 冻结行高，scrollHeight 停在旧值，滑回时跳变 | pinned 纳入 B 的前置条件 |
| 实测高度取整 | 占位高度与实测值不再严格相等，逐行 ≤0.5px 同号累积 | 不取整 |
| IO 迟到条目写回已 prune 的 key | `unobserve` 不清空已排队条目；幽灵记录会让行在切回会话的首帧被判离屏 | 观察回调统一按「当前仍在列表里」过滤 |
| 流式期间每 token 全量重算 | 决策 memo / prune effect / pinned 集合都随 `chatItems` 每 delta 换身份而重跑 | `rowKeys` 用 `useStableMapById`；pinned 先签名再建 Set |
| 全局重排后折叠行高度是旧值 | 应用内可达：TitleBar A−/A+ 改 `--chat-font-size`；窗口缩放、Ctrl+滚轮 zoom、拖面板同理 | 新增 `useLayoutEpoch`，字号或容器宽度变化即作废整个高度缓存 |
| 错峰规则被 `:nth-child` 的兄弟计数挤偏 | 有 `history-loader` 时行序整体后移一位，与注释声称的"按行序"不符 | 改由 `data-enter-index` 按行序下发 |
| hook 文件名 kebab-case | 违反 `docs/standards/code-style-guide.md` 的 Hook → `camelCase.ts` | 改名 `useWindowedRows.ts` |
| **折叠卸载会掐断正在播放的朗读** | `MessageActions` 的卸载清理**无条件**发全局 `voice:tts:stop-preview`；改动前行只在切会话/删消息时卸载，窗口化把"滚动即卸载"变成日常路径 → 用户一边听一边滚就会被别的行打断 | 只在本组件确实发起过朗读时才停（`startedPreviewRef`） |
| **空消息行白占一份 flex gap** | `ChatMessage` 对无内容 assistant 消息返回 null，改动前不产出元素、改动后留下 0 高包裹层，相邻两条消息之间多 16px | `.message-row:empty:not([data-row-placeholder])` 藏掉（占位行也是空的，靠该属性区分） |
| **作废高度缓存的两个副作用**（复核自身引入） | ① 挂在"宽度每变 ≥1px"上 → 拖动窗口/分隔条时逐帧清空，折叠行按帧"展开→重测→再折叠"，等于按帧重挂整份会话；② 清空后依赖 RO 重新回调，而**高度与宽度无关**的行（纯代码块、max-height 锁死的折叠块、单行短消息）重新展开时盒子尺寸没变 → RO 不回调 → 记录永远回不来 → 该行从此不再折叠 | ① 200ms 防抖：布局静默了才算一次变更；② 作废时对每个元素 `unobserve + observe` 强制补投一次初始条目 |

### 核实后**不**改（记录结论）

| 结论 | 依据 |
|---|---|
| "`rowKeyOf` 去掉 index 后重复 id 会撞 key" —— 当前数据下不成立 | 实测重复 id **0 条**（`messages.id` 是主键，客户端乐观消息用的是 `incoming-*` 前缀，两者形状不同）。上游 `useStableMapById` 本就按 id 复用，重复 id 会先在它那里出问题 |
| 首帧仍是全量挂载 | 设计取舍：折叠的前提是"先测到高度"。要消掉它需要估算高度 + 滚动锚点补偿（真正的虚拟列表语义），是另一个量级的改动。**代价明确：冷启动/切回长会话的瞬时峰值与改动前一致**，窗口化只压稳定态 |
| 折叠会丢弃行内本地 UI 状态（展开态、编辑草稿、预览） | 虚拟化的固有代价。最坏情况是编辑草稿丢失；缓解办法是行在编辑时置 pinned，属后续可选项 |
| 跨窗口带的文本选择 / Ctrl+A 复制只覆盖已挂载的行 | 同上，固有代价 |
| 折叠期间内容变化（工具结果到达、子 Agent 挂载）会让占位高度过期 | 依赖浏览器原生 scroll anchoring 兜底；行位于 ±2000px 之外，展开发生在屏外。登记为冒烟观察项 |
| `Math.round` 之外的"每 token 分配 Set/MB 级垃圾"等量级估计 | 复核实测：n=300 时单项操作是微秒级，finder 给的量级高估了一个数量级；只修了真正会造成重复工作的那几处（见上表） |
| 把 hook 迁到 `hooks/common/`、去掉 `.message-row` 包裹层改用 ChatMessage 根元素、去掉手段 B | 都是架构取舍而非缺陷，需要单独评估；本片保持现状，理由已写在 §二 |

**一处要靠证据推翻的自我更正**：本表原先有一条"空 assistant 消息在当前数据下不成立（实测 0 条）"。那个查询查的是 `content_json.content`，而两种历史形态里**都没有这个键**（新式是 `{type, parts} `，旧式是 `{type:'text', text}`），等于在量空气。按 `ChatMessage` 真实守卫条件重查（parts 空 + text 空 + 无 thinking）是 **1 / 579 条**，且就在常用会话 `394c9ea3...` 里 —— 结论反过来，已按上表修掉。

### 后续观察项（**不是待办**：真出问题再优化）

下面四项都是"做了更好、但不做也不影响正确性"的：本片已交付的能力在没有它们时是自洽的，
所以不排期、不挂账。**只有真的在冒烟或线上观察到对应症状时才回来动**——每项都写清了
"出什么症状就该动它"。（其中第 1 项的症状已在 09-16 实测中出现，见本节末。）

| 项 | 真出现这些症状就该动它 | 为什么值得做 | 量级 |
|---|---|---|---|
| markdown 解析结果缓存（按内容 hash 的 LRU，或保留已解析的元素树） | **症状已出现**（09-16 实测，§六之二第 2 条）：进程内首次打开大会话即付 ~+300MB 常驻 RSS；原定触发条件（来回滚动掉帧 / `blinkTotal` 随滚动上升）尚未单独验证 | 窗口化把"一次性大分配"换成了"滚动时反复解析"：滚回看过的消息会重付整段 remark/rehype/highlight 成本（自测口径 43 条/826KB 需 2.4s）。缓存能直接削掉这笔重复成本，也顺带减少 span 的反复分配/释放——而后者正是本片认定的碎片成因 | 与本片相当（牵动 ChatMessage 的插件/组件映射） |
| 用真实 `ChatContainer` 渲染的接线测试 | 改动折叠/跳过接线后**测试依旧全绿但行为已在真机出错** | 现有 28 例里，折叠/跳过的**组合**是在测试自建的 Harness 副本上断言的；生产侧接线（`collapsed ? null : renderRowBody`、`data-row-placeholder`、`wasCollapsed → noEnter`）零覆盖 | 测试基建（需铺 `ChatMessageActionsProvider` + workspace 上下文） |
| 行在编辑 / 朗读时置 pinned | 用户反馈"**打字打一半滚了一下就没了**"或"**听着听着被打断**" | 能同时缓解"编辑草稿丢失"与"朗读中行被卸载"两件事 | 跨组件改造（行的瞬态 UI 状态要上报到列表层） |
| 行内 `style={{height}}` 与 `ui-design-standards.md` §4.2"禁止行内样式"的冲突 | 要做全仓样式审计、或规范被升级为硬门禁时 | 规范层面的偏离 | 小，但要么给规范加"动态尺寸例外"，要么改成 ref 里直接写 DOM（更不 React） |

**第 1 项已不再是无症状的预防性条目**：09-16 实测证明打开大会话当场就付出 ~+300MB 常驻 RSS，
不需要"来回滚动"（依据见 §六之二第 2 条）。是否升级为待办是排期决策，此处不擅自改口径；
但那笔开销的量级已经由估计变成了实测。