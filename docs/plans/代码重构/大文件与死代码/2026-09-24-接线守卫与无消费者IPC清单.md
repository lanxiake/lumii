# 接线守卫与无消费者 IPC 清单

> **状态：已完成** — §3 十批已执行并并入 `pnpm verify`；§4 的 38 项已清 28，判定保留 10（`pet.getMouseIgnoreState` + `updater.*` 9 个，后者待接线勿删）；守卫已扩到七类
> 日期：2026-09-24
> 上游文档：[死代码清理清单（批次 0）](dead-code-cleanup-list.md)（前代 grep 口径）、[Lumii 源码重构瘦身分析报告](../2026-09-12-源码重构瘦身分析报告.md)
> 关联代码：`scripts/check-wiring.mjs`（守卫本体）、`scripts/wiring-baseline.json`（棘轮基线）

前代清单（批次 0）用 grep 全仓引用统计找死代码。那套方法在本仓**不可靠**——本轮的守卫改用 TypeScript Compiler API 做 AST 判定，并把判定固化成 CI 门禁。本文记录守卫的设计、三批已执行的清理，以及第六类检测（⑥ 无消费者）报出的完整待决策清单。

---

## 一、为什么正则探针在这个仓库必然撒谎

实测踩过三种形态，正则全部误报：

| 形态 | 例子 | 正则为何错 |
| --- | --- | --- |
| 模板字符串拼 channel | `ipcRenderer.invoke(\`${prefix}:startLogin\`)`（`channel-api.ts` 的工厂） | 报出 16 个「孤儿 handler」全是假的 |
| 常量表 | `ipcMain.handle(PET_IPC.switchMode, ...)`、`FILE_PREVIEW_IPC.open`（在 `shared/*.ts`，还带 `as const`） | channel 名根本不是字面量 |
| 监听封装 | `createPetEventListener(...)`、`subscribeMainEvent(...)`、channel 名放数组里遍历注册 | **形态是开放的，追不完** |

另有两条判据坑，会让入口可达性分析把 main 一半判成死代码（实测 549 → 41）：

- TS/ESM 的 `.js` 说明符指向 `.ts` 源文件（`'./voice/model-manager.js'`）
- `await import()` 动态加载

以及一条容易搞错的：类型位置的 `import('./x')` 是 `ImportTypeNode`，**不算运行时依赖**。

「没人 import」也 ≠ 死代码：barrel（`index.ts`）会 re-export 让模块看着像活的，必须从真入口做可达性遍历才分得出来。

---

## 二、守卫的七类判定

| # | 判定 | 危害 |
| --- | --- | --- |
| ① | main 注册了 handler，preload 从不引用 | renderer 永远调不到 |
| ② | preload 引用了，main 没实现 | **调用必然 reject**（活 bug） |
| ③ | main 推送事件，但无人监听 | 事件石沉大海 |
| ④ | 工具导出但没进 `ALL_BUILT_IN_TOOL_CONFIGS` | 模型看不到这个工具 |
| ⑤ | 从真入口走不到的文件 | 死文件 |
| ⑥ | preload 暴露了，但 renderer 从不调用 | 链路通却没消费者 |
| ⑦ | command handler 写了但没进分派表 | **调用必然 throw**（活 bug，且 renderer 静默吞掉） |

入口取四个：`main/index.ts`、`preload/index.ts`、`preload/webview-selection.ts`、`renderer/main.tsx`。

**棘轮机制**：存量断线记进 `scripts/wiring-baseline.json`，守卫只拦新增。`--strict` 供 CI（已接入 `pnpm verify` 与 `.github/workflows/ci.yml` 的 typecheck job），`--update` 在清理后收紧基线。

### ⑥ 的判据（刻意保守）

两级豁免：

1. AST 找 `electronAPI.<ns>.<method>` 链——覆盖 `window.` 前缀、`?.` 可选链、`as`/`!`/括号包裹
2. **方法名只要在 renderer 里以任何 `.name` 形式出现过就豁免**

第 2 条是因为 renderer 大量使用**转存**：

```ts
// renderer/hooks/business/useAgentRuntime/ 下 5 处
const api = window.electronAPI?.agentRuntime
api.isEnabled()
```

转存之后，属主既不在 AST 里也不在正则里，只能按名字兜底。代价是 `list` / `read` / `status` / `getState` 这类通用名会被全豁免（漏报），换的是不误报——**把活的判成死的，会让人直接去删活代码**。

> 因此 ⑥ 的数字是**下界**。§4 的 38 个里，B 组的 3 个（`updater.getState`、`updater.getConfig`、`news.latest`）就是 ⑥ 漏报、靠命名空间级交叉验证补出来的：`updater` 和 `news` 在 renderer 里**连转存入口都是 0 次**。

**消费者不只在 renderer 里**（2026-09-24 补）。E2E 与验证脚本会通过 CDP 在 renderer 上下文执行代码，调用藏在**字符串字面量**中——`verify/pet-sprite/check-pet-passthrough.mjs` 就是靠 `evaluate('window.electronAPI.pet.getMouseIgnoreState()')` 取证的。所以 ⑥ 也扫 `verify/` 与 `docs/test/` 下的脚本，**但只认字符串字面量内部**。

为什么强调「只认字面量」：`e2e-harness.mjs` 的注释里提过 `app.quit()`，若按纯文本匹配就会把那个真的无消费者的方法误豁免。实测双向验证过——字符串命中会豁免（30 → 29），注释命中不会。

### ⑥ 顺带补上的 ③ 盲区

③ 只查「main 推的事件 preload 有没有注册监听」，查不到「preload 注册了但 renderer 从不订阅」。实例：`updater:state-change` —— `updater-service.ts:208` 在推，`preload/index.ts` 也 `createEventListener` 了，但没人调 `updater.onStateChange`，事件实际无人接收。

### ⑦ 的判据

`agent-runtime` 的命令走**统一的** `'agent-runtime:command'` 入口（`agent-runtime-ipc.ts` 里一个大 switch），**不注册 `ipcMain.handle`**。于是：

- ① 看不到它们（它比的是 `ipcMain.handle` 注册 vs preload 引用）
- ⑥ 也看不到（renderer 经 `agentRuntimeApi.sendCommand` **透传**，不经 preload 包装）

判据一句话：**遍历 `main/ipc/agent-runtime/*-commands.ts` 的每个 `export function handle*`，看它有没有被 `agent-runtime-ipc.ts` import**。漏 case 时 switch 的 `default` 会 `throw`，而 renderer 侧普遍把 `sendCommand` 包在 `try/catch` 里返回 `null`/`[]`——**UI 静默失效，控制台也没有报错**。

单测同样抓不到：`*-commands.test.ts` 都是**直接调 handler 函数**，根本不经过 switch。

> 首个实例见 §3 第十批。值得注意的是那一批的结论**与初判相反**——补上 case 之后才发现那三个命令其实是死链（图谱 UI 入口早已移除），于是 revert 后改为清理。但守卫这一类的价值独立于此：判据本身能抓住「写了没接」这个形态。

---

## 三、已执行

### 第一批 · 守卫本体 + 43 个死文件（`923bb95a`）

- **真死组件**：`WindowControls`、`SlidingCaptcha`、`ChatSessionRail`、`SessionFileList`、`TipsBanner`（保留 `useRotatingTip.ts`）、`WikiGraphView`、`screen-record/smart-cues-generator`
  - `WikiGraphView` 的删除有产品意图背书：`WikiTab` 有一条测试断言「不显示知识图谱入口」
- **死 hooks**：`useAuditLog`、`useSystem`、`useAgentFiles`、`useLocalStorage`
- **19 个死 barrel**（`index.ts`）：无人 import，只做 re-export
- `bash-tool-evolution/index.ts`：bridge 等 3 个文件 5 处类型位置改指具体文件
- `styles/tokens/breakpoints.ts`：无人引用

### 第二批 · 悬空 HTTP API 面（`04dc9aa7`，守卫 ② 由 77 归零）

`api-server-http-api.ts` 从 94 个方法删到 23 个——**71 个是悬空的**（main 侧从未注册对应 channel）。这套「连远端 Lumii 服务」的面在本地优先的独立版里本就不存在：

| 分组 | 个数 |
| --- | --- |
| 登录注册 / 设备配对 / 用户资料 | 19 |
| 会话与消息 | 12 |
| 验证码与安全 | 3 |
| 记忆（`api:getMemories` 等，与 `api:getUserMemory` 不同） | 4 |
| 审计日志 | 7 |
| 文件管理 | 9 |
| 技能管理（`api:getSkillList` 等，与技能商店不同） | 9 |
| 系统管理 | 7 |
| 技能商店投稿 | 1 |

`preload/index.ts` 另删 6 个悬空 channel：

- **`auth` 整块**（4 个 invoke + 2 个事件监听）：main 既不实现那些 invoke，也从不发 `auth:token-refreshed` / `auth:token-expired`，renderer 侧零调用
- `news.refresh`、`skills.updateConfig`（同块的 `news.latest` 与 `skills.*` 其余方法都有活 handler，保留）

> **`skills.updateConfig` 是真 bug 不是纯死代码**：`SkillsContext.tsx:389` 确实在调它，而 main 侧 `skills-ipc.ts` 的 10 个 handler 里没有它，调用必然 reject。查下来 `updateSkillConfig` 从类型声明（68）→ 实现（378）→ 调用（389）→ context 暴露（443）**零消费**，整条链一并清掉，而不是去 main 补 handler。

### 第三批 · 守卫加第 ⑥ 类 + 首批 3 个（`611264bb`）

清掉 `api:getConfigModels`、`api:getUserSkills`、`api:getStoreRecent`：

- `getConfigModels` 的 handler 读 `bridge.getModelMapping()`，而后者已退化成 `return {} /* purpose 模式：客户端不再持有 tier→model 映射 */`——**恒返回空列表**
- 另两个的 handler 本身就是 `data: []` 空桩

**连带发现 13 个重复定义**：`api-server-api.ts`（`apiServerApi`）与 `api-server-http-api.ts`（`apiServerHttpApi`）在 `api:*` 上有**整整一组同名方法**，而 `apiServerApi` 从不整体暴露给 renderer、那些方法也从不被单独调用——纯重复。删掉 main handler 后守卫 ② 立刻把其中两个报成新增，是守卫自己抓出来的。同文件里真正在用的 `provider:*` / `usage:*` / `news:latest` / `dashboard-feed:*` 保留。

### 第四批 · A 组 4 个（renderer 本就不该有的能力）

清掉 `system.getProcessList` / `killProcess` / `launchApp` / `executeCommand`——**三层同删**：preload 桥接（`system-api.ts`）、`ElectronAPI` 类型声明、main handler（`file-system-ipc.ts`）。删完 `electronAPI.system` 只剩 `getInfo` / `getDiskInfo` / `getUserPaths` 三个只读方法。

连带清掉两个 import：`file-system-ipc.ts` 的 `validatePid`（唯一用途就是 killProcess handler 的 PID 校验）、`main/index.ts` 的 `validatePid`（**本来就没用的死 import**，与本轮无关，顺手清掉）。`security-utils.ts` 里 `validatePid` 的定义保留——`system-service.ts:751` 还在用。

守卫 ⑥ 由 35 降到 31。

### 第五批 · 修 ⑥ 的 E2E 盲区 + 清 pet 那 3 个

**先修守卫**：⑥ 原先只扫 renderer，会把「消费者在 E2E/验证脚本里」的方法误判成死代码——`pet.getMouseIgnoreState` 就是这样被冤枉的。修法是让 ⑥ 也扫 `verify/` 与 `docs/test/` 下的脚本，**但只认字符串字面量内部**（CDP `evaluate` 的代码就藏在字符串里）。⑥ 由 31 收到 30。

按守卫的规矩做了双向变异测试：往 `verify/` 塞一个含 `electronAPI.pet.updateClickRegion` 字符串的探针文件，⑥ 应从 30 掉到 29，移除后回到 30（确认字符串命中会豁免）；同时确认**注释里的提及不会**被算作消费者——`e2e-harness.mjs` 注释里提过 `app.quit()`，它仍被正常报出。

**再清 3 个**（22 处、4 个文件）：`pet.updateClickRegion` 整条链、`pet.getForceIgnoreMouse` 的 IPC 层、`pet.onForceIgnoreChanged` **连广播一起**。`pet.getMouseIgnoreState` 保留。详见 §4 D 组。

守卫 ⑥ 由 30 降到 27。

### 第六批 · D 组剩余 8 个的 IPC 层

按「**只清 IPC 层，下层留着**」执行。8 个分三类：

**被取代或重复（5 个）**

| 方法 | 现役是什么 |
| --- | --- |
| `app.quit` | renderer 走 `window.close()`；`isQuitting` 标志由主窗口 close 流程管 |
| `app.setCodingDevAcpWorkspace` | 现役是 `setCodingDevActiveProject`；旧的 `codingDevAcpWorkspace` 配置项只在「无活动项目时回退」时读 |
| `file.readAsBase64` | 注释写「用于图片附件」，现在走 `file-preview-window` 独立预览窗口 |
| `autonomous.getPendingGoals` | `getGoals` 是它的超集（全状态 + `reflectionId`） |
| `clipboard.readText` | renderer 只用 `navigator.clipboard` **写**，从不读 |

> `clipboard.readText` 有个容易看走眼的地方：`test/setup.ts` 里确实有个 `readText` mock，但那是 **`navigator.clipboard`** 的（同一段注释就写着 `// Mock navigator.clipboard`），跟 IPC 无关。

**死机制（2 个）**

`workspace.sessionRenamed` + `workspace.ensureThreadDir`——顺着它们查出 `DirectoryManager` 的 **thread 目录那一整套已死**：`resolveThreadDirectories` 只被 `ensureThreadDirectories` 内部调、`renameTaskDirectory` 只被这俩 handler 调、`resolveTaskDirectory` **本来就没有调用者**。而隔壁 `ensureWorkspaceSubDirs` 是活的（`workspace:ensureDir` 在用，Files 面板在调）——说明「按 threadId 分目录」的设计已被「工作空间根目录 + `securityUtils.addAllowedBasePath` 白名单」取代。

**能力活着、只是 renderer 用不着（1 个）**

`cloudSync.resolveConflict`——底层 `CloudSyncManager.resolveConflict` 有 **2 个非 renderer 的调用者**（`bridge-tool-registrar-sync.ts` 的 Agent 工具、`app-ui-control/server.ts` 的控制口），renderer 侧用的是另一个方法 `retryConflict`（交给 Agent）。所以**只删 IPC 层**。

与 A 组的区别：A 组是「renderer 本就不该有」（安全收窄），这组是「renderer 用不着」。

**连带清理**

- `registerAppQuitHandler` **整个函数**——它只注册 `app:quit` 一个 handler，且形参 `isQuittingGetter` 压根没用到
- `ipc-handlers-registry.ts` 的 import / 调用 / deps 里的 `isQuittingGetter` + `setIsQuitting`，以及 `main/index.ts` 传这两个字段的地方（`isQuitting` 变量本身保留）
- `ResolveStrategy` / `ResolveChoice` 类型——只服务 `resolveConflict`

**过程记录**：改完第一次跑守卫报了 **① 新增 1**——漏了 `app:setCodingDevAcpWorkspace` 的 handler，它在 `coding-dev-ipc.ts` 而不在我以为的 `file-system-ipc.ts`。是守卫自己抓出来的。

守卫 ⑥ 由 27 降到 19。

### 第七批 · `news.latest`（B 组）

清掉 `news:latest` 的 IPC 层：preload 的 `news` 块（类型 + 实现）、`apiServerApi.getLatestNews`、main handler，连带 3 个 import（`NewsSnapshot` 类型、`api-ipc.ts` 的 `readNewsSnapshot`、`main/index.ts` 一个本来就没用过的死 import）。

依据是两个 handler 的注释对比——`news:latest` 写「概览页资讯」，而 `dashboard-feed:latest` 写「Dashboard **通用 feed（资讯只是默认 feed**，后续工作流可替换其内容）」，后者是前者的泛化版。

**连带发现（待决策）**：删掉那两个 import 后，守卫 ⑤ 报出 **`main/news-store.ts` 从入口不可达**——它的 `readNewsSnapshot` / `runNewsPipeline` 只剩自己的测试在调，`fetchLatestNews` 连测试都没有。而 `dashboard-feed-store.ts:369` 的注释写着「读取指定 feed 的最新快照；**news 会兼容旧版 `~/.lumii/news/latest.json`**」，`ensureDashboardFeedMigrated` 里也有 `legacyNewsPath()`——**数据层其实也被完全取代了**，只是原来那两个 import 让它看起来还活着。按「只清 IPC 层」的约定，`news-store.ts` + `news-store.test.ts` 本轮未动。

> 同批 ⑤ 还报出 `main/agent-runtime/pet-dispatch.ts` 不可达——它的引用只出现在 `bridge.ts:66` 与 `local-companion-handler.ts:134` 的**注释**里，是并行会话 pet 工作的产物，与本轮无关。

**后续（同日执行）：`news-store.ts` 已清**。删前确认过——它的全部导出（`NewsItem` / `NewsSnapshot` / `NewsPipelineDeps` / `fetchLatestNews` / `readNewsSnapshot` / `runNewsPipeline` / `__testables`）在整个 `apps/windows/src` 里零引用，唯一的 import 来自它自己的测试文件。两个文件一起删（`.ts` + `.test.ts`）。

连带改了两处文字引用：

- `README.md` 的 DashboardPage 功能列表：`NewsFeed + news-store` → `NewsFeed + dashboard-feed`（`NewsFeed` 组件本身是活的，在 `DashboardPage/components/NewsFeed/`）
- `seed-cron-jobs.ts` 注释里指向 `ensureNewsCronJobSeeded` 的历史说明（那个符号已不存在）

守卫 ⑤ 随之归零。

### 第八批 · C 组的甲类 + 乙类（5 个）

C 组 12 个先查清了分层——**⑥ 报的「无消费者」在这里掩盖了三种完全不同的情况**。

先补一条判定上的坑：`agentRuntimeApi` 有 `sendCommand: send` 能**透传任意 command**，所以不能只看 `electronAPI.agentRuntime.<method>`，必须逐个 action 名搜。搜完发现 12 个 action 里**只有 `runtime:ping` 在 renderer 出现过**，而且走的是透传（`bridge-init.ts` 的 `.sendCommand({ type: 'runtime:ping' })`）——`isAvailable()` 那个包装方法确实没人用，但它包的 command 是活的。

| 类 | 个数 | 特征 | 处理 |
| --- | --- | --- | --- |
| 甲 | 3 | command 有别的消费者（控制口白名单 / renderer 透传），下层也活 | **只删 preload 包装** ✅ 本批 |
| 乙 | 2 | command 死了，但 bridge 方法活着（main 内部驱动） | 删 preload + 协议 + 分派，**保 bridge** ✅ 本批 |
| 丙 | 7 | 从 IPC 一路空到数据层 | 待决策（§4 C 组） |

**甲类**（`getFeatureFlags` / `getInstances` / `isAvailable`）：控制口确实能到这些 command——它在 `app-ui-control/server.ts:984` **直接调 `handleCommand`**，和 IPC handler 是同一个分派函数、不经 preload。所以删 preload 那 3 个方法对控制口零影响。

**乙类**（`createInstance` / `createInstanceById`）：bridge 方法在 main 内部被大量调用（会话启动 `bridge.ts:532`、子 agent `bridge-lifecycle.ts:262`、cron `cron-scheduler.ts:1096`、目标执行 `bridge.ts:1231`），**实例创建完全由 main 自己驱动**。删的是「IPC 这条路径」——连带 `shared/agent-runtime-commands.ts` 的 2 个 command 类型与联合成员、`agent-runtime-ipc.ts` 的 2 个 case 与 import、`agent-commands.ts` 的 2 个 handler。

> TSC 顺带抓到一处：`ElectronAPI` 的 `agentRuntime` 是**手写类型**（不是 `typeof agentRuntimeApi`），所以 `preload/index.ts` 里还有 5 条声明要同步删。这和 `autonomous` / `cloudSync` 用 `typeof` 的写法不一致。

守卫 ⑥ 由 19 降到 14（剩丙类 7 + `updater.*` 7）。

### 第九批 · C 组的丙类（7 个，清到数据层）

**这是唯一一组「整条链全死」的**：preload 包装 → command 类型 → main 分派 → `bridge.ts` 转发 → 下层 `AgentDefinitionStore` / lifecycle，**每一层都只被上一层调用、没有任何旁路消费者**。12 个文件，-353 / +4 行。

| 方法 | 删到哪一层 |
| --- | --- |
| `setFeatureFlags` | preload + command + 分派 + `bridge.setFeatureFlags` |
| `listCachedAgentDefinitions` | 全链 → `store.listCachedRows()` |
| `removeCachedAgentDefinition` | 全链 → `store.removeCached()` |
| `clearCachedAgentsOlderThan` | 全链 → `store.removeOlderThan()` |
| `clearAllCachedAgentDefinitions` | 全链 → `store.clearAllCached()` |
| `refreshCachedAgentDefinition` | 全链 → `store.refreshOne()` |
| `getLifecycleSnapshot` | 全链 → `lifecycle.getLifecycleSnapshot()` + `AgentLifecycleSnapshot` 类型 |

**下层连锁**：删掉 4 个缓存管理入口后，`AgentDefinitionStore` 的 `removeCached` / `removeOlderThan` / `clearAllCached` / `refreshOne` / `listCachedRows` **全部失去调用者**——`removeCached` 只被 `removeOlderThan` 调，随它一起死。包内测试只覆盖 `get` 与 `syncUserAgents`/`getSyncStatus`，不受影响。store 保留 `get` / `upsertFromApiRecord` / `syncUserAgents` / `getSyncStatus` 四个方法：**能力收窄为「只读缓存 + 同步」，不再有删除/清理入口**。

**`setFeatureFlags` 顺带消掉一个从未暴露的 bug**：它是 `this.featureFlags = createFeatureFlags(flags)`——**换了个新对象**，而 `bridge.ts:1964/2005` 早已把旧对象按引用传给了 `promptDispatcher` 与 `instanceFactory` 的 deps。所以真调用它，flag 改动对那两处**静默不生效**。唯一入口是 IPC 且 renderer 从不用，所以一直没暴露。删掉正好消除隐患。删后 feature flags 由 `createFeatureFlags()` 在构造期定格，运行时不改写——这与控制口「读放行、写明确被拒」的既有取向一致。

**两条被推翻的「还是活的」证据**：

- `preload/index.ts` 与 `bridge-types.ts` 都写着「**DetailPanel 运行状态**」的注释——但 `DetailPanel.tsx` 是**纯展示组件**（只接 `agent` prop + 一个管 Escape 的 `useEffect`），**从不碰 `electronAPI`**。注释是过时设计意图的残留。同族教训见 §4 D 组的「别信注释里的『遗留』二字」。
- `agent-runtime-ipc.ts` 的 `QUIET_COMMANDS`（日志降噪名单）里有 `agentInstance:lifecycleSnapshot`——case 删掉后该条目永远不可达，一并清掉。

**连带清理**：`bridge.ts` 的 `AgentLifecycleSnapshot` import 与 re-export、`agent-runtime/index.ts` 的 re-export、`command-allowlist.test.ts` 里 `'runtime:featureFlags:set'` 那条断言（action 已不存在，留着就退化成和 `totally:unknown` 一样的断言）。

守卫 ⑥ 由 14 降到 7（剩下的全是 `updater.*`）。

> **过程教训（行尾）**：本轮的批量删除脚本用 `\n` 归一化读入后原样写回，把这 12 个文件的工作区行尾从 CRLF 变成了 LF。`core.autocrlf=true` 让 `git diff` **完全看不出**（索引两侧都归一化成 LF），只有 `git ls-files --eol` 能查出来——同目录未改动的文件是 `w/crlf`，被脚本碰过的是 `w/lf`。已恢复。**写批量改写脚本必须保留原行尾**，不能靠 diff 自查。

### 第十批 · 撤销 wiki 分派修复 + 清除知识图谱（并加守卫第 ⑦ 类）

**缘起**：写 ⑦ 的探针时发现 `wiki-commands.ts` 有 4 个 handler 没进分派表，其中 3 个 renderer 在调，看起来是 3 个活 bug，于是补了 case（`b62e0f80`）。

**随后被推翻**：查实「renderer 在调」这个判断不够——那三个调用点是 `useWikiPage` 里的**死 hook 函数**：

| 证据 | 内容 |
| --- | --- |
| `useWikiPage.ts` 的 `getGraphData` / `extractEroFromSources` / `listEntitySources` | 全仓只出现在**定义处 + return 对象**，**没有任何组件解构使用** |
| `WikiGraphView` | 已在第一批清理中删除（当时就有产品意图背书） |
| `WikiTab.test.tsx` | **两条**断言钉着「不显示知识图谱入口」（L140、L405） |

所以它们是**死链**（UI 入口早就断了），不是活 bug。`b62e0f80` 已 revert（`bafd6f0d`）。

**然后清理**：11 个文件 + 2 个整文件（另含 `agent-runtime-ipc.ts` 的一处类型修补，见下）。

| 层 | 删了什么 |
| --- | --- |
| `wiki-commands.ts` | 4 个 handler（`WikiGraphData` / `WikiEroList` / `WikiEroExtract` / `WikiEroEntitySources`）+ 3 个 import |
| `shared/agent-runtime-commands.ts` | 4 个 command 类型 + 4 个 union 成员 + 4 个 `AgentRuntimeCommandResult` 分支 |
| `useWikiPage.ts` | 3 个死 `useCallback` + return 项 + 4 个类型 import |
| `useWikiPage.types.ts` | 5 个类型（含零消费的 `WikiObservationItem` / `WikiGraphLayer`） |
| `useWikiPage/index.ts` | 6 条 type re-export |
| `command-allowlist.ts` | `'wiki:graph:data'`（注释还写着「CLI 侧 wiki graph 一直有此命令」） |
| `wiki-commands.test.ts` | 4 个测试 + 相关 import |
| `packages/agent-runtime` | **整文件删** `wiki-graph.ts` + `wiki-graph.test.ts`，连带两个 barrel 的 re-export |
| `resources/app-ui-cli/commands.mjs` | `wiki graph` 整条 CLI 命令（**是测试抓出来的**，见下） |

**「清干净」是既有测试抓出来的**：删掉白名单里的 `wiki:graph:data` 后，`command-allowlist.test.ts` 的「CLI 注册的 /command 类型全部在白名单内」立刻失败——`resources/app-ui-cli/commands.mjs` 里还留着 `wiki graph` 这条 CLI 命令（它发的正是 `wiki:graph:data`）。**这说明白名单与 CLI 设计表之间有交叉断言守着**，删一侧必须删另一侧。

> 注意：CLI 侧这条命令**守卫的 ⑦ 也看不到**——`SCAN_DIRS` 只覆盖 `apps/windows/src/{main,preload,shared,renderer}`，不含 `resources/`。守卫的覆盖面止于应用源码，CLI 设计表由它自己的测试守。

**失效用例一并清理**（`28f93291`）：清理后 `docs/test/lumii-cli/wiki/run-wiki-cli-suite.mjs` 里有 7 个用例不可执行——`P2-G01~G05` 调 `wiki graph`、`P2-R02` 调 `wiki:ero:list`、`P2-R01` 调 `wiki ero bootstrap`。其中 `P2-R01` 更早就有问题：那条命令早已不存在，而它的断言结构（对任何非 `rate_limited` 结果都记 `PASS`）让它**几乎总是通过**——一个不会失败的失效测试。已删 7 个用例块与只服务 G01/G04 的 `arch` 变量。

配套文档：`wiki-p2-test-cases.md` 删对应章节与覆盖表两行；`wiki-p3-test-cases.md` **整份删除**（G/E/X/B 四组全部走已删命令，无一条可执行），并同步清掉 `docs/test/lumii-cli/README.md` 的表格行与 `docs/wiki/10-knowledge-index/01-文档快速索引.md` 里的引用。

**下层边界（关键）**：`WikiEroRepo` / `WikiEroExtractor` **必须保留**——它们由非 UI 路径驱动：

```
local-companion-handler.ts:93  '__wiki_ero_extract__'（注释：cron / 手动触发）
  └─ L167 分支 → deps.runWikiEroExtract()
       └─ bridge.ts:1779 → new WikiEroRepo + new WikiEroExtractor → extractFromSources()
```

这是 **C 组乙类的同型**：IPC 层死、下层活（main 内部驱动）。删完 `listEntities` 仍被 `wiki-ero-extractor.ts:267` 用（名称→id 映射）；但 `listRelations` / `listActiveObservations` / `listEntitiesBySources` / `listSourceIdsForEntity` **失去了生产消费者**，只剩包内单测覆盖——按本轮「只清到 IPC 层」的纪律未动，待将来做图谱 UI 时启用或另判。

**TSC 顺带抓到一处**：删掉 4 个 command 后 switch 已穷尽 `AgentRuntimeCommand` 全部成员，`default` 里的 `command` 被收窄成 `never` → `command.type` 报 TS2339。**不能因此删掉 default**——那会让将来新增 command 忘加 case 时静默返回 `undefined`，正是刚踩的坑。改为 `(command as { type?: string }).type` 保留运行时兜底。

**守卫第 ⑦ 类**（同批加，判据见 §2）：变异测试双向验证过——往 `runtime-commands.ts` 注入一个没被 import 的 `handleMutationProbe` → 报出（含 `文件:行号`）；移除 → 归零。

守卫 ⑦ 由隐藏的 3 项归零。这一批的脚本也验证了行尾纪律：全程 `w/crlf`。

---

## 四、⑥ 清单：38 个无消费者 IPC

判定口径见 §2 末。**A、C、D 组已于 2026-09-24 执行**（§3 第四、五、六、八、九批）；B 组的 `news.latest` 已清，`updater.*` 判定为待接线、勿删。

**这 38 个与第二批那 3 个性质不同**：那 3 个 main 侧是空桩，删干净没有损失；这 38 个的 main 实现**大多是真实逻辑**（`system.executeCommand` 真的会执行命令、`updater` 真的会下载安装）。所以删之前要先分清删的是「死代码」还是「能力」。

### A 组 · renderer 本就不该有的能力（4 个）— ✅ 已执行

| 方法 | 作用 | main 侧实现（已随 IPC 一起删） |
| --- | --- | --- |
| `system.getProcessList` | 列系统进程 | `SystemService.getProcessList()` |
| `system.killProcess` | 结束指定 PID | `validatePid()` + `SystemService.killProcess()` |
| `system.launchApp` | 启动外部程序（可带参数） | 参数类型校验 + 启动 |
| `system.executeCommand` | **执行 shell 命令**，返回 stdout/stderr | 类型 + 长度（≤1000）校验 + 执行 |

同族在用的 3 个**全是只读信息**：`getInfo`、`getDiskInfo`（Dashboard 用）、`getUserPaths`（Files 用）。死的这 4 个都是**动作类**。

**执行后的下层残留**：`SystemService` 的 `getProcessList` / `killProcess` / `launchApplication` 三个方法随 IPC 一起失去了唯一调用者，但**本轮未动**——它们是 `SkillExecutionContext.systemService: SystemService` 暴露给技能的面，是否收窄需要单独判断。注意 `executeCommand` **必须保留**：`skill-runtime-types.ts:221` 的内置技能在用它。

### B 组 · 整组不可达（10 个）

**两种性质完全不同，别一起处理**：

| 方法 | 结论 |
| --- | --- |
| `news.latest` | ✅ **IPC 层已清**（§3 第七批）——被 `dashboard-feed:latest` 的泛化版取代 |
| `updater.*`（9 个） | ❌ **待接线，勿删**——见下 |

#### `updater.*` 为什么不能删

三层各自的真实状态不同：

| 层 | 状态 |
| --- | --- |
| main `updater-service.ts` | ✅ **完整实现**（真实调用 electron-updater），且**内部自洽**——每个方法都有类内部调用者：`updateConfig({autoCheck})` → `this.startAutoCheck()/stopAutoCheck()`（L242/244）、autoUpdater `update-available` → `this.downloadUpdate()`（L139）、`update-downloaded` → `this.installUpdate()`（L171）、定时器 → `this.checkForUpdates()`（L313/317） |
| preload `electronAPI.updater.*` | ✅ 9 个方法齐全 |
| renderer `UpdaterView` | ⚠️ **UI 骨架 + 假实现** |

`UpdaterView.tsx` 三个 `TODO: 实现...逻辑`：检查更新是 `await new Promise(r => setTimeout(r, 2000))` 然后**谎报「已是最新版本」**，下载是 `for (i=0; i<=100; i+=10)` 睡 200ms 的进度动画，安装只弹一句 toast。

**但它不是死组件**——`SettingsPage.tsx:758` 真渲染 `<UpdaterView standalone />`。

**卡点写在 `main/index.ts:525`**：

```ts
autoCheck: false, // 发布渠道未配置前禁用，避免请求不存在的仓库
```

所以这不是「UI 没做」，而是「**两边都就绪，只差发布渠道和一次接线**」。删 `updater.*` 等于把将来的接线口拆掉。

> ⚠️ 附带一个**不属于本轮清理**的问题：现在用户点「检查更新」会看到 2 秒 loading 然后「✅ 已是最新版本」——**假 UI 在说谎**。要么接线、要么先把入口藏起来，属产品决策。

### C 组 · agentRuntime 的运行时管理面（12 个）— ✅ 全部已处理

全部走统一入口 `agent-runtime:command`，各带一个 action。逐个查过 action 名（含 `sendCommand` 透传）后分成**三类，处理方式完全不同**：

| 方法 | action | 控制口白名单 | main 内部另有消费者 | 结论 |
| --- | --- | --- | --- | --- |
| `getFeatureFlags` | `runtime:featureFlags:get` | ✅ | ✅ 2 处 | 甲类 ✅ 已清 |
| `getInstances` | `agentInstance:list` | ✅ | ✅ 4 个 channel adapter | 甲类 ✅ 已清 |
| `isAvailable` | `runtime:ping` | ✅ | ✅ renderer 透传 | 甲类 ✅ 已清 |
| `createInstance` | `agentInstance:create` | ❌ | ✅ main 多处 | 乙类 ✅ 已清 |
| `createInstanceById` | `agentInstance:createById` | ❌ | ✅ main 多处 | 乙类 ✅ 已清 |
| `setFeatureFlags` | `runtime:featureFlags:set` | ❌ **明确被拒** | ❌ | 丙类 ✅ 已清 |
| `listCachedAgentDefinitions` | `agentDefinition:cacheList` | ❌ | ❌ | 丙类 ✅ 已清 |
| `removeCachedAgentDefinition` | `agentDefinition:cacheRemove` | ❌ | ❌ | 丙类 ✅ 已清 |
| `clearCachedAgentsOlderThan` | `agentDefinition:cacheClearOlder` | ❌ | ❌ | 丙类 ✅ 已清 |
| `clearAllCachedAgentDefinitions` | `agentDefinition:cacheClearAll` | ❌ | ❌ | 丙类 ✅ 已清 |
| `refreshCachedAgentDefinition` | `agentDefinition:cacheRefresh` | ❌ | ❌ | 丙类 ✅ 已清 |
| `getLifecycleSnapshot` | `agentInstance:lifecycleSnapshot` | ❌ | ❌ | 丙类 ✅ 已清 |

甲乙两类的判据与删法见 §3 第八批，丙类见 §3 第九批。

**丙类 7 个的特征是「从 IPC 一路空到数据层」**：preload 包装 → command 类型 → main 分派 → `bridge.ts` 的转发 → 下层 `AgentDefinitionStore` / lifecycle 的方法，**每一层都只被上一层调用，没有任何旁路消费者**。这与前几轮的「IPC 层死、下层活」完全不同，所以它是唯一一组能一路清到底的。

> 其中 `setFeatureFlags` 值得单说：控制口白名单里有 `runtime:featureFlags:get`（读放行），而 `runtime:featureFlags:set` 被列在 `command-allowlist.test.ts` 的**「拒绝高危命令」**里，和 `files:delete`、`agentInstance:prompt` 并列——**读放行、写明确被拒**，是个有意的安全边界。

**这 7 个与 `updater.*` 的关键区别是没有任何 UI 骨架**：`updater` 至少有 `UpdaterView`（哪怕是假的），丙类连壳都没有——纯运维排障面（列/删/按时限清理/清空/刷新缓存、生命周期快照），从 IPC 到数据层零消费者。

### D 组 · 零散（12 个）— ✅ 全部已处理

`pet` 那 4 个见下一小节；其余 8 个的 **IPC 层**已清（§3 第六批），下层一律留着：

| 方法 | 结论 | 现役是什么 / 留下什么 |
| --- | --- | --- |
| `app.quit` | ✅ IPC 层已删 | renderer 走 `window.close()`；`isQuitting` 变量保留（主窗口 close 流程在用） |
| `app.setCodingDevAcpWorkspace` | ✅ IPC 层已删 | `setCodingDevActiveProject`；**配置项 `codingDevAcpWorkspace` 保留**（无活动项目时回退读它 + `MTBOT_CODING_DEV_ACP_WORKSPACE` 环境变量） |
| `workspace.sessionRenamed` | ✅ IPC 层已删 | **下层 `DirectoryManager` 的 thread 目录那套全部保留**，待单独判断 |
| `workspace.ensureThreadDir` | ✅ IPC 层已删 | 同上 |
| `file.readAsBase64` | ✅ IPC 层已删 | `file-preview-window` 独立预览窗口 |
| `clipboard.readText` | ✅ IPC 层已删 | renderer 从不读剪贴板 |
| `autonomous.getPendingGoals` | ✅ IPC 层已删 | `getGoals`（超集） |
| `cloudSync.resolveConflict` | ✅ **仅** IPC 层已删 | **底层必须保留**——Agent 工具（`bridge-tool-registrar-sync.ts`）与控制口（`app-ui-control/server.ts`）在用 |

#### pet 的 4 个（2026-09-24 已处理）

`pet` 总共 34 个方法、30 个在用，死的这 4 个全跟**鼠标穿透**有关。

| 方法 | 结论 | 依据 |
| --- | --- | --- |
| `pet.updateClickRegion` | ✅ **整条链已删** | main 侧是 **no-op**，注释写明「遗留 API：setShape 会裁剪绘制，不再使用」。穿透机制已换成 hover 聚合 + `setMouseIgnoreEvents`（`applyMouseIgnoreState`），`setShape` 因为会裁剪像素导致模型不可见而被弃用。连带 `PetClickRegion` 类型一起删 |
| `pet.getForceIgnoreMouse` | ✅ **IPC 层已删** | `toggleForceIgnoreMouse` 返回切换后的状态，无需单独读。main 的 `isForceIgnore()` 仍有调用者（`isPetForceIgnore()` 供系统托盘），保留 |
| `pet.onForceIgnoreChanged` | ✅ **已删（含广播）** | main 侧有两个消费者要分清：**回调** `deps.onForceIgnoreChanged` 供**系统托盘**更新菜单（保留）；**事件广播** `broadcastForceIgnoreChanged` 的目标是注释里写的「同步控制面板 UI」，而控制面板从没订阅过。⚠️ 广播**必须连删**——只删 preload 监听会让守卫 ③ 报「推送无人监听」 |
| `pet.getMouseIgnoreState` | ❌ **保留** | **有真消费者**：`verify/pet-sprite/check-pet-passthrough.mjs` 与 `check-notice-focus.mjs` 通过 CDP 调它取证。这是有意保留的诊断 getter（见 §2「消费者不只在 renderer 里」） |

> 附带一条经验：`updateClickRegion` 与 `reportHover` 在代码里**都标着「遗留」**，但只有前者是真遗留——`reportHover` 在 PetCanvas / PetControlDock / PetSpeechBubble / PetModeShell 里密集使用（因为 hover 上报正是现行穿透机制的输入）。**别信注释里的「遗留」二字，要查调用点。**

---

## 五、删除方案与影响

两种删法，影响不同：

| 方案 | 做法 | 后果 |
| --- | --- | --- |
| 只删 preload 侧 | 删 `electronAPI` 上的入口 | main handler 立刻变成孤儿 handler，**守卫 ① 会报出来** |
| 两处都删 | preload + main handler | 完整清理，但等于删掉这些能力本身 |

**建议分组处理**：

- **A 组（4 个）**：✅ **已执行**（见 §3 第四批）。这是能力收窄，与 B 层「不暴露文件操作」的既有取向一致
- **B 组（10 个）**：`news.latest` ✅ 已清（§3 第七批）；`updater.*`（9 个）❌ **勿删**——待接线，证据见 §4 B 组
- **C 组（12 个）**：甲类 3 + 乙类 2 ✅ 已清（§3 第八批，command 与 bridge 能力全保留）；丙类 7 ✅ 已清（§3 第九批，**这是唯一一组一路清到数据层的**——连带 `AgentDefinitionStore` 的 5 个方法与 `AgentLifecycleSnapshot` 类型）
- **D 组（12 个）**：✅ **已处理**（§3 第五、六批，均「只清 IPC 层、下层留着」；其中 `pet.getMouseIgnoreState` 判定为保留）

---

## 六、后续

- **§4 的 38 项：已全部处理完毕**——清 28 个，判定保留 10 个（`pet.getMouseIgnoreState` 有 verify 脚本消费者；`updater.*` 9 个待接线）。守卫基线：`孤儿 handler 0｜悬空引用 0｜无人监听 0｜未注册工具 2｜不可达文件 0｜无消费者 7｜未接分派 0`，剩的 7 个无消费者全是 `updater.*`
- **守卫第 ⑦ 类**（§3 第十批同批加）：查「command handler 写了但没进分派表」。首个实例正是促成它诞生的那一个——虽然最终结论是死链而非 bug，但判据本身独立有效，且这是六类唯一没覆盖的形态
- **知识图谱的清理边界**（§3 第十批）：`WikiGraphBuilder` 及整条 IPC/renderer 链路已清；`WikiEroRepo` / `WikiEroExtractor` 保留（由 `__wiki_ero_extract__` 指令驱动）。但 `WikiEroRepo` 的 4 个读方法（`listRelations` / `listActiveObservations` / `listEntitiesBySources` / `listSourceIdsForEntity`）现在只剩单测覆盖——将来要么做图谱 UI 用起来，要么单独判断
- **丙类清完后的能力缺口（待产品确认）**：`AgentDefinitionStore` 现在只能读与同步，**没有删除/清理缓存的入口**；feature flags 也只能在构造期由 `createFeatureFlags()` 定格。若将来需要「清缓存排障」，得重开这条链而不是补一个局部 handler（command 类型与分派都要一起加回来）
- **`news-store.ts` 已清**（§3 第七批后续）——数据层被 `dashboard-feed-store` 完全取代（含 `~/.lumii/news/latest.json` 的旧路径兼容），连同它那个「测死代码」的测试一起删了
- **第六批的下层残留**：`DirectoryManager` 的 thread 目录那套（`resolveThreadDirectories` / `ensureThreadDirectories` / `renameTaskDirectory` / `resolveTaskDirectory`）已全部无调用者，按本轮「只清 IPC 层」的约定留着
- **A 组的下层残留**：`SystemService.getProcessList` / `killProcess` / `launchApplication` 已无调用者，但它们是 `SkillExecutionContext` 暴露给技能的面——要收窄的话是一次独立的决策，别顺手删（`executeCommand` 在同族里是活的，正好说明这一层不能按「有没有 IPC 调用」来判断）
- **④ 剩余 2 项**：`bingSearchToolConfig`、`wikiCaptureToolConfig` 导出了但没进 `ALL_BUILT_IN_TOOL_CONFIGS`，尚未确认是否有意不注册
- **⑥ 判据可提升**：目前靠「名字出现过就豁免」兜住转存，代价是通用名全漏。若要更准，需在 `collectRendererUsage` 里加**文件级别名追踪**（`const api = electronAPI?.<ns>` → 该文件内 `api.<method>` 计入 chained）。注意保持保守取向：宁可漏报，不可误报
- **守卫自身的回归**：改判定逻辑后，用注入假 channel 的方式做变异测试（造一个假的悬空 invoke → 确认被报 → 回滚），别只看「跑通了」
