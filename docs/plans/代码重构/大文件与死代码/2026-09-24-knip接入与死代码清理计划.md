# knip 接入与存量死代码清理计划

> **状态：进行中** — 已完成：K0（unused files 9 → 3）、K1（unlisted 58 → 1）、K3（unresolved 7 → 0）、K2-1/2/4a（真死 95 → 68）、K2-3/5、K2-6a/6b。待办：**K2-4b**（约 66 条）与 **K4**（棘轮入 verify）。K2-6c 经实测判定为独立工作（见 §4.7）
> 日期：2026-09-24
> 上游文档：[接线守卫与无消费者 IPC 清单](2026-09-24-接线守卫与无消费者IPC清单.md)、[源码重构瘦身分析报告](../2026-09-12-源码重构瘦身分析报告.md)
> 关联代码：`knip.jsonc`（新）、`scripts/check-wiring.mjs`、`scripts/wiring-baseline.json`、`apps/windows/electron.vite.config.ts`

接线守卫解决了「跨进程名字配对」，但**看不见模块图可达性和依赖卫生**。本文记录接入 knip 的实测归因、任务清单与执行进度。任务带 ID，状态与证据逐条回填。

---

## 一、为什么是加法不是替代

| 判定 | `check:wiring` | knip |
| --- | --- | --- |
| ① main handler 孤儿 / ② preload 悬空 / ③ 推送无人监听 | ✅ | ❌ |
| ⑥ preload 暴露无消费者 / ⑦ command 未接分派 | ✅ | ❌ |
| ④ 工具导出未注册 / ⑤ 文件不可达 | ✅ | ⚠️ 部分重叠（不同引擎） |
| 导出/类型/enum 成员未使用 | ❌ | ✅ |
| 未声明的依赖、声明未用的依赖、循环依赖 | ❌ | ✅ |
| 扫描范围 | 仅 `apps/windows/src/{main,preload,shared,renderer}` | 全仓含 `packages/` |

最后一行是实测补上的盲区：`packages/agent-runtime/src/kernel/index.ts` 是个没人 import 的 3 行 barrel，守卫扫不到，knip 抓到了。

---

## 二、首轮实测与归因（2026-09-24）

### 2.1 两处「够不到」让原始数字不可用

| 现象 | 证据 | 处理 |
| --- | --- | --- |
| **renderer 整条主链被判不可达** | `knip --debug` 里插件给出的 entry 是裸文件名 `apps/windows/main.tsx` / `early-splash.ts`（glob 落空）；正确的 html entry 在上一行 | 在 `knip.jsonc` 显式补 `src/renderer/main.tsx`、`early-splash.ts`。修后 unused files **282 → 9** |
| `docs/`、`verify/`、`scripts/` 被当成模块图成员 | 187 个文件报 unused files | 分别标 `ignore`（前者）与 `entry`（后两者） |

> renderer 那条是 **knip 6.38.0 electron-vite 插件的 bug**：`getHtmlScriptEntries()` 单独调用（喂绝对路径或仓库相对路径）返回正确结果，所以丢在插件→knip 内部那一步。值得上游报 issue。

### 2.2 归因后的存量

| 类型 | 条数 |
| --- | --- |
| exports + types | 1453 |
| unlisted（未声明依赖） | 58 |
| devDependencies（声明未用） | 10 |
| unresolved（解析不了的导入） | 7 |
| dependencies（声明未用） | 5 |
| binaries | 4 |
| files（不可达文件） | 9 |
| **合计** | **1546** |

### 2.3 那 1453 条不是同一种东西

按**引用形态**（import/export 语句本身，非「名字出现过」）全量归类：

| 类别 | 条数 | 占比 | 判据 | 处置 |
| --- | --- | --- | --- | --- |
| B 仅文件内使用 | 705 | 48.5% | 符号在自己文件里被用，只是没人 import | `export` 关键字多余，K5 |
| C 从别处 import | 267 | 18.4% | 有人 import，但来源是另一个模块 | barrel 那层多余，K5 |
| D 冗余再导出 | 287 | 19.8% | 只在 barrel 的 `export ... from` 里出现 | K5 |
| **A 真死** | **95** | **6.5%** | 全仓库无人 import，自己文件里也只有声明 | **K2** |
| E 假阳 | 2 | 0.1% | 有人正是从这个模块 import | 忽略 |

> **一条自我纠正**：最初用「全仓库搜符号名」当判据，得到 46% 假阳——**那个数是错的**。文本判据只看名字出现，不看从哪个模块来（`getScreenRecordService` 在别处出现，但人家 import 的是 `./accessor` 而非被标记的 barrel）。换成按 import/export 语句归类后假阳只剩 2 条。**knip 判定本身可信**，是我的判据粗。

真死 95 条的分布：`src/main 41 / src/renderer 24 / packages/* 16 / src/shared 14`，其中函数/常量 72 条。已手工抽查 6 条（`createSecureSandbox`、`createTrustedSandbox`、`SKILL_EXECUTE_EVENT`、`CHANNEL_LIST_TOOL`、`readCache`、`resolveSharedCacheDir`）——**每条全仓只出现一次，就是它自己的声明**。

---

## 三、任务清单

| ID | 任务 | 状态 | 证据 / 落点 |
| --- | --- | --- | --- |
| **K0-1** | 补 `resources/app-ui-cli` 为 entry（被 spawn 调用，非 import） | ✅ 完成 | unused files 9 → 7 |
| **K0-2** | 加 `check:knip` 脚本并落盘配置 | ✅ 完成 | `pnpm check:knip` 可跑；见 §4.1 的 **pnpm add 陷阱** |
| **K0-3** | 更新本目录 README 与上级 README 的状态段 | ✅ 完成 | 两个 README 已加行 |
| **K1-1** | `packages/browser-control` 补 `ws` | ✅ 完成 | 4 处 import 无声明；版本对齐 apps/windows 的 `^8.16.0` |
| **K1-2** | 根 `package.json` 补 4 个开发脚本依赖 | ✅ 完成 | `@xenova/transformers` / `isomorphic-git` / `sherpa-onnx-node` / `sharp` |
| **K1-3** | `apps/windows` 的 `pi-agent-core` / `pi-ai` / `typebox` + `tar`：**声明 + 进 exclude 白名单** | ✅ 完成 | bundle 字节数不变（6,184,081）；见 §4.2 |
| **K1-4** | `isolated-vm`：沙箱"完整隔离"路径**从未生效** | 🔶 需决策 | 见 §4.2，**新发现** |
| **K2-1** | 清测试缝类真死（`_forTest` / `reset*ForTests` / `_reset*`） | ✅ 完成 | 9 条，双重取证 |
| **K2-2** | 清协议常量类真死（`SKILL_*_EVENT/METHOD`、`CHANNEL_*_TOOL`） | ✅ 完成 | 6 条，逐条查过字面量与活定义 |
| **K2-3** | 沙箱工厂 `createSecureSandbox` / `createTrustedSandbox` | ✅ 完成（删除） | 见下 |
| **K2-4a** | 遗留别名/副本/局部残留 | ✅ 完成 | 真死 95 → **68**；见 §4.3 |
| **K2-4b** | 剩余真死（约 66 条，含 23 类型） | ⬜ 待办 | 逐条过；**先分类再删**，别直接照 knip 清单删 |
| **K2-5** | `apps/windows/vite.test.config.ts` | ✅ 完成（删除） | unused files 2 → **1**（只剩并行会话在改的 `sprite-lab.ts`） |
| **K2-6a** | 接线 `readStoredTriggerThreshold`（阈值重启即丢） | ✅ 完成 | 见下 |
| **K2-6b** | 接线 `EvolvedToolInfo` / `PendingToolInfo`（契约被消费者抄了一份） | ✅ 完成 | 见下 |
| **K2-6c** | 采用 `AgentRuntimeCommandResult<T>` | ❌ **实测后判定不可"顺手接"** | 见下，代价已量化 |
| **K3-1** | 修 5 处坏导入（指向不存在的东西，tsc 与 vite 都看不见） | ✅ 完成 | unresolved 7 → 0；见 §4.4 |
| **K3-2** | `verify-approval-config.mjs` + 配套 `README-verify.md` | ✅ 完成（**改为整对删除**） | 目标模块已随 Gateway 架构移除；见 §4.4 |
| **K3-3** | 删 `packages/agent-runtime/src/kernel/index.ts`（死 barrel） | ✅ 完成 | unused files 3 → 2 |
| **K4-1** | 棘轮接入 `pnpm verify`：rules 分级 + 预算 | ⬜ 待办 | 见 §5 |
| **K5-1** | 批量 `knip --fix`（B/C/D 共 1259 条） | ⛔ 待批 | 见 §6 |

---

## 四、各阶段判据与退出条件

### 4.1 K0 —— 接入收尾（✅ 已完成）

- **K0-1 ✅**：`resources/app-ui-cli/{lumii-ui,lumii-sync}.mjs` 列为 entry（`lumii-ui.mjs` 自己 `import ./commands.mjs`，一并覆盖）。unused files 9 → 7。
- **K0-2 ✅**：脚本为 `pnpm dlx knip@6.38.0 --no-exit-code`。
- **K0-3 ✅**：本目录 README 文档表 + 上级 `代码重构/README.md` 状态段已加。

**另补三条有理由的 `ignore`**（unused files 7 → **3**，且 knip 的 `Configuration hints` 归零）：

| 文件 | 为什么不删 |
| --- | --- |
| `src/renderer/stubs/util.ts`、`src/main/stubs/qrcode-terminal.ts` | 构建必需：`electron.vite.config.ts` 的 `resolve.alias` 把 `import 'util'` / `'qrcode-terminal'` 指向它们，源码里永远不会出现指向它们的说明符。与 `scripts/check-wiring.mjs` 的 `REACHABILITY_EXEMPT` 同款豁免 |
| `resources/app-ui-cli/commands.d.mts` | `command-allowlist.test.ts` 靠 TS 的 `foo.mjs` ↔ `foo.d.mts` 自动配对拿类型，没有显式 import。knip 不建模这个配对 |
| `packages/agent-runtime/src/tools/mcp/windows-hide-spawn-preload.cjs` | Electron 的 preload 路径以字符串传给 spawn 的子进程 |

**knip 的配置提示另外纠正了我两处**：原来在根 `ignore` 里写的 `docs/**`、`verify/**`、`.tmp-petgen/**` 全是**多余的**——根 workspace 的 `project` 已限定为 `scripts/**`，这些目录本就不在作用域内。已删除，报告变干净。

> ⚠️ **pnpm add 陷阱（本轮实测，与本任务无关但影响接入方式）**
> 原本打算把 knip 装成根 devDependency。`pnpm add -Dw knip` 期间 pnpm 重新解析了依赖图，把 `@earendil-works/pi-ai` 的 **zod peer 从 `3.25.76` 抬到 `4.6.5`**（连带 `@anthropic-ai/sdk` 的 peer 变体也翻了）。
> 已用 `pnpm install --frozen-lockfile` 验证：**装之前锁文件与清单是同步的**，所以这是 `pnpm add` 的副作用，不是既有偏差。
> 处理：还原 `package.json` 与 `pnpm-lock.yaml`，改用 `pnpm dlx`（不碰依赖图）。
> **推论**：本仓任何一次 `pnpm add` 都会顺手抬 pi-ai 的 zod peer。将来要加依赖时，要么锁 zod，要么接受并单独验证。

**K0 退出时的残留 3 个 unused files**（都有归宿，不是烂尾）：

| 文件 | 归宿 |
| --- | --- |
| `packages/agent-runtime/src/kernel/index.ts` | K3-3 删除 |
| `apps/windows/vite.test.config.ts` | K2-5 需决策（删 / 或补 npm script 接上） |
| `apps/windows/pet-lab/sprite-lab.ts` | 正被并行会话修改，本轮不碰 |

### 4.2 K1 —— 依赖声明（**最高优先，防打包崩溃**）

58 条 unlisted 按风险分四组。**这一阶段真正的风险不在 package.json 本身，而在它会翻转 `electron-vite` 的外部化决策。**

取证（2026-09-24，`out/main/index.js` 今天 18:35 构建、配置 09-23 未变）：

```
@anthropic-ai   2 次   ← external 模块会以字符串出现（探针自校验通过）
isolated-vm     8 次
better-sqlite3  8 次
earendil        0 次   ← pi-ai / pi-agent-core / typebox 现在是【被内联打包】的
```

而 `electron.vite.config.ts:230` 的注释写「pi-ai / pi-agent-core 已通过 externalizeDepsPlugin（默认规则）外部化」——**这句是错的**：它们没在 `apps/windows/package.json` 里，插件不会外部化它们。同一文件 177 行的设计意图是反的：「只外部化 electron，其他依赖内联打包，避免 pnpm 传递依赖无法被 electron-builder 正确打包」。

| 组 | 内容 | 改法 | 为何安全 |
| --- | --- | --- | --- |
| K1-1 | `packages/browser-control` 的 `ws`（4 处） | 补进该包 dependencies | 不影响 apps/windows 的外部化集合 |
| K1-2 | 根 `scripts/` 用的 `@xenova/transformers`/`sharp`/`sherpa-onnx-node`/`isomorphic-git` | 补进**根** devDependencies | 根不参与 electron-builder |
| K1-3 | `apps/windows` 的 `pi-agent-core`/`pi-ai`/`typebox`（27 处） | **声明 + 同步加进 `externalizeDepsPlugin.exclude`** | 保持内联（行为不变），去掉对 `.npmrc` hoist 的隐性依赖 |
| K1-4 | `tar`（skill-exporter）/ `isolated-vm`（skill-sandbox） | **先取证**：是否真装、是否 optional | `isolated-vm` 已在 `external` 正则里但不在 `onlyBuiltDependencies`，疑似未构建 |

**退出条件**：`knip --include unlisted` 归零或每条都有书面豁免理由；`pnpm typecheck` + `test:all` 全绿；`git diff pnpm-lock.yaml` 只有预期改动。

#### K1 执行结果（2026-09-24）

`unlisted` **58 → 1**（只剩下面的决策项），`pnpm typecheck` 全绿，`pnpm verify` 见进度日志。

| 组 | 做了什么 | 证据 |
| --- | --- | --- |
| K1-1 | `packages/browser-control` 补 `ws: ^8.16.0`（与 apps/windows 同范围，不引入新版本） | 锁文件只给该 importer 加了 3 行 |
| K1-2 | 根 `package.json` 补 devDependencies：`@xenova/transformers`、`isomorphic-git`、`sherpa-onnx-node`、`sharp`（版本对齐 apps/windows） | 锁文件 `.` importer 从 `{}` 变为 4 条 |
| K1-3 | `apps/windows` 声明 `pi-agent-core` / `pi-ai` / `typebox` / `tar`，**同时**加进 `externalizeDepsPlugin.exclude` | 重建后 `out/main/index.js` **字节数完全相同**（6,184,081）；探针 `earendil` 仍为 0 |

**验证方法（可复现）**：改完重跑 `electron-vite build`，再用同一套探针比对新旧 bundle。`external` 的模块会以字符串形式出现（同文件 `@anthropic-ai` 有 2 处，`isolated-vm` 8 处），被内联的不会（`earendil` 0 处）。字节数相同即证明外部化决策未变。

**顺手修正了一处错误注释**：`electron.vite.config.ts` 原写「pi-ai / pi-agent-core 已通过 externalizeDepsPlugin（默认规则）外部化」——与实测相反（它们一直是被内联的，只因没声明）。已改写为准确描述，并说明为什么必须靠 `exclude` 的显式排除来维持内联。

> 🔶 **K1-4 决策项：`isolated-vm` 未安装，技能沙箱的"完整隔离"路径从未生效**
>
> - `apps/windows/src/main/skill-sandbox.ts:105` 用 `require('isolated-vm')` 尝试加载完整沙箱，失败则落到 **"降级模式（Function 构造器，安全性较低）"**，只打一条 **WARN**（按[error 日志盲区]的口径，WARN 不进 error 文件，等于隐形）。
> - 实测：`isolated-vm` **既没在任何 package.json 声明，也没装进 node_modules**（`node_modules/.pnpm/isolated-vm*` 无匹配）→ 该分支**必然**走降级。
> - 代码自己在 WARN 里写「生产环境建议安装 isolated-vm 以获得完整隔离保护」——但依赖没声明，这个"建议"**根本无法被执行**。
> - 要注意：`isolated-vm` 是原生模块，装它要同时处理 `onlyBuiltDependencies`（当前没有它）、Electron ABI 重建（postinstall 只重建 better-sqlite3/sharp）与 electron-builder 打包。
>
> **三个选项**：(a) 声明 + 配原生构建 + 验证打包（工作量大，且要过隔离实例冒烟）；(b) 保持现状，但把「沙箱降级」升成 ERROR 级日志并在文档里写明这是已知状态；(c) 删掉完整隔离分支，只留降级实现并明确它不安全。**这是安全语义决策，需要定夺。**

### 4.3 K2 —— 真死代码 95 条

**分批删，每批一次门禁**（`pnpm typecheck` + 相关包测试）。顺序按「确定度」从高到低：

1. **K2-1 测试缝**：`_forTest` / `reset*ForTests` / `_reset*` 前缀的，无人调用的测试专用导出。最安全。
2. **K2-2 协议常量**：`SKILL_EXECUTE_EVENT`、`SKILL_RESULT_METHOD`、`CHANNEL_LIST_TOOL`、`CHANNEL_SEND_TOOL` 等。**删前必须确认不是协议预留**——它们名字像协议面，删错等于改契约。
3. **K2-3 需决策项**：`createSecureSandbox` / `createTrustedSandbox`（`skill-sandbox.ts`）。安全相关，见 §6。
4. **K2-4 其余**：含 23 条类型导出。

**退出条件**：95 条逐条有归宿（删除 / 保留并写理由）；保留的进 knip 的 `ignoreExportsUsedInFile` 或 JSDoc 标记，不留无声豁免。

#### K2-1 / K2-2 执行结果（2026-09-24）：删 15 条

删前对每条做了**两道独立取证**（不只信 knip）：

1. 分类器判据：全仓没有任何文件在 import/export 子句里提到它；
2. 全仓逐条 `rg -c`：**每条命中文件数 = 1**（就是它自己的声明行）。第 2 道专门防「`(mod as any).xxx()` 这种非 import 形态的调用」——判据只看 import 子句，会漏这一种。

| 文件 | 删除的导出 |
| --- | --- |
| `shell/bash-provider.ts`、`shell/powershell-provider.ts` | `_resetBashPathCache`、`_resetPwshPathCache` |
| `main/coding-dev-acp-run.ts` | `resetAcpRunControllerForTests` |
| `main/client-data-root.ts` | `_resetClientStateDirCacheForTest`（合并后遗留的**等价别名**） |
| `main/desktop-notify.ts` | `resetDesktopNotifyForTests` |
| `main/channel/cross-channel-continuity.ts` | `__resetCrossChannelContinuity` |
| `main/pet/pet-perch-tracker.ts` | `_resetPerchTrackerForTest` |
| `main/skill-runtime.ts` | `SKILL_EXECUTE_EVENT`、`SKILL_RESULT_METHOD`、`SKILL_INSTALL_EVENT`、`SKILL_INSTALL_RESULT_METHOD`（4 个字面量全仓只在自己那行出现） |
| `main/channel/outbound-types.ts` | `CHANNEL_LIST_TOOL`、`CHANNEL_SEND_TOOL`（活定义在 `agent-runtime/src/tools/built-in/channel-tools.ts:12` 的 `CHANNEL_LIST_TOOL_NAME`，main 侧是重复定义） |
| `main/app-ui-control/server.ts` | `_getActiveTokenForTest`、`_getActiveControllerForTest`（见下） |

**`server.ts` 那两条牵出了连带清理**：删掉两个 getter 后，模块变量 `activeToken` / `activeController` 变成**只写不读**（声明 ×2、赋值 ×4、读取 ×2 全是这两个 getter）。查过 token 校验走的是**闭包参数**（`createRequestHandler(controller, token)`，1017 行 `bearer !== token`），不是这两个变量——它们是纯记账。所以连同变量与 4 处赋值一并删除，否则会留下"只写不读"的死状态，正是本轮要清的东西。

**验证**：`unused exports` 667 → **652**（正好少 15）；`pnpm typecheck` 全绿；接线守卫 + agent-runtime 2402 用例通过。

#### K2-4 的形态（进行中，含一个需要「接线而非删除」的反例）

**本仓真死代码的主要形态不是「整块没写成的功能」，而是「重命名/合并后留下的别名与副本」。** 已确认的样本：

| 形态 | 样本 |
| --- | --- |
| 等价别名 | `_resetClientStateDirCacheForTest`（注释自己写着「与上一个等价的别名」）；`wiki-embedding-model-path.ts:63,71` 的 `isBundledWikiEmbeddingModelReady = isWikiEmbeddingModelReady`、`resolveBundledWikiEmbeddingModelDir = resolveWikiEmbeddingModelDir` |
| 单复数/前缀变体 | 同文件 `resolveWikiEmbeddingModelsDir`（复数）vs 在用的 `resolveWikiEmbeddingModelDir`（单数） |
| 跨包重复定义 | `CHANNEL_LIST_TOOL` vs `CHANNEL_LIST_TOOL_NAME` |
| 局部残留 | `schedule-helpers.ts`（**活的 327 行模块**）里 5 个导出无人用，消费方用的是同文件的另一批 |

> ⚠️ **反例：有些「没人 import 的导出」不该删，该接线。** 两个实例：
>
> 1. **契约被消费者抄了一份。** `apps/windows/src/shared/agent-runtime-commands.ts:1495/1509` 的 `EvolvedToolInfo` / `PendingToolInfo` 无人 import——因为 `renderer/pages/SettingsPage/components/EvolvedToolsSection/index.tsx:18/28` **自己又声明了一遍**。而且**本地版本更新**：`PendingToolInfo` 在 shared 里是 5 个字段，本地是 10 个（多 `whenToUse?` / `whenNotToUse?` / `samples?` / `similarApproved?` / `lowValueReason?`）。**删 shared 那份等于丢掉契约，正确动作是反过来让组件 import shared 并先把 shared 补齐。**
> 2. **类型安全的返回契约从未被采用。** `AgentRuntimeCommandResult<T>` 是约 60 行的条件类型映射（把每个 command type 映射到它的返回类型），**全仓出现 1 次——就它自己**。写了但没人用，与 ⑦ 类守卫发现的「command 未接分派」是同一类问题的不同侧面。
>
> 这两条**不进删除清单**，进「接线」清单。第三条同类：**`readStoredTriggerThreshold`**（`main/ipc/agent-runtime/tool-commands.ts`）——那里 `persistTriggerThreshold` 写、`readStoredTriggerThreshold` 零调用，意味着**阈值落盘后从没被读回，重启即丢**。删掉这个"没人用的导出"会把 bug 一起删掉（bug 就消失了，但功能也没了）；正确动作是把它接上。

#### K2-4a 执行结果（2026-09-24）：删 14 条 + 3 条级联

真死总数 **95 → 68**。这一批全是「上一版实现的遗留」：

| 文件 | 删除内容 | 判据 |
| --- | --- | --- |
| `wiki-embedding-model-path.ts` | `isBundledWikiEmbeddingModelReady`、`resolveWikiEmbeddingModelsDir`、`resolveBundledWikiEmbeddingModelDir` | 三条的 JSDoc 自己写着 **`@deprecated 使用 X`**，而 X 就是在用的那个 |
| `main/paths.ts` | `resolveSharedCacheDir`、`resolveSharedTempDir`、`resolveRfsRootDir` | 同段落里 `resolveSharedLogsDir` / `resolvePerfLogsDir` / `resolveLegacyWorkspaceDir` 都在用，只有这三条零调用 |
| `wiki-vault-sync.ts` | `refExtForSource` | 展示用后缀推断，零调用 |
| `wiki-ero-extractor.ts` | `clampEroExtractMaxPages` | 同文件 `clampEroExtractMaxChars` 在用，pages 那条零调用 |
| `schedule-helpers.ts` | `datetimeLocalToMs`、`getSystemTimezone`、`getTimezoneOptions`、`buildCronExpr`、`describeCronExpr`、`matchPreset` ＋ 级联的 `fmtTime`、`PRESETS`、`SchedulePreset`、`SelectOption` | **上一版 CreateJobModal 的遗留**：现版 `CreateJobModal.tsx` 里"预设""时区"一个字都没有；`describeCronExpr` 也已被 `cron-utils` 的 `describeCron(job)` 取代 |

> ⚠️ **这一批我犯了一个错，被 tsc 拦下，值得记下来。**
>
> 级联项是我**在 knip 报告里 grep 符号名**找出来的，于是把 `parseCronExpr` 也当成了删除目标——它确实没人 import（knip 说得对），但它**在文件内被 `getNextCronRun` 调用**（`schedule-helpers.ts:115`），属于 §2.3 分类里的 **B 类「仅文件内使用」**，不是 A 类。
>
> 教训：**knip 的 `unused exports` 清单不能直接当删除清单用**——它把 A/B 两类混在一起。要么用本计划的分类器过一遍，要么就靠 `pnpm typecheck` 兜底。这次是后者救的场（`error TS2304: Cannot find name 'parseCronExpr'`），已恢复 `parseCronExpr` 与它依赖的 `CronFields`。
>
> 换句话说：**「knip 报的都删」和「knip 报的都不删」都错**，判据要走在删除前面。

### 4.4 K3 —— 坏导入与死文件（✅ 已完成）

`unresolved` **7 → 0**，`unused files` 3 → 2。

**K3-1：比计划里判断的更严重。** 计划写的是「指向目录，扩展名不对」，取证后发现是**指向了根本不存在的东西**：

| 文件 | 原文 | 真相 |
| --- | --- | --- |
| `compact/policy.test.ts:4`、`compact/transform-context-phase1.test.ts:5` | `import type { AgentMessage } from "../types.js"` | `src/types/index.ts` **不导出 `AgentMessage`**——它来自 pi SDK；全仓非测试文件一律写 `from "@earendil-works/pi-agent-core"` |
| `compact/strategies/{micro-compact-phase1,proactive-prune}.test.ts:3` | `from "../../types.js"` | 同上 |
| `storage/schema-autonomous-p2.test.ts:7` | `import type { DatabaseAdapter } from "./types.js"` | `storage/types.ts` 不存在；`DatabaseAdapter` 在 `./local-database.js`（同目录 `schema-v47/v49.test.ts` 都这么写） |

**为什么两个工具链都看不见**：`packages/agent-runtime/tsconfig.json` 的 `exclude` 含 `src/**/*.test.ts`（tsc 不检查测试文件），而它们又全是 `import type`（vite/esbuild 直接擦除，运行时不解析）。**这两条叠加 = 该路径从未被任何工具验证过。**

修法：5 处全部改成与同目录既有文件一致的来源。验证：`vitest run src/compact src/storage` → 36 文件 / 282 用例通过；knip `unresolved` 归零。

**K3-2：计划的前提不成立，改为整对删除。** 该脚本 import 的 `GatewayClient`（`../src/main/gateway-client.js`）与 `ExecApprovalsManager`（`../src/main/exec-approvals-manager.js`）**两个模块都已随 Gateway 架构移除**（`find -name 'exec-approvals*'` 无结果；`GatewayClient` 只存在于文档与一个独立的 python harness）。所以「修路径」无从谈起——脚本一运行就 `ERR_MODULE_NOT_FOUND`。配套的 `README-verify.md` 还在教人 `export GATEWAY_URL="ws://127.0.0.1:18789"` 然后跑它，描述的是已移除的架构。两者自 2026-08-04 初始提交起未改动，只互相引用。**整对删除**（免审批功能本身还在，落在 `agent-runtime/bridge-instance-factory.ts` 等处）。

**K3-3：** `packages/agent-runtime/src/kernel/index.ts`（3 行 barrel）无人 import 裸 `kernel` 说明符，包的 `exports` 也没暴露 `./kernel`，删除。

### 4.7 K2-3 / K2-5 / K2-6 —— 三个决策项的执行（2026-09-24）

#### K2-3 ✅ 删两个沙箱工厂

`createSecureSandbox`（64MB/10s）与 `createTrustedSandbox`（256MB/60s + 允许网络与文件系统）无人调用——运行时用的是 `createDefaultSandbox()`，而 `DEFAULT_OPTIONS` 本身就是限制性的（128MB/30s、无网络、无文件系统）。**删掉不改变任何行为**：那两个只是预设包装，其中"更宽松"的那个还没被用过（是好事）。

#### K2-5 ✅ 删 `vite.test.config.ts`

24 行手工 Vite 配置，无 npm script 引用；且 renderer 需要 `window.electronAPI`（由 preload 注入），这份配置里**没有任何替身**，真跑起来只会是坏页面。所以它不是"待接线的能力"，是上一版调试手段的残留。

#### K2-6a ✅ 接回触发阈值

`handleToolEvolutionSetTriggerThreshold` 一直调 `persistTriggerThreshold` 落盘，但**没有任何地方读回**——用户设过的值重启即丢。接法：在 `engine-assembly.ts` 里引擎装配完成后 `engine.setTriggerThreshold(readStoredTriggerThreshold(bridge))`。

> 一个诚实的前提：这个阈值**已经弃用**（引擎里的日志与常量注释都写着"不影响调度"，实际挖掘用的是 `minSamples: 5` / 周窗口 Top5 / count>100）。所以这条修的是**设置页显示的一致性**，不是恢复某个能力。彻底的做法是把这个弃用旋钮从设置页摘掉——那属于产品决策，未做。

#### K2-6b ✅ 契约并回，消费者改为 import

`EvolvedToolsSection/index.tsx` 自己声明了一份 `EvolvedToolInfo`/`PendingToolInfo`，而 shared 里那份没人 import。做法是**反过来**：把本地副本多出来的 5 个字段（`whenToUse?` / `whenNotToUse?` / `samples?` / `similarApproved?` / `lowValueReason?`）并回 shared，组件改为 `import type { ... } from '@shared/agent-runtime-commands'`。此后契约只有一份。

#### K2-6c ❌ `AgentRuntimeCommandResult<T>`：实测后判定不是"顺手接"的活

那份 569 行的条件类型映射从没被采用，看着像"接一下就完了"。**实际改完跑了 tsc，14 个错误**，暴露出三件事：

1. **映射只覆盖 115 / 179 条命令（64 条缺失）**，兜底分支是 `never` —— 未覆盖的命令在调用点会变成 `never`，比不接还难用。
2. **renderer 看到的类型根本不由它决定**。preload 里另有一份**手写接口**（`preload/index.ts` 的 `ElectronAPI`，声明了 17 个方法的精确返回类型），renderer 拿到的是那份。而它那 17 个方法对应的命令——`runtime:enabled`、`agentDefinition:*`、`agentInstance:*`、`storage:*`——**全部落在未覆盖的 64 条里**。所以映射表目前**替代不了**那份手写契约。
3. **参数收紧会暴露真实的调用点漂移**：`ChatPage.tsx:542` 传 `Record<string, unknown>`、`slash-command-executor.ts:367` 传的 `codingDev:setBackend` 缺 `scope`/`accountId`、`ChatInput` 传的 `codingDev:getBackend` 缺字段——这些调用点今天能跑，是因为签名是 `unknown`。

**结论与代价**：要用它，得按顺序做三件事——(a) 补全缺失的 64 条映射；(b) 把那 17 个方法的类型**改为从映射派生**（删掉三份契约里的一份）；(c) 处理 8 处调用点漂移（每处都要先确认主进程那边是不是真的接受缺字段）。这是一项**独立工作**，不是本轮的收尾动作。已撤回尝试，留着不动。

> 撤回也说明一件事：**"没人用的导出"里，有些的修复成本远高于删除成本，而删掉又会丢契约** —— 这类必须先量化再动手，不能凭"接上应该很快"就开工。

### 4.5 K4 —— 棘轮

接进 `pnpm verify`，但**分级**而不是一刀切：

| 类型 | 级别 | 理由 |
| --- | --- | --- |
| `files` / `unresolved` / `unlisted` / `dependencies` / `devDependencies` | `error` | 清完即零，防新增 |
| `exports` / `types` | `warn` | K5 未做前不许拦；否则 CI 必红 |

另设 `--max-issues` 预算兜底（数字在 K4 时按当时的真实存量定，不预设）。

### 4.6 K5 —— 批量 fix（⛔ 待批）

`exports`/`types` 是 knip 标注 🔧 可自动修的类型，`knip --fix` 能处理 B/C/D 三类共 1259 条。**但必须先解决两件事**：

1. knip **看不见运行时字符串访问**——`window.electronAPI.x()`、CDP `evaluate('...')`、`resources/app-ui-cli` 的设计表字符串调用。守卫 ⑥ 类就是为此存在的。所以 fix 之后必须 `pnpm verify` 全绿 + 至少一次 `dev:restart` 冒烟。
2. 先小范围试点（例如 renderer 的 `components/ui/index.ts` 一个 barrel），确认 diff 可读、无副作用，再扩大。

---

## 五、验收门禁

每批改动后依次跑：

```bash
pnpm typecheck            # 全 workspace
pnpm check:wiring         # 守卫（--strict）
pnpm --filter lumii-windows test:all
npx knip --no-exit-code   # 看存量是否按预期下降
```

K4 之前 `knip` 不进 `verify`；K4 之后以 `pnpm check:knip` 形式进入。

> ⚠️ **`pnpm verify` 目前是红的，但红的不是本轮改动**（2026-09-24 实测）
>
> `pnpm verify` 在第二步 `check:large-files` 就中断，报：
>
> ```
> packages/agent-runtime/src/storage/schema.ts: 1809 → 1840 行（增长 31，允许 20）
> packages/pet-core/src/state/notice.ts: 936 行（新增超标文件，阈值 800）
> ```
>
> **取证**：这两个文件在工作区**没有任何改动**（`git status` 干净），最后一次提交分别是 `3cd4d34e`（宠物六期）与 `092dcaff`（宠物五期）——都是**其他会话已提交的工作**。也就是说门禁是在 HEAD 上红的，与 knip/依赖声明无关。
>
> **没有跑 `--update`**：那会把别人正在推进的文件增长一次性"洗白"，正是这道门禁要防的事。该由改动方决定「拆文件」还是「显式接受增长」。
>
> **副作用**：`verify` 是 `&&` 串联，`check:large-files` 失败导致**接线守卫与全部测试根本没跑**。本轮 K1 是拆开单独跑补齐的（见进度日志）。建议把 `verify` 改成汇总式（跑完所有步骤再统一报告），否则一道红门禁会遮蔽后面所有门禁。

---

## 六、风险与禁区

| 项 | 判定 |
| --- | --- |
| `.tmp-petgen/`（9 个文件） | **不动**。未跟踪，是 pet 精灵图的在制工作，可能属并行会话 |
| `apps/windows/pet-lab/sprite-lab.ts` | **不动**。git status 显示正被另一会话修改 |
| `apps/windows/src/renderer/stubs/util.ts`、`src/main/stubs/qrcode-terminal.ts` | **不删**。构建必需，守卫里已有 `REACHABILITY_EXEMPT` |
| `createSecureSandbox` / `createTrustedSandbox` | **待决策**。已取证：`skill-runtime.ts` 用的是 `createDefaultSandbox()`，而 `DEFAULT_OPTIONS` 本身就是限制性的（无网络、无文件系统、128MB/30s）。两个死工厂只是预设包装：一个更严（64MB/10s）、一个更松（256MB/60s + 允许网络/文件系统，无人使用是好事）。**删掉不改变安全语义**，但要确认不是打算将来当开关用 |
| `isolated-vm` | **待决策**，见 §4.2 K1-4。沙箱降级路径一直在跑，且只有 WARN |
| **任何 `pnpm add`** | **会重新解析依赖图**。实测：knip 依赖 `zod@^4.4.3`，一装就把 `@earendil-works/pi-ai` 的 zod peer 从 `3.25.76` 抬到 `4.6.5`。而本仓**没有任何 package.json 声明 zod**（它只作为 pi-ai 的 peer 存在）→ 任何带 zod 4 的新包都会再次触发。**加依赖后必须检查锁文件里的 zod 行** |
| **中文注释的编码损坏（U+FFFD）** | 全仓扫描 2543 个已跟踪文件，**只有 2 个**含有损字节：`apps/windows/src/preload/index.ts`（108 处）与 `renderer/pages/SettingsPage/SettingsPage.module.css`（20 处）。`git log -S` 指向更早的 `be8acbeb`（preload 那次「消除重复的能力模块调用」）——**不是本轮造成的**；本轮对这 2 个文件的改动都带「损坏数不变」的断言（`preload/index.ts` 改一行前后都是 108）。修法是从父提交逐行恢复注释文本，属独立收尾任务，未做 |
| `pi-*` / `typebox` / `tar` 的 package.json 改动 | 必须**同时**改 `externalizeDepsPlugin.exclude`，否则翻转外部化决策；改完按 §4.2 的 probe 复核 bundle |
| 并行会话 | 提交按文件显式 `git add`，不用 `git add -A` |

---

## 七、进度日志

> 提交哈希逐条列出，便于回溯；`27a86056` 的 `knip.jsonc` 初版由并行会话提交（内容与本计划一致）。

| 提交 | 任务 | 动作 | 证据 |
| --- | --- | --- | --- |
| `27a86056` | — | 落 `knip.jsonc` 初版（并行会话扫入提交） | — |
| `3c320fd7` | **K0** | 补 CLI entry；三条有理由的 `ignore`；删根 `ignore` 里多余的三条；加 `check:knip` 走 `pnpm dlx` | unused files 9 → **3**，`Configuration hints` 归零 |
| `1cf3ce16` | **K1** | 补 57 条未声明依赖；`pi-*`/`typebox`/`tar` 走「声明 + exclude」保持内联；修正一处错误注释 | unlisted 58 → **1**；bundle 字节数不变 |
| `34448703` | **K3** | 删 Gateway 遗留的 `verify-approval-config.mjs` + `README-verify.md` + 死 barrel `kernel/index.ts` | unused files 3 → 2 |
| `aa369684` | **K3 补交** | 上面那次漏 `git add` 的 5 处坏导入修复，单独补交（门禁当时是在含这些改动的工作区上跑的，结论不受影响） | unresolved 7 → **0** |
| `4834e649` | **K2-1/2** | 删 15 条（9 测试缝 + 6 协议常量），含 `server.ts` 的连带清理 | unused exports 667 → **652** |
| `51ca85d3` | **K2-4a** | 删 14 条遗留别名/副本 + 3 条级联；误删的 `parseCronExpr` 经 tsc 拦下后恢复 | 真死 95 → **68**，unused exports → **637** |
| （本轮） | **K2-3/K2-5** | 删两个沙箱工厂；删 `vite.test.config.ts` | unused files 2 → **1**，unused exports 637 → **634** |
| （本轮） | **K2-6a/b** | 接回触发阈值（装配时从 runtime_state 读回）；契约并回 shared，组件改为 import | knip 里这三项已消失 |
| （本轮） | **K2-6c** | 尝试采用 `AgentRuntimeCommandResult<T>`，**实测 14 个类型错误后撤回** | 覆盖 115/179；详见 §4.7 |

### 各阶段的一次性取证（命令可复现）

| 时间 | 内容 | 证据 |
| --- | --- | --- |
| 2026-09-24 | 首轮 knip 实测（无配置）：1862 条 / 727 文件 | `knip --no-exit-code` |
| 2026-09-24 | 归因：renderer 入口 bug + 非模块图成员 | unused files 282 → 9 |
| 2026-09-24 | 按引用形态分类 1453 条；**纠正自己的 46% 假阳结论**（实际假阳 2 条） | 分类器脚本 |
| 2026-09-24 | 载体取证：pi-* 当前为内联打包，推翻 config 注释 | `out/main/index.js` 探针 |
| 2026-09-24 | `pnpm add` 陷阱：knip 依赖 `zod@^4.4.3`，会把 pi-ai 的 zod peer 从 3.25.76 抬到 4.6.5 | `pnpm install --frozen-lockfile` 通过 = 原锁文件同步 |
| 2026-09-24 | `pnpm verify` 当前红在 `check:large-files`（他人提交的 `schema.ts` / `notice.ts` 超标），**不是本轮改动** | `git status` 干净 + 最近提交为他人的宠物五六期 |
| 2026-09-24 | 每批删除后：`pnpm typecheck` + 接线守卫 `--strict` + agent-runtime/lumii-windows 全量 | 6792 / 4036 / 5958 用例逐批通过，无 `Errors` 行 |
