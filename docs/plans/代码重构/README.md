# 代码重构 · 技术债治理

Lumii 全仓技术债治理的实施记录。**本目录由三个原目录合并而来**（2026-09-17）：

| 原目录 | 说明 |
| --- | --- |
| `plans/代码重构瘦身/` | 总报告 + 死代码清理 |
| `plans/大文件重构分析处理/` | >400 行文件的分级索引与拆分方案 |
| `plans/客户端优化/` | renderer 的 6 片纵向切片 |

三者本是同一条线：大文件分析是前代体检，客户端切片是总报告「批次 2/3」在 renderer 一侧的重新排期。合并后按「总报告 → 分析 → 执行」排列。

## 文档

| 文档/目录 | 说明 |
| --- | --- |
| [`2026-09-12-源码重构瘦身分析报告.md`](2026-09-12-源码重构瘦身分析报告.md) | **总报告**：全仓体检、六类问题清单、批次 0–3 计划。先读这篇 |
| [`大文件与死代码/`](大文件与死代码/) | >400 行文件分级索引、死代码清理清单（含逐项复核证据）、Gateway 遗留代码分析 |
| [`客户端切片/`](客户端切片/README.md) | renderer 纵向切片 6 片：样式地基、ui 组件库收敛、Wiki 样式归位、ChatPage 结构、数据层规范、消息列表虚拟化 |

## 核心结论（摘自总报告）

**拆分方案不是瓶颈，「拆完没有东西阻止它长回去」才是。**

2026-08-21 的大文件拆分方案本身合理，但执行后缺少门禁，文件在 3 周内重新膨胀（`bridge.ts` +83%、`agent-runtime-commands.ts` +117%）。

因此批次顺序不可颠倒：**先清库 → 再立门禁 → 后抽象 → 最后拆大文件**。

> 现状记录（2026-09-12 口径）：仓库无 ESLint / Prettier / CI，`.githooks/pre-commit` 内容为 `exit 0`。

## 状态

- 批次 0（清库）：进行中
- 批次 1（死代码清理）：部分完成
- 批次 2/3：未开始；其中客户端一侧已改以「纵向切片」方式推进，6 片全部完成
